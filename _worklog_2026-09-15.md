# 工作日志 2026-09-15

本轮目标（用户指令）：找创新点 / 多出数据结果 / 多出图 / 完善论文 / 安装 nature-skills。
约束：**每一步都要有依据**；工作留痕；做完提交。

---

## 0. 环境侦察结论（决定了后面能做什么）

| 项 | 状态 |
|---|---|
| conda / torch / CUDA 环境 | **不存在**（README 里的 `mgfnet` 已没了） |
| 数据集 `.h5` | **全盘 0 个** |
| checkpoint `*.pth` | **全盘 0 个**（`.gitignore` 忽略，find 计数 0） |
| GPU | RTX 3050 **6GB**（README 写 4GB），当时 4GB 被 `UnrealEditor.exe` 占用 |
| 网络 | 直连不通；经 Steam++ 加速器（hosts 劫持到 127.0.0.1）+ Windows 证书库可用 |
| LaTeX 工具链 | **无**（tectonic / pdflatex 都没有）→ `.tex` 无法编译验证 |

**推论：本轮一切新训练/新评测都做不了。** 能做的只有两类：
（a）对已有 172 份产物的再分析；（b）论文文本与图表的修订。

网络细节（重要，后续会用到）：shell 出网必须
`curl --ssl-revoke-best-effort`（保留证书验证，只放宽 CRL 检查）。
**不要**用 `git -c http.sslVerify=false`（那会关闭验证）。
git 走 `-c http.sslBackend=schannel` 可直接用 Windows 证书库。

---

## 1. 🚨 核心发现：§5.3 的 S1 闭环数字查无出处（已修）

**这是本轮最重要的发现，且我独立复核过，不是转述 agent 结论。**

事实链：

| 检查 | 结果 |
|---|---|
| 论文 §5.3 表格 S1 `unconstrained` | 写 **1.25e-01** |
| 仓库唯一 S1 产物 `roundtrip_checkpoints_w3.json` | **0.14916279911994934，`n: 1`** |
| 同一行 `pr` / `fixed` | 1.619e-07 / 1.638e-07 —— 与文件**逐位吻合** |
| S2 那格 1.45e-01 | 与 `roundtrip_checkpoints_lit.json` 均值 0.145137 **吻合**（真 n=3） |
| `calibrate_mismatch.py` | `TARGET = 1.253e-01  # …（论文 §5.3）` ← **循环出处** |
| `TARGET_LO, TARGET_HI = 0.11, 0.15` | **标定旋钮**，被论文当成"S1 逐种子范围" |
| `fig2_mech.png` | **读那份 JSON 画的 → 图上印的是 1.49e-01** |

即：**三格对得上、一格对不上**；且**图与表在论文最核心的机制数字上互相打架**。
起因是上一轮勘误（commit e9d7113）修掉了"99% 查无出处"，却用一个同样查无出处的数替换。

**结论不受影响**：换成实测 14.9% 后"六个数量级"仍为 6.0 个数量级；
干预实验的因果论证锚在**实测的 8.7%**（ε=0.222），单调性与显著性均不变。
坏掉的只是**可复现性与图表一致性**。

**已改**（三处副本 + 表注 + 脚本注释）：
`conference_paper.md` / `conference_paper_zh.md` / `latex/main.tex` /
`calibrate_mismatch.py` / `make_figures.py` / `README.md`。

---

## 2. 新增证据（零算力，全部来自已有产物）

新增脚本 `experiments/analyze_extended.py` → 输出 `analysis_extended.json`。

**先验证了三个对齐假设**（`_explore_artifacts.py`，非猜测）：
- `test_per_sample` 前 211 条 = L506，后 224 条 = L067（段均值与报告值**差 0.00e+00**）
- 所有 S1 运行 435 条、S2 运行 211 条
- 相邻切片 PSNR 差分 0.267 vs 打乱后 2.18（**8.2 倍**）→ 顺序确实按切片保持

**新结论：**

| 指标 | 数值 | 意义 |
|---|---|---|
| Cohen's $d_z$ | pr−unc **+4.67**、pr−fixed **+4.78** | 效应量极大，非边际 |
| MDES (n=5, 80% power) | **0.136 dB** | 本设计的检出下限 |
| \|Δ\|/MDES | pr−fixed **2.01×**；unc−fixed **0.18×** | 收益在检出线上，零结果在**下 5.6 倍** |
| 方差分解 | 逐切片 SD 1.75–1.80 vs 种子间 SD 0.030–0.087 | 大 **20–60 倍** → 以种子为单元的依据 |
| Levene | p=0.167 (S1) / 0.630 (S2) | pr 臂波动偏大但**无法证实**（防过度声称） |
| SSIM | pr 0.8762 > fixed 0.8729 ≈ unc 0.8725；$d_z$ +4.07/+3.40 | **复现同一排序** |
| best_epoch | **几乎全是 30/30** | ⚠️ 训练**未收敛**（论文从未提） |

**对论文主张的实质升级**：把"p=0.11，分辨不出"改成**定量上界**——
"任何真实差异 < 0.14 dB，而 PR 的收益是 0.272 dB，约为其两倍"。

---

## 3. 新增 5 张图（`experiments/make_figures_ext.py`）

`fig5_slice_dist` / `fig6_convergence` / `fig7_forest` / `fig8_power` / `fig9_perpatient`

配色经 dataviz 校验器**实测**通过：`#2E5EAA,#D1495B,#7B52AB`（CVD ΔE 14.3 protan）。
原拟用中性灰 `#6C757D` 给 `fixed`，**实测未通过**（色度 0.016 读作灰 + 与红色 protan ΔE 5.9），
故改紫色；灰色只留给参考线。

**修掉两个自己引入的 bug**（渲染后逐张看图才发现）：
1. 森林图 S1 组标签被裁到画布外（轴反转后 ylim 未留白）
2. 对数轴次刻度标签泄漏出 `4×10⁰` / `3×10¹`
3. **fig6 横轴错误**（由 workflow 发现）：`val_psnr` 只有 **6** 个点（每 5 轮验证一次），
   我按 `min(len)` 截断后把它们画在 x=1..6 —— **时间轴压缩 5 倍**。
   已改为各用各的 x 坐标（训练 1..30，验证 5..30）。
   *注：`train_l1` 的统计量不受影响，论文引用值本来就是对的。*

---

## 4. 创新点挖掘 workflow：37 个候选 → **只活下来 3 个**

42 个 agent、473 万 token。侦察出 37 个候选贡献点，对抗性验证后 **34 个被驳回**。
**存活的 3 个全部是第 1 节那个勘误，没有一个新的创新点。**

被驳回的（含我本来也想推的）：
- "bound 消融" → 论据是**手工设定的** taps 扫描，非训练漂移；论文措辞把它写成学习结果
- "逐切片显著性分析" → 与论文明文的"以种子为分析单元"冲突，一句 pseudoreplication 就打回
- "加理论闭式解" → 教科书内容，且**低估论文自身可行域最坏情形 11 倍**
- "方差反转 / 未收敛" → 已在 limitation，且由单个种子驱动

完整方案存于 `_workflow_plan.md`（含可直接用的摘要/引言英文改写草稿）。

---

## 5. nature-skills：**评估完成，安装受阻于网络**

仓库 `Yuan1z0825/nature-skills` **真实**：41,597 star、Apache-2.0、2026-04-24 建、当日仍在更新、20 个子技能。

**对 CS 会议论文的适用性（这是本节的实际交付）**：

| 子技能 | 判断 |
|---|---|
| `nature-figure` | ✅ 出版级绘图，与期刊无关 |
| `nature-statistics` | ✅ 统计报告规范 |
| `nature-ref-verifier` | ✅ 引文核验 |
| `nature-polishing` | ✅ 英文润色 |
| `nature-reviewer` | ✅ 模拟审稿 |
| `nature-writing` / `nature-reader` | ❌ Nature 系正文结构 ≠ 4–6 页会议论文 |

**安装失败，原因非脚本**：`raw.githubusercontent.com` 稳定返回 **502**
（20 分钟前同一 URL 尚可返回 11840 B）——加速器侧故障/限流。
codeload tarball **不支持 Range**（无 Content-Length），`curl -C -` 静默产出 9.5 MB 截断文件。
已改为 git clone + 6 次退避重试后台进行中。

抓取脚本保留在 `LDCT/experiments/_fetch_nature_skills.sh`（逐文件、可续传、带重试）。

---

## 6. 环境重建进展

- Miniconda 装到 `D:\huan.yue\tools\miniconda3`（本地、`/RegisterPython=0 /AddToPath=0`、可逆）
- `prlwt` 环境已建（Python 3.10.21）
- **用 conda-forge 而非默认频道** —— 默认频道要求接受 Anaconda ToS（含商用限制），
  那是法律协议，**不应由我代用户接受**
- torch 2.5.1+cu121 下载中

**数据仍阻塞**：AAPM 官方 Box（`app.box.com`）从本机**连不上**（连接 8.7.198.45:443 后挂起），
已派 agent 找可达镜像。

---

## 7. 本轮产出的文件

新增：
```
LDCT/experiments/analyze_extended.py     扩展分析（效应量/功效/方差分解/收敛）
LDCT/experiments/make_figures_ext.py     5 张新图
LDCT/experiments/_explore_artifacts.py   对齐假设验证（只读）
LDCT/experiments/_fetch_nature_skills.sh 技能抓取（可续传）
LDCT/experiments/run_redcnn_seeds.ps1    RED-CNN 补到 n=5
LDCT/experiments/run_seeds10.ps1         三臂 n=5→10
LDCT/experiments/run_ablation.ps1        bound / n_taps / levels 扫描
LDCT/experiments/analysis_extended.json  全部分析数字
LDCT/paper/figures/fig5..fig9_*.png      5 张新图
_workflow_plan.md                        创新点重构方案（含英文改写草稿）
_workflow_survivors.md                   workflow 存活的 3 项详证
```

修改：
```
LDCT/paper/conference_paper.md      勘误 + 效应量/MDES/SSIM + 3 条新局限 + 图注附录
LDCT/paper/conference_paper_zh.md   同步勘误
LDCT/paper/latex/main.tex           同步勘误 + 表格列 + fig8 + 局限 + SSIM
LDCT/models/denoiser.py             暴露 n_taps / bound（默认值不变，零影响）
LDCT/experiments/train.py           同上 + config 记录
LDCT/experiments/calibrate_mismatch.py  去掉循环出处
LDCT/experiments/make_figures.py    修掉同样无源的注释
README.md                           二次勘误说明
```

**未做（等环境）**：所有新训练；S1 闭环 n=1→n=5；bound 剂量-响应；
RED-CNN 多种子；n=10；跨库泛化。

**未验证**：`.tex` 无法编译（无 LaTeX 工具链）；SSIM 与参考实现的一致性未核。
