# LDCT 去噪 PSNR 口径调研报告

**结论先行**：AAPM 官方从未定义过任何 PSNR 口径（官方指标是放射科医师阅片评分）；这个数据集上不存在唯一事实标准，至少并存 5 个互不兼容的阵营，同一个模型换口径可差 20 dB。对你手里这 10 患者 3mm 子集，应当采用 **RED-CNN/CTformer 谱系口径（下称"口径 A"）**。

---

## 一、结论：该用哪个窗

### 推荐（主口径）：口径 A —— 训练 [-1024, 3072] 归一化，评测裁剪 [-160, 240]

| 阶段 | 操作 |
|---|---|
| 预处理 | `HU = pixel_array * RescaleSlope + RescaleIntercept`（=-1024），**不裁剪**，`x = (HU + 1024) / 4096` → [0,1] |
| 训练 | 直接喂上面这个 `x` |
| 评测 | 网络输出先反归一化回 HU：`x * 4096 - 1024`，再把**预测与参考同时** clip 到 `[-160, 240]` HU |
| PSNR | `10 * log10(400² / MSE_HU)`，即 `data_range = 400`，RMSE 单位为 HU |
| SSIM | skimage，`data_range = 400`（等价 `C1 = (0.01·400)² = 16`，`C2 = (0.03·400)² = 144`） |

**理由（按权重排序）**

1. **数据本体完全对口**。口径 A 的源头 SSinyu/RED-CNN 用的就是 10 患者 3mm 512×512 quarter/full 配对，与你手里的数据同一批。上游材料记录了 SSinyu 论文语境下的「2,378 对 10 患者」，你的 2398 对与之吻合。跨口径比较时，**测试集是不是同一批患者和同一种划分，比窗宽本身更容易毁掉可比性**。
2. **它是 2023-2026 的实际收敛点**。CTformer（PMB 2023）、EHSANet（2025）、MRED-Net / SDCNN（2025）都是 SSinyu 那份 `prep.py/loader.py/main.py/measure.py/solver.py` 模板的直接分叉，参数名与默认值（`--norm_range_min -1024` / `--trunc_min -160` / `--trunc_max 240`）逐字相同。选它，你的数字能直接落到 30-35 dB 那一档文献里。
3. **有一个独立来源交叉验证**。Eulig et al.（Med Phys 2024，ldct-benchmark）是唯一把统一口径写死并开源的第三方权威基准，其腹部检查窗 `L = (1074, 400)`（HU+1024 域）换算回 HU 即 **[-150, 250]**——与口径 A 的 [-160, 240] 只差 10 HU 平移，同为 400 HU 宽软组织窗。**两个互不相关的团队收敛到同一个 400 HU 窗，这是"事实标准"最硬的证据。**
4. **不要用你当前的口径去发表**。`[-1000, 1000]`（R=2000）不属于上述任何一族的公共口径，它把 PSNR 系统性抬高约 14 dB，与 A 族数字直接并列会被认为是量纲错误。

### 不推荐但在特定场合可用的替代

- **若要复现 CT-Mamba 的 40+ dB 数字**：需 `data_range = 4096`、且评测**不做任何 clip**（其 `[-160+1024, 240+1024]` 只传给了出图函数 `save_fig`，不进 `compute_measure`）。但这会把 LDCT 基线本身抬到 38-42 dB，与你其余方法不可比，只能单列。
- **若要对齐 Eulig 基准**：需要按检查类型分窗（腹 L / 胸 C / 头 N）并改用 TCIA 150 例扫描的 70/20/10 划分。你的 10 患者子集与该划分**不重叠**，无法直接对齐，仅可作旁证引用。
- **逐图 min-max（DenoMamba、CoreDiff 的做法）明确不要用**——data_range 随图像浮动，指标不可复算。

---

## 二、逐论文口径表

只列上游或我本轮实际核实过的条目。PSNR 一栏凡标注"反推"者，均为由论文报告的 RMSE 按 `10·log10(R²/RMSE²)` 换算，非论文直报。

| 论文 | HU 窗 | 归一化 | PSNR | 测试划分 | 证据来源 | 置信度 |
|---|---|---|---|---|---|---|
| **SSinyu/RED-CNN**（口径 A 源头，TMI 2017 的第三方实现） | 训练/存储 [-1024, 3072]；评测 clip [-160, 240] | `(HU+1024)/4096`；评测 denormalize→HU 后 clip，`data_range=400`，RMSE 单位 HU | 该 repo 未给汇总数字 | 9 训练 / 1 测试，`--test_patient L506` | 代码 `prep.py` L61-63、`main.py`、`solver.py` L80-88/171-178；上游逐行核验 | 高 |
| **CTformer**（PMB 2023, wdayang/CTformer） | 同上 | 同上（逐字分叉） | Table I 只报 SSIM/RMSE：CTformer RMSE 9.0233 → **≈32.9 dB**；RED-CNN RMSE 10.1044 → **≈31.9 dB**；WGAN-VGG 11.6370 → ≈30.7 dB；LDCT 14.2416 → ≈29.0 dB | L506 全部切片 | 代码 `prep.py` L61-63、`main.py` L53-56、`solver.py` L220-226；论文 Table I | 高 |
| **SSinyu/WGAN-VGG**（Yang TMI 2018 的参考实现） | 同上 | 同上 | 未给汇总数字 | `--test_patient L506` | 代码四处逐行核验（`prep.py`/`main.py` L50-53/`solver.py` L191-195/`metric.py`） | 高 |
| **RED-CNN 原论文**（Chen TMI 2017） | **未说明** | **未说明**；由数字自洽反推为 [0,1] 且 `data_range=1.0` | 44.4187±1.2118（LDCT 39.4314） | 10 患者**留一交叉验证**（非单一 L506） | PMC5727581 Table III/IV；全文检索 HU/window/normalization 零命中 | 中（协议为中，数字为论文直报） |
| **DU-GAN**（Huang, IEEE TIM 2022） | **[-300, 300]**（clamp） | `(clamp(HU,-300,300)+300)/600`；`max_val=1.0` | Mayo-10%（chest）22.3075；Mayo-25%（abdomen）34.6186 | **20 训练 / 20 测试患者**（胸 C0xx / 腹 L0xx 队列），**不是** 10 患者 L-split | 代码 `utils/dataset.py` L9-14、`models/basic_template.py` L53-54、`utils/metrics.py` L41-50、`train_dugan.sh` | 高 |
| **jcy132/DML_CT**（MICCAI 2022） | [-1000, 1000]（**仿射隐含，代码无 clip**） | `(HU+1000)/2000` | 未核实 | 未核实 | 代码 `data_manager/AAPM.py` L56-63 | 高（公式）/ 未核实（PSNR） |
| **icon-lab/DenoMamba**（JBHI 2024） | **无 HU 口径** | 8-bit PNG `/255` **且**逐图 min-max；PSNR 用 `max_pixel=1.0` | 未核实 | 未核实 | 代码 `data.py` L17-20/26-31、`test.py` L12-16/83-84 | 高 |
| **CT-Mamba**（CMIG 2025, linxuan-li） | 训练 shifted-HU [0, 4096]；`[-160,240]` 只用于 `save_fig` | `(HU+1024)/4096`；评测 `data_range = 4096`，**不 clip** | 论文表 LDCT 基线即 38.5 / 40.x / 42.3 | 未核实 | 代码 `Predict.py` L22-27/114-116/163-172、README | 高 |
| **LEDA**（2024）/ **FoundDiff**（2025） | [-1000, 2000] | `(clip(HU,-1000,2000)+1000)/3000` | 未报告 | 未核实 | 代码 `mayo16_dataset.py`、`transforms.py` L572-587 | 高 |
| **CoreDiff**（TMI 2024, qgao21） | 预处理 [-1024,3072]；评测**逐图**动态范围 | 落盘原始 uint16；dataloader 转 HU→clip→/4096；`data_range = full.max()-full.min()` | 论文报 PSNR 但 data_range 逐图浮动，跨论文不可比；RMSE 因残留 `img*2000/255-1000` 公式**单位不可解释** | 未核实 | 代码 `prep_mayo2016.py`、`utils/dataset.py` L96-100、`corediff.py` L139-140/289-290、`utils/measure.py` L19-25 | 高 |
| **FMDNet**（JACMP 2026, acm2.70656） | Mayo **[-160, 240]**；LoDoPaB [-1000,400] | 转 HU → clamp 到窗 → 归一到 [0,1] 再算 PSNR/SSIM；**RMSE 以 HU 报告** | L506+L333：LDCT **23.44**；RED-CNN 28.48；DuGAN 28.60；Uformer 28.74；CTformer 28.5637±1.5846 | L506 + L333 | 论文摘要级转述（PDF 直连 403），**未读正文** | 中 |
| **Eulig et al.**（Med Phys 2024, ldct-benchmark） | 按检查类型（HU+1024 域）：腹 **L=(1074,400)** → HU[-150,250]；胸 C=(424,1500)；头 N=(1064,80)。另有 VIF/RMSE 上界 2924 | 训练 `meanstd`: `(x-481.454)/502.185` 或 `minmax: x/4095`（x 为原始 uint16）；指标前先加窗到 [0,1]，`data_range=1.0` | 腹（25% 剂量）：**LDCT 28.67**；RED-CNN 33.223±0.071；DU-GAN 32.127±0.269；WGAN-VGG 30.509±0.854；TransCT 30.529±0.175 | TCIA 150 例扫描，按检查类型各 70/20/10，测试 15 例（每类 5 例） | 代码 `ldctbench/data/info.yml`、`evaluate/utils.py` L20/246-290、`utils/auxiliaries.py`；论文 §IV-A/§IV-D | 高 |
| **AAPM 2016 官方** | 无 | 无 | **无 PSNR/SSIM/RMSE** | 官方指标 = 医师 per-lesion + per-case NS 取平均，JAFROC 仅破平 | 官网 + McCollough Med Phys 44(10):e339-e352 | 高 |
| **MICCAI 2023 LDCT IQA 挑战赛** | 腹窗 C=300 / W=1400 | 先加窗再套 ImageNet Normalize | 无 PSNR（无参考 IQA） | — | 代码 `ldct_iqa.py` | 中 |
| **本项目（本地实测）** | **[-1000, 1000]** | `(clip(HU,-1000,1000)+1000)/2000` | identity **40.0864 / 0.9291** | L506 + L067（8 训练） | `data/prepare_aapm.py` L59-82、`utils/metrics.py`、`experiments/sweep_hu_window.py` | 高 |

---

## 三、是否存在事实标准

**答案：不存在。** 但存在一个**强度很高的收敛点**，就是 ~400 HU 软组织窗。

### 3.1 分歧在哪、量级多大

PSNR = `10·log10(R²/MSE_HU)`，纯线性缩放不改变 PSNR，**真正改变数值的是窗宽 R（一个非线性操作）**。抬升量恰为 `20·log10(R₁/R₂)`。我在**同一测试集（L506+L067，435 片）上对同一份数据实测**了 identity 基线（输出=输入），逐切片 PSNR/SSIM 平均：

| HU 窗 | R | identity PSNR | SSIM | 观测值被裁比例 | 相对 [-160,240] 理论抬升 |
|---|---|---|---|---|---|
| **[-160, 240]** | 400 | **27.8630** | 0.8432 | 59.16% | +0.000 |
| [-300, 300]（DU-GAN） | 600 | 31.1857 | 0.8638 | 57.50% | +3.522 |
| [-1000, 400] | 1400 | 37.1289 | 0.8878 | 17.52% | +10.881 |
| **[-1000, 1000]（我们当前）** | 2000 | **40.0864** | 0.9291 | 16.31% | +13.979 |
| [-1024, 1185] | 2209 | 40.6548 | 0.9380 | 0.09% | +14.843 |
| [-1024, 1500] | 2524 | 41.8029 | 0.9489 | 0.03% | +16.001 |
| [-1000, 2000]（LEDA/FoundDiff） | 3000 | 43.5824 | 0.9613 | 16.15% | +17.501 |
| [-1024, 3071]（CT-Mamba data_range） | 4095 | 46.0016 | 0.9762 | 0.00% | +20.204 |

实测抬升与理论值完全吻合（裁剪不重时成立），这直接量化了"换窗 = 换尺子"。

### 3.2 最直接的证据：同一算法在两个口径下的数字

- **FMDNet（[-160,240] 窗）**：RED-CNN **28.48**、CTformer **28.5637**、DuGAN **28.60**、LDCT 基线 23.44。
- **CT-Mamba（data_range=4096）**：同一数据集的 LDCT **基线本身**就有 38.5 / 40.x / 42.3 dB。

**同一批 AAPM-Mayo 数据，"低剂量输入"这个零信息基线相差近 20 dB——与网络无关，纯由窗与 data_range 决定。**

### 3.3 交叉验证：口径 A 上独立测得的 RED-CNN 数字

第三方 graph wavelet 论文在 L506 上实测：LDCT 27.24 / RED-CNN **32.93** / CPCE 33.04 / WGAN 30.80 / NLnet 33.06 / AGC-LSRED 33.17 / Wavelet 34.06 / Graph wavelet 35.47。与由 CTformer RMSE 反推的 31.9 dB 同一档，互相印证口径 A 上 RED-CNN ≈ 32-33 dB。

### 3.4 我实测的一个反例（重要）

Eulig 基准报告腹窗 LDCT 基线 **28.67 dB**，我在口径 A 窗上对 L506+L067 实测 **27.86 dB**、L506 单患者 **29.25 dB**——三者高度自洽，**说明我的测量与主流一致**。而 FMDNet 报告的 LDCT 基线 **23.44 dB** 比这低 4.7 dB。我专门按 FMDNet 的测试集（L506+L333）复算，得到 **28.18 dB**（L506 29.2489 / L333 27.2508，均为 455 片逐切片平均），**仍差 4.7 dB**。我进一步排除了"少量纯空气切片拉高均值"这一解释：L506 的 211 片 body 覆盖率**全部 >20%**，per-slice PSNR 分布紧致（min 23.61 / 中位 29.76 / max 33.05），无 >35 dB 的离群片。**该 4.7 dB 缺口我用现有材料无法解释**（见第五节）。

---

## 四、对我们的建议

### 4.1 核心判断

你当前的 `[-1000, 1000]` 口径**不属于任何已发表族群的公共口径**，它比口径 A 高约 13.98 dB。identity 基线 40.0864 / 0.9291 这个数字**不能直接与文献并列**——文献在口径 A 上的 "LDCT 输入" 基线是 27-29 dB。改口径**不是性能下降，是换尺子**：同一模型从 40.09 掉到 27.86 是尺子变了，不是变差了。

### 4.2 建议采用

**主口径 = 口径 A**（第一节表格）。辅以一条说明：在论文中同时给出该窗下的 RMSE（单位 HU），因为 RED-CNN/CTformer 谱系的主表用的就是 SSIM/RMSE。

### 4.3 需要改什么（按修改顺序，含绝对路径）

1. **重建 h5 数据（必须先做，无法在指标层绕过）**
   `D:/图像相关论文/LDCT/data/prepare_aapm.py` L78-79 在**落盘前**就做了 clip：
   ```python
   hu = np.clip(hu, hu_min, hu_max)
   x = (hu - hu_min) / (hu_max - hu_min)
   ```
   当前默认值在 L178-179 是 `-1000.0 / 1000.0`。改为：
   ```
   python data/prepare_aapm.py --hu-min -1024 --hu-max 3072
   ```
   这会把 `x = (HU+1024)/4096`，与 SSinyu 的 `normalize_(MIN_B=-1024.0, MAX_B=3072.0)` 逐字一致（注意 SSinyu 用的是 **3072 不是 3071**，分母恰为 4096；实测本数据 HU 上限 3071 → x_max = 4095/4096，与参考实现完全相同）。
   **这一步会改变存储的信号**，不只是量纲——所以之前算出的 40.0864 在新 h5 上不可复现，必须重跑全部基线。

2. **改指标层**
   `D:/图像相关论文/LDCT/utils/metrics.py`：
   - `DATA_RANGE = 1.0`（L31）是个陷阱——它是为 LoDoPaB（图像本就在 [0,1]）写的，同文件 docstring L14-15 明说"这与 MGF-Net 的 [0,255] 域不同"。AAPM 路径复用它会静默出错。
   - 需新增"Hodges 前先反归一化回 HU 再 clip 到窗"的步骤，或改为先把窗归一化到 [0,1] 再传 `data_range=1.0`（两者数学等价）。
   - 若走 HU 域路线：`psnr(..., data_range=400.0)`、`ssim(..., data_range=400.0)`。
   - L20 "不做裁剪"这条设计原则与口径 A 冲突——口径 A **要求**对预测和参考都做窗裁剪。需明确改为"按论文口径裁剪并声明"。

3. **改测试划分（可比性的关键，比窗更容易被忽略）**
   `experiments/sweep_hu_window.py` L104 与 `prepare_aapm.py` L180 当前默认 `--test-patients L506,L067`，即 8 训练 / 2 测试。口径 A 是 **9 训练 / 1 测试，测试患者固定 L506**。若要数字能直接对上 RED-CNN 32.93 / CTformer 32.9，应改为只留 L506 做测试（训练 9 例）。建议**同时报两组**：L506-only（对外比）与 L506+L067（自有更稳的估计），并在表注中标明。

4. **固定聚合方式（我们仓库内部已经不一致了）**
   同一数据、同一窗 `[-1000,1000]`，三种聚合给出三个数（我实测）：
   - 逐切片 PSNR 再平均 = **40.0864**（= 你报的数）
   - 逐患者汇总 MSE 再平均 = **39.803**（`experiments/hu_window_sweep.json` 里写的数）
   - 全体汇总 MSE 再算 PSNR = **39.6872**

   跨度 0.40 dB。必须在代码里定死一种（建议**逐切片 PSNR 后平均**，与 SSinyu 的 `compute_measure(x, y, pred, ...)` 逐图调用一致），并写进论文。注意 `sweep_hu_window.py` L129-135 的"逐患者加权"在数学上恒等于"逐切片平均"，两者不会分歧；分歧只来自"先汇总 MSE"这条路径。

5. **SSIM 参数**
   我们已用 `gaussian_weights=True, sigma=1.5, use_sample_covariance=False, win_size=11`，`data_range` 传窗宽。需注意：**我未核实 SSinyu/CTformer 的 `measure.py` 里 SSIM 具体传了哪些窗口参数**，只核实了 `C1, C2 = (0.01*data_range)**2, (0.03*data_range)**2` 这一条。若他们的 `gaussian_weights`/`win_size` 与 skimage 默认不同，SSIM 会有小量级差异。建议直接把他们的 `measure.py` 抓下来对一遍再定稿。

### 4.4 改完之后预期看到什么（用于自检）

改到口径 A 后，identity 基线应从 40.0864 变为：

| 测试集 | identity PSNR | identity SSIM |
|---|---|---|
| L506 only（211 片，对外可比） | **29.2489** | 0.8838 |
| L067 only（224 片） | 26.5576 | 0.8049 |
| L506+L067（435 片） | **27.8630** | 0.8432 |
| L506+L333（455 片，对齐 FMDNet） | 28.18 | — |

如果你的模型在口径 A 上打不过 identity 基线（29.25 / 0.8838 于 L506），那就是真问题——口径 A 的窗窄（只 400 HU），噪声相对信号被放大，**identity 基线本身就不高**，这与你此前在任务 #16「模型为何在保真类指标上输给平凡平均」里遇到的困境是同一件事的两个侧面。**换口径不会解决它，反而会暴露它**：在 R=2000 的宽窗里，背景空气像素占了 16% 且 obs≈gt，白送 PSNR；裁到 [-160,240] 后这些"白送分"全部饱和为常数，模型必须在真实的软组织纹理上竞争。这对你的方法是好事（判别力更强），但要有心理准备数字会难看。

---

## 五、诚实的不确定性

**已确证（可放心引用）**
- AAPM 官方无任何图像归一化/PSNR 规定（官网 + Med Phys 论文逐节检索，无 PSNR/SSIM/normalization 字样）。
- 口径 A 的代码细节：`(HU+1024)/4096` + 评测 clip `[-160,240]` + `data_range=400`，四个仓库（SSinyu/RED-CNN、SSinyu/WGAN-VGG、wdayang/CTformer、以及 EHSANet/MRED-Net/SDCNN）逐字一致，已逐行核验。
- 表 3.1 全部数字是我本轮在本机 DICOM 上实测（脚本：`D:/图像相关论文/_research_tmp/win_table.py`、`agg_check.py`、`dist.py`、`l333.py`），非推测。

**不牢 / 未解决**

1. **FMDNet 的 23.44 dB 缺口未解释（4.7 dB）。** 我按它的测试集 L506+L333 复算得 28.18 dB，并排除了空气切片拉高均值的解释。可能原因：PDF 直连 403，我只看到二次源转述，**其"LDCT"一行未必是我理解的 identity 基线**（也可能是经过某种后处理的输入），或聚合方式不同，或他们用了更新版的 TCIA 数据格式。**建议不要引用 FMDNet 的绝对数字**，只引用它"提出 HU-windowed protocol 这件事"本身。
2. **第三方 L506 数字也有 ~2 dB 缺口。** graph wavelet 论文报 L506 LDCT = 27.24，我实测 L506 identity = 29.2489。同一患者、同一窗，差 2.0 dB。该数字来自 Semantic Scholar reader 表（置信度中），我**未读到该论文原文的协议描述**，无法判断其切片子集与聚合方式。这个 2 dB 就是当前跨论文比较的**实际噪声下限**——任何小于 2 dB 的"改进"在跨论文口径下都不可信。
3. **RED-CNN 原论文的口径仍是反推。** `data_range=1.0` 是我从 `10log10(1/0.0060²)=44.44` 与其报告的 44.4187 自洽推出的，论文正文**确认为空白**（Methods 逐节检索 HU/window/normalization/clip 全部零命中；唯一强度参数是仿真光子数 10^5）。所以 44.42 dB **不是**任何可复现的代码口径，引用时必须注明"作者自报，口径未公开"。
4. **`prepare_aapm.py` 里 "实测 HU 范围约 [-1024, 1185]" 这条注释是错的。** 我本轮实测：L506 上界 **3071 HU**（>3000 HU 有 114 像素，>2000 HU 有 917 像素），L067 上界 2129，L333 上界 1719。`>1185 HU` 的像素占 L506 的 0.099%、L067 的 0.076%。这意味着：(a) 1185 这个数字不可作为窗的依据；(b) 若你曾用 [-1024,1185] 当"数据实际范围"，会裁掉真实的高密度像素（骨头/金属/床板）。该注释同时出现在 `prepare_aapm.py` L17 与 `experiments/sweep_hu_window.py` L54 的候选表里，**两处都该改**。
5. **CT-Mamba / LEDA / FoundDiff / DML_CT / DenoMamba 的 PSNR 数字未核实**——我只核了它们的归一化代码，未抓论文表格，也未复算。表中已标"未核实"。
6. **DU-GAN 的署名**：任务描述写"Zhou et al., IEEE TMI 2022"，与其公开仓库和实际出处不符。实为 **Huang, Zhang, Zhang, Shan, IEEE TIM 71:1-12, 2022（DOI 10.1109/TIM.2021.3128703）**。引用时请修正。
7. **SSinyu 谱系的 `measure.py` SSIM 窗口参数未逐行核实**（只核实了 `C1/C2` 公式），见 4.3 第 5 点。
8. **Eulig 的 `apply_center_width` 我只看到摘要级描述**，未逐行核对它是否等价于「clip 后线性映射」；若它对窗内值做了非线性重映射，其数字与口径 A 的可比性会打折（但两者窗几乎相同，风险有限）。
9. **本项目 h5 已按旧参数落盘**，`data/aapm_h5/*.h5` 与 `data/aapm_h5/index.json`（记录 `"hu_window": [-1000, 1000]`）都需按 4.3 第 1 步重建后重跑，旧结果一律作废。

**可复现的核验脚本**（本轮新写的，未清理）
- `D:/图像相关论文/_research_tmp/win_table.py` —— HU 分布 + 各窗 identity 基线换算表（逐切片口径）
- `D:/图像相关论文/_research_tmp/agg_check.py` —— 三种聚合方式的差异
- `D:/图像相关论文/_research_tmp/dist.py` —— per-slice PSNR 分布与 body 覆盖率
- `D:/图像相关论文/_research_tmp/l333.py` —— L506/L333 在口径 A 窗上的 identity 基线

运行方式（本机无 pydicom，系统 python 是 3.14）：`uv run --with pydicom --with numpy --with scikit-image python <脚本>`。