# 换方向决策报告（医学图像融合 → 更容易发表的方向）

**核实日期：2026-09-13。所有标注"✓实测"的条目本次会话用 curl / GitHub API / Zenodo API 现场复验过；标注"未核实"的明确说明。**

---

## 一、推荐排序

排序依据是一个复合判据：**（数据+划分+基线+评测）四件套的完备程度** × **4GB 可行性** × **该主题在 2024–2026 中档刊的实际出货证据**。前四名我给出了可直接开工的顺序；第五名开始是"能发但要么更挤、要么要学新概念"。

### 第 1 名：息肉分割 —— 轻量化 + 跨中心泛化的权衡曲线

| 项 | 内容 |
|---|---|
| 任务类型 | 2D 二分类语义分割，像素级真值 mask，Dice/IoU |
| 数据 | 训练 1450 张（Kvasir-SEG 900 + CVC-ClinicDB 550），测试 798 张覆盖 5 个中心。全部打包在 PraNet 官方 Google Drive：TrainDataset 399.5MB、TestDataset 327.2MB |
| 许可 | Kvasir-SEG 官方 Terms of use 原文（✓实测逐字）："restricted for research and educational purposes. The use of the Kvasir-SEG dataset for commercial purposes is forbidden without prior written permission" —— 学术发表可用，商用不可 |
| 基线 | 全部免复现：PraNet（官方预训练权重 + Res2Net 权重 + MATLAB 一键评测工具箱）、U-Net++（2682★）、UACANet(167★)、Polyp-PVT(266★) |
| 算力 | 352×352 统一输入，Res2Net50/PVT 主干 batch 8–16 + AMP 在 4GB 内可训；原文称 20 epoch 约 0.5 小时收敛 |
| 目标刊物 | BSPC（Elsevier，中科院医学 2 区，订阅路线 0 版面费）、Journal of Imaging、Diagnostics、Scientific Reports |
| 数据实测 | kvasir-seg.zip 官方直链 HTTP 200、Content-Length = 46,227,172 字节（✓实测） |

**为什么比融合容易**：融合的四个病灶它一个都没有 —— 五集协议已成事实标准（无划分争议）、Dice 有唯一真值（无"贴近单一源刷分"）、评测代码和基线权重全部现成（无需自建）、1450/798 的规模统计功效足够（不再靠 2 个病例下结论）。

**为什么能发**：不是靠"通用 SOTA"（Kvasir 已卷到 0.93+，赢不了），而是靠**参数量/FLOPs vs 未见过数据集 Dice 的权衡曲线**——这个子表目前没有公认赢家。BSPC 2025 年实际刊出了 TPCP-Net（<1M 参数，Vol.108 p.107970）和 EfficientPolySeg（3.58M 参数），说明"轻量化"这个叙事在该刊已被验证可接收。

---

### 第 2 名：半监督分割（SSL4MIS 路线）—— 基建复用率最高

| 项 | 内容 |
|---|---|
| 任务类型 | 分割（少量标注 + 大量无标注），Dice / HD95 |
| 数据 | ACDC 心脏 MRI（100 例，2D 约 1900 切片）、LA 左心房 3D、Pancreas-CT、BraTS2019、BUSI 780 张 |
| 许可 | SSL4MIS 仓库 MIT（✓实测：license=MIT，2687★，pushed 2025-06-07） |
| 基线 | **单一仓库里躺着约 15 个方法的完整实现 + 官方划分 + 统一评测**：train_mean_teacher / uncertainty_aware_mean_teacher / cross_pseudo_supervision / cross_consistency_training / uncertainty_rectified_pyramid_consistency / deep_co_training / interpolation_consistency / entropy_minimization / regularized_dropout / adversarial_network / cross_teaching_cnn_transformer / fixmatch 等，外加 val_2D.py / val_3D.py |
| 算力 | 2D U-Net 256×256 batch 8–16 显存 <2GB；3D V-Net（112×112×80, batch 4）也在 4GB 内 |
| 目标刊物 | BSPC、Artificial Intelligence in Medicine、Expert Systems with Applications、Neurocomputing、CMIG、IEEE JBHI；会议 ISBI |

**为什么比融合容易**：数据划分写在 `code/dataloaders/` 里，评测脚本是统一的，**不需要复现任何基线、不需要发明划分**——这正好是研究者抱怨的两个死结。而且 2D U-Net + 损失函数 + 训练循环是研究者已经写熟的东西，迁移成本接近零。

**唯一风险**：赛道拥挤。2026 年 ACDC 10% 标注下 Dice 集中在 88–91%（MC-Net 88.44 / SS-Net 88.63 / DC-Net 88.93 / BCP 88.57 / AD-MT 88.96），中档刊的当前门槛是"新一致性模块 + 0.3~1% Dice"。可达，但不宽裕。

---

### 第 3 名：ISIC 2018 皮肤病变分割 + 单源域泛化 / 鲁棒性

| 项 | 内容 |
|---|---|
| 任务类型 | 2D 二分类分割 + 跨域泛化评测 |
| 数据 | ISIC 2018 Task 1，**官方划分且官方测试真值已公开**。训练 2594 张 mask（实测下载后 zipfile 计数）、测试 1000 张 mask |
| 许可 | 数据集页 License 列以 **CC-0** 为主（✓实测页面原文含大量 "CC-0" 行）；**但同一页有一行 1512 张图像标 CC-BY-NC** —— 见第五节的不确定性说明 |
| 基线 | nnU-Net（✓实测 Apache-2.0，8880★，pushed 2026-09-10，仍活跃）、MedSAM（Apache-2.0，4394★）、U-Net++、TransUNet 系 |
| 算力 | 全场最轻。256×256 或 384×384，ResNet34 主干 batch 16 无压力；冻结主干 + batch 4 可跑 ConvNeXt/DINOv2 |
| 目标刊物 | Diagnostics、BSPC、Journal of Imaging、Scientific Reports、CMIG |
| 数据实测 | `ISIC2018_Task1_Test_GroundTruth.zip` HTTP 200、Content-Length = 9,680,101 字节（✓实测） |

**为什么比融合容易**：这是候选里**唯一同时具备"许可最干净 + 官方划分 + 测试集真值公开"的数据集**。测试集有公开真值意味着任何审稿人都能直接复算你的数字，这在医学分割数据集里是少数派。

**必须避开的坑**：不要写"又一个双编码器注意力模块"。三个有意义且未饱和的切口：(a) 单源域泛化（ISIC2018 训练 → PH2 / PAD-UFES 测试）；(b) 采集设备与低质量退化下的鲁棒性评测与修复；(c) 不确定性校准与失效检测。

---

### 第 4 名：低剂量 CT 去噪 —— 小波频域技能的最自然迁移

| 项 | 内容 |
|---|---|
| 任务类型 | 图像复原，真值 = 同患者全剂量 FBP 重建，PSNR/SSIM/RMSE |
| 数据 | **LoDoPaB-CT**（Zenodo 3384092，✓实测：title "LoDoPaB-CT Dataset"，license **cc-by-4.0**，ground_truth_train.zip = 15,940,074,239 字节、observation_train.zip = 29,938,738,627 字节、ground_truth_test.zip = 1,582,139,537 字节）。约 40000 张切片 / 约 800 名患者。可取 128×128 版本，本地只需几十 GB |
| 备选数据 | AAPM-Mayo LDCT 的 2016 挑战赛 30 例子集有公开 Box 直链（无需伦理协议），是绝大多数去噪论文实际用的那个子集 |
| 基线 | RED-CNN、WGAN-VGG、DU-GAN、CTformer，以及 **dival 官方库**（PyPI v0.6.2, MIT），内置 `get_standard_dataset('lodopab')` 自动下载/校验/评测 + 在线榜单 |
| 算力 | 全场最低档。362×362 降到 256/128，小 U-Net batch 4–8，远低于显存上限 |
| 目标刊物 | BSPC（2025 Vol.108 107870 SSEN；2025 Vol.109 107888 RHA-Net；2026 Vol.121 TGMamba）、Medical Physics、Physics in Medicine & Biology、Biomedical Optics Express（APC 1,913，门槛明显更低） |

**为什么比融合容易**：真值是**数值张量**，PSNR/SSIM 任何人都能复算，彻底不存在"贴近单一源刷高"的空间。数据零门槛、许可清晰、无需伦理审批、基线全开源。而且研究者现成的 **PR-LWT 完美重构可学习小波 + 74K 参数轻量网络** 技能栈可以**直接迁移**——"轻量化 + 小波频域 + 可逆性保证"正好是这类期刊偏爱的叙事。

**唯一风险**：高度饱和。必须带明确卖点（极低参数量 / 可学习小波的完美重构性 / 跨剂量泛化），纯堆网络必被拒。

---

### 第 5 名：BUS-BRA 乳腺超声 —— 划分最干净、竞争最少

| 项 | 内容 |
|---|---|
| 任务类型 | 2D 病灶分割 + 良恶性/BI-RADS 分类（双真值） |
| 数据 | 1875 张乳腺超声、1064 位患者、722 良性 / 342 恶性（活检证实），**自带官方 5-fold 与 10-fold 划分** |
| 许可 | CC-BY-4.0（✓实测 Zenodo 8231412：BUSBRA.zip = 133,917,740 字节） |
| 基线 | 官方复现代码 github.com/wgomezf/BUS-BRA（✓实测 GPL-3.0，28★，pushed 2024-07-30），MATLAB 2022b 的 DeepLabV3+ + ResNet18/50，内置 5 折交叉验证的 RUNME.m |
| 算力 | 单通道 256–512，batch 8–16，1875 张一次完整 5 折也只要数小时 |
| 目标刊物 | Medical Physics（数据集本体即发表于此，投同刊有天然连续性）、BSPC、Diagnostics、CMIG |

**为什么比融合容易**：**"自带官方划分"这一条直接消灭了研究者最大的痛点**（"没有标准划分、没有公开划分清单"）。真值双重：分割有 mask、分类有病理标签。数据量适中，文献量明显小于息肉和皮肤——同样一张表更容易讲出新意（分类-分割多任务、BI-RADS 有序分类的校准、低质量超声鲁棒性）。

注：备选 BUSI 780 张存在重复图像问题（2025 年已有论文指出并修正），引用前需自查。

---

### 快速热身通道（副线，两周内拿第一张正反馈）

**MedMNIST+ 冻结特征 + 线性探针**：Zenodo 10519652（✓实测 license cc-by-4.0，标题确认 "[MedMNIST+] 18x Standardized Datasets for 2D and 3D Biomedical Image Classification with Multiple Size Options"），官方划分 + 官方评测包 `python -m medmnist evaluate` 直接算 AUC/ACC，DINOv2 / BiomedCLIP 权重免费。算力门槛全场最低，当天就能出第一张表。

**但这个方向不能单独成篇**：纯 MedMNIST 论文会被审稿人当 toy benchmark，**必须至少配一个真实临床集（ISIC2018 / HAM10000）做外部验证**，否则会被质疑临床意义。

---

## 二、与现有工作的衔接

我读了 `D:\图像相关论文\MGF-Net` 的实际代码结构。研究者的资产比自述的更多，需要分三类处理。

### A. 可直接复用（迁移成本按天计）

**1. 训练基建整体可用。** `config.py` 已经把 4GB 约束写死成正确配置（`batch_size=4`、`patch_size=128`、`use_amp=True`），`experiments/run_cv.py` 实现了**按病例的 K 折交叉验证 + 病例内先平均再跨病例统计**（脚本注释里明确写了"不要把切片当独立样本——同一折内 20 张相邻切片高度相关"）。这个统计纪律**在换方向后仍然是稀缺且加分的能力**——绝大多数分割论文作者并不做病例层聚合。`evaluate.py` 的"显式 ID 配对、缺失即报错、不静默截断"也是同一套严谨性。

**2. 受控消融的方法论。** `models/mgf_net.py` 的两个可切换维度（`wavelet: legacy|lifting`、`gate_type: residual_capped|neutral_sigmoid`）配合 `experiments/inspect_gate.py`、`analyze_gamma.py`，以及 `experiments/evaluate_trivial_baselines.py`（跑平凡基线）——这套"把每个设计选择做成二分对照 + 对比平凡基线"的习惯，是任何分割/去噪/合成论文里都能直接用的**审稿人好感来源**。

**3. PR-LWT（完美重构提升小波）是真正的技术资产。** `models/lifting_dwt.py` 的正逆变换共用同一组 P/U 参数、结构上保证可逆、taps 有界化保证 float32 数值稳定；`tests/test_lifting_pr.py` 有可逆性测试。**这条线可以 1:1 搬到低剂量 CT 去噪**（小波域去噪 + 可逆性保证 + 极低参数量，是一个完整的故事），也可以搬到 MRI→CT 合成的频域分支。

**4. 组件级复用。** `models/gated_fusion.py`（逐像素学习两输入的融合权重）和 `models/edge_refine.py`（边缘精炼）是通用的多分支融合组件——在双编码器分割、MRI→CT 合成、多模态去噪里都能直接插进去当一个模块。

**5. 融合文献调研 + 指标模块。** `utils/metrics.py` 实现了 EN/SD/SF/MI/CC/PSNR/SSIM/VIF/SCD/Qabf，并有详尽的量纲约定文档（[0,255] 域、MI 用求和、SCD 原论文 Eq.10 分母漏印根号）。**这套东西在分割/去噪任务里没有用**，但在第四节的"坚持做融合"路线里是继续用武之地。

### B. 只能当配菜，不能当主菜

- **Harvard/AANLIB 数据本身**（`data/harvard_full/`，`data/train` 8 例 + `data/test` 2 例）。它是 10 个病例的数据，任何以它为主数据集的新方向都会重蹈统计功效不足。可作为小规模演示或外部验证集。
- **74K 参数的 MGF-Net 本身**。它是一个双输入 → 单输出的图像融合网络，输出没有真值。改造成分割/去噪网络的成本高于重写一个标准 U-Net，不值得为复用而复用。

### C. 必须从零开始

- **分割专用部件**：Dice/HD95/ASD 损失、mask 解码头、nnU-Net/MONAI 数据管线、各数据集的 dataloader。
- **域泛化协议**：leave-one-domain-out 划分、域间方差报告方式。
- **不确定性指标**：ECE、Brier、校准曲线、与专家标注方差的相关性。
- **配准概念**：形变场、微分同胚、spatial transformer、TRE、折叠雅可比——这一套是新知识，约 2–4 周上手（如果走配准路线）。

**一句话**：`experiments/` 和 `config.py` 这一层是通用资产，全部可搬；`utils/metrics.py` 和 `models/` 是针对融合任务专用的，只有 PR-LWT 值得带走。

---

## 三、不建议的方向

### 1. BraTS 3D 多模态 / 3D 大模型预训练 —— 4GB 直接爆
3D nnU-Net 训练需要 128³ patch，4GB 上能跑但极慢；全分辨率 3D 基础模型（如 3D 医学大模型预训练）完全不可能。属于"热门但门槛高"的典型。

### 2. SAM / MedSAM + LoRA / PEFT 微调 —— 显存硬约束 + 赛道已满
**必须纠正一个流传很广的错误认知**：网上"SAM + LoRA 只需 4GB"的说法，追查后源自 SAM-Adapter 项目的配置（inp_size 768、batch 2、开 gradient checkpointing），**并非 SAM ViT-B + LoRA 的独立实测**。SPIE Medical Imaging 2026 的一篇 PEFT-SAM 论文实测 **QLoRA 峰值显存 4.9GB**，仍在 4GB 之上；另有资料明确质疑 LoRA 不适合 SAM（ViT 注意力权重本身高度低秩，LoRA 会扭曲梯度）。

同时，SAM ViT-B 官方权重实测 375,042,383 字节（约 375MB / 91M 参数），1024×1024 原生分辨率下训练必然超 4GB。MedSAM（4394★）赛道 2024–2026 已被灌满，"又一个 LoRA 微调"的审稿容忍度在快速下降。

若一定要碰 SAM，只走 **training-free 推理路线**（如 arXiv 2503.04826 的 SAM2 训练无关框架，CHAOS-MRI 上 83.30% Dice），风险大幅降低。

### 3. 通用 2D 分割 SOTA 军备竞赛（Synapse / ACDC 榜单）
nnU-Net（8880★，2026-09 仍在推）已经把赛道封死。纯"又一个 U-Net 变体"很难过审。若走这条，必须选一条差异化叙事：显存/参数受限下的分割、跨中心域泛化、或被忽视的模态/解剖部位——**不能只是换注意力模块**。

### 4. 报告生成 / 医学 VQA —— 与融合同一种病，只是搬到了文本域
这是全清单里最危险的陷阱。BLEU/ROUGE 看起来是数值，但多项 2024–2025 综述明确指出其与临床正确性仅弱相关；IEEE 2025 综述实测 MIMIC-CXR 上 BLEU-1 在所有架构间拥挤在 **22–33** 区间、已饱和不再有区分度——**这与"零参数像素平均击败训练模型"是完全相同的失效模式**。加上约 22% 的 AI 生成报告含事实性错误，逼得审稿人转向 RadGraph F1 + 人工评估，单人独立完成人工评估不现实。

### 5. 3D 配准（原生分辨率）
VoxelMorph 维护者在 issue #235 中自述"我们用的模型和体数据需要约 10GB"；PatchMorph 论文报告同类 3D 脑配准显存区间 5.7–10.1GB，且 VoxelMorph 训练耗时 25–37 小时。有用户在 160×192×224、batch=1 时于 GTX 1050Ti/1060 上直接 OOM。**若走配准，必须先降到 128³/96³ 或分块训练，并把"显存受限"本身当作论文卖点。**（2D 配准如 FIRE 无此问题。）

### 6. 联邦学习 / 边缘部署 / 不确定性 —— 作为首篇偏重
联邦学习需要多机构数据（模拟联邦的说服力弱）；不确定性估计的指标设计（aleatoric/epistemic 分解、与专家方差相关性）比单纯 Dice 更容易被挑刺；边缘部署的"部署"本身在中档刊常被视为工程而非研究。这三条都可做，但不适合当"先发一篇出去"的第一篇。

### 7. 数据集论文（用现有 184 对）
**仅把现有 184 对图重新打包会被拒。** 数据集论文是唯一一条"评价标准不是方法新颖度"的路径（Scientific Data 只看数据质量），但前提是有**真实的数据贡献**——需要新增标注（跨模态配准标志点、多读者分割）才有分量。

---

## 四、如果坚持做融合

融合的病根是"无唯一真值 + 无标准划分 + 数据集只有 10 例"。这三条都有针对性的解法，按推荐度排序：

### 路线 A（最推荐）：换模态与场景 —— 转成 MRI→CT 合成（synthetic CT）

这是"融合"技术栈最自然的逃生门。**代码几乎不用改**：研究者已经在写图像到图像的编解码网络，pix2pix/ResNet 生成器与 MGF-Net 是同一套东西。

- **数据**：SynthRAD2023（Zenodo 7260705，✓实测 license **cc-by-nc-4.0**，title 确认 "SynthRAD2023 Grand Challenge dataset: synthetizing computed tomography for radiotherapy"）。1080 对 CT/MRI/CBCT，来自荷兰 3 家大学医学中心，脑 540 + 盆腔 540。
- **真值最硬**：不是模拟出来的干净图，而是**同患者实测的真实 CT**（MRI/CBCT 已刚性配准到 CT）。MAE/MSE/SSIM/PSNR 无人能质疑。
- **官方评测闭环**：SynthRAD2023 组织下有 preprocessing(75★)/metrics(11★)/algorithm-template(9★) 三个 GPL-3.0 仓库。
- **减分项**：3D 显存（建议 2D 逐切片或 128³ patch + AMP）、配准质量依赖、CC-BY-NC 非商业（学术发表无碍）。
- **可写的切入口**：挑战赛官方指出"图像相似度与剂量精度不相关"——用剂量 gamma pass rate 作为更客观的下游指标，本身就是一个完整的故事。

### 路线 B：换数据集 —— 从 10 例扩到 120 例真配对 CT-MRI

若坚持"CT-MRI 融合"这个词，第一步必须换掉 Harvard。

- **✓实测可用**：Zenodo 17486320 "A Paired Head CT-MRI Dataset for Cross-Modality Image Synthesis"，**CC-BY-4.0**，120 例术前神经外科患者（垂体瘤 / 脑膜瘤 / 听神经瘤 / 脑血管病），NIfTI 格式，四个 zip 分别为 26.7GB / 4.5GB / 4.5GB / 4.4GB + participants.xlsx。
- **未核实**：检索提到一个 **PMPBench**（声称 2642 对跨癌种配对 CT-MRI，CC BY-NC-ND 4.0），但给出的仓库名 `github.com/YifanChen02/PMPBench` 我实测 **301 重定向到 `YifanChen02/Contrast-X`** —— 仓库名未经核实，**不要在论文里引用这个 URL**。
- **TCIA 路线**：规模最大（>50,000 例），但集合多为同模态，跨模态 CT-MRI 需要**自己做配准**，工作量显著上升。只有在你能接受一整个配准管线时才选。

### 路线 C：换评价方式 —— 用下游任务指标替代 MI/VIF

这是 2025 年的领域趋势，不是我的臆造。检索到的明确证据：

- FreeFusion（IEEE 2025-05）原文："image fusion has no ground truth"，并指出手工融合损失可能损害高层任务，改用交叉重建学习 + 语义交互，报告分割精度提升。
- TeSG（arXiv 2506.16730）报告 mIoU 作为主管指标，消融中明确发现"VIF 可以小幅上升而整体性能下降"——直接证明传统指标不足。
- TBC（arXiv 2512.15211，2025-12-09）批评 EN/AG 类无参考指标陷入"Noise Trap"，把高频传感器噪声误判为有效细节。

**具体做法**：把融合当作下游分割的预处理模块，用下游 Dice / mIoU 作为**主指标**，MI/VIF 退为辅助。这样就给了融合一个客观落点。可行搭配：BraTS 多模态 MRI（T1/T2/FLAIR，数百例）融合 → 肿瘤分割 Dice；或 CT-MRI 融合 → 器官分割 Dice。

### 路线 D：做成诊断性 / 基准论文 —— 把"平凡基线击败模型"写成发现

研究者的 `experiments/evaluate_trivial_baselines.py` **已经跑了像素平均基线并发现它赢**（MI 差 30%）。这个结果本身有价值，只是被当成了失败而不是发现。

可以写成一篇**基准/警示性研究**：在公开有真值的多模态数据上，系统评测 N 个公开融合方法（含零参数基线）对**下游任务**的影响，给出"源参考指标排名与下游任务排名不相关"的定量证据。这类论文评价客观（下游有真值）、基线全公开、工作量可控（主要是推理 + 评测），且 Scientific Reports / BSPC / CMIG 都在收。**前提是必须换数据集**——10 例上的基准没有说服力。

### 路线 E：换任务定义但保留 10 例 —— 不推荐
任何在 184 对图上做统计推断的路线都解决不了统计功效问题。这条路我明确不建议。

---

## 五、诚实的不确定性

### 我本次实测复验过的（2026-09-13）

| 断言 | 实测结果 |
|---|---|
| BUS-BRA Zenodo 8231412 | title 与 license=cc-by-4.0 确认；BUSBRA.zip = 133,917,740 字节 |
| LoDoPaB Zenodo 3384092 | license=cc-by-4.0；ground_truth_train = 15,940,074,239；observation_train = 29,938,738,627；ground_truth_test = 1,582,139,537 |
| SSL4MIS 仓库 | MIT、2687★、pushed 2025-06-07 |
| nnU-Net 仓库 | Apache-2.0、8880★、pushed 2026-09-10 |
| VoxelMorph 仓库 | Apache-2.0、2750★、pushed 2026-09-11 |
| BUS-BRA 代码仓库 | GPL-3.0、28★、pushed 2024-07-30 |
| kvasir-seg.zip | HTTP 200、Content-Length = 46,227,172 |
| Kvasir-SEG Terms of use | 逐字确认：研究/教育用途，商用需书面许可 |
| ISIC2018_Task1_Test_GroundTruth.zip | HTTP 200、Content-Length = 9,680,101 |
| ISIC 数据页许可 | 页面确认存在多个 CC-0 行，以及一行 1512 张标 CC-BY-NC |
| MedMNIST+ Zenodo 10519652 | license=cc-by-4.0，标题含 28/64/128/224 四种分辨率 |
| SynthRAD2023 Zenodo 7260705 | license=cc-by-nc-4.0 |
| Paired Head CT-MRI Zenodo 17486320 | CC-BY-4.0；四个 zip + participants.xlsx，大小如上 |
| ISBI 2027 论文要求与截稿 | 官方页确认 4 页正文 + $200 第 5 页（**仅限非技术内容**，技术内容必须在前 4 页内）；4 页正文截稿 Monday 26 October 2026，通知 2027-01-12，会议 2027-05-25~28 洛桑 |
| QUBIQ 官方页 / 参与页 | 均 HTTP 200 |
| FIRE 项目页 | HTTP 200 |

### 我没核实到 / 证据不完整的

1. **ISIC 2018 许可的精确归属**。页面渲染文本里 CC-0 行和一行 CC-BY-NC（1512 张）并存，我**无法从渲染文本可靠地把每一行映射到具体 Task**。原始调研称"Task 1/2/3 为 CC-0，仅 Task 3 的 1512 张测试集为 CC-BY-NC"，但我这次**没能独立确认这个映射**。若选 ISIC，请自行到页面上按 Task 逐行核对。

2. **PraNet 仓库的 star 数与 license**。本次 GitHub API 触发 403 rate limit，**未能复验**。原始调研报 553★、pushed 2025-12-11。尤其注意：**PraNet 仓库可能没有 LICENSE 文件**——如果属实，复用其代码在许可上是个隐患，需自行确认。

3. **所有 APC 数字均为 OpenAlex 估计值，不是官方页面原文**。ScienceDirect 与 MDPI 直连均返回 403。具体：BSPC 2590、BMC Medical Imaging 3090、Scientific Reports 2690、Bioengineering 3258、Diagnostics 3137、CBM 3080、Scientific Data 2490、IEEE Access 2075、Medical Physics 3360、PMB 3325、Biomedical Optics Express 1913。**投稿前必须以期刊官网为准。**

4. **BSPC 的影响因子与分区**。检索到 2025 IF 报告为 4.9–5.7（多源不一致），中科院医学 2 区。以 JCR 为准。

5. **录用率数字**。CBM 录用率 12% 来自其 ScienceDirect Journal Insights（页面 403，未能复核原文）；IEEE Access 2026 录用率约 19.5% 来自搜索摘要。ISBI 历年录用率**未找到官方数字**。

6. **ACDC / BTCV 的确切图像数与许可未核实**（Synapse 需登录）。选该路线前请登录 synapse.org 确认，不要引用二手数字。

7. **CHASE_DB1 官方页** `blogs.kingston.ac.uk/retinal/chasedb1/` 返回 HTTP 000（疑似失效），需要另找镜像。HRF 页（www5.cs.fau.de）与 STARE 页（cecas.clemson.edu/~ahoover/stare/）可访问。

8. **Learn2Reg 的仓库名**。`learn2reg/Learn2Reg`、`MIC-DKFZ/Learn2Reg` 经 GitHub API 查询**均返回 404**，不要在论文里引用。入口是 learn2reg.grand-challenge.org。

9. **PMPBench 的仓库名未核实**（301 重定向到 `YifanChen02/Contrast-X`），不要引用检索给出的那个 URL。

10. **ICASSP 2027 截稿 2026-09-16** 未由我本次复验（仅来自原始调研的官方页抓取）。距今仅 3 天，若要用这个窗口，请立即自行确认。

11. **VoxelMorph 的 10GB 显存数字**来自 GitHub issue 中的维护者口述，不是受控测量；PatchMorph 的 5.7–10.1GB 区间来自论文报告。可作为量级参考，不宜当精确值引用。

12. **"零参数平均击败训练模型（MI 差 30%）"这一条我未独立复现**——是从研究者的 `experiments/evaluate_trivial_baselines.py` 与任务描述中得知的，未读取其输出结果文件核对。

---

## 附：我的推荐路径（如果只能给一句话）

**首选第 1 名（息肉分割）作为主线，第 4 名（LoDoPaB 低剂量 CT 去噪）作为保留小波技术资产并行的备选；同时用两周时间跑通 MedMNIST 线性探针拿到第一个正反馈。** 如果三个月内出不了结果，退守到第 5 名（BUS-BRA），因为它的官方划分和官方基线代码是全场最省心的组合。

**不建议为了保住融合方向而留在 Harvard 10 例上。** 若一定要留，走路线 A（SynthRAD2023 MRI→CT 合成）——代码改动最小，真值最硬，且它仍然是"跨模态医学图像"这个研究者熟悉的故事。

**关键文件路径（现有资产）**：`D:\图像相关论文\MGF-Net\experiments\run_cv.py`（病例级 K 折，换方向后仍可直接复用）、`D:\图像相关论文\MGF-Net\config.py`（4GB 正确配置）、`D:\图像相关论文\MGF-Net\models\lifting_dwt.py`（可迁移的技术资产）、`D:\图像相关论文\MGF-Net\utils\metrics.py`（融合专用，仅在坚持融合路线时保留）。