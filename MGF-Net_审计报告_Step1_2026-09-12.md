# MGF-Net Step-1 审计报告

> **日期**：2026-09-12
> **脚本**：`MGF-Net/diagnose_step1.py`（可复跑）
> **动机**：按《发表路线与实验计划_2026-09-12_修订版》"先测单元重构，再归因"的要求，在改动任何代码前量化现有实现的问题
> **环境**：`D:/DeveloperTools/miniconda/envs/mgfnet/python.exe`（torch 2.5.1+cu121）
> **检查点**：`MGF-Net/checkpoints/best_model.pth`（epoch=60, loss=1.7254）

---

## 结论速览

| 问题 | 结果 | 影响 |
|---|---|---|
| **Q1** 推理时滤波器是否被正确载入 | ✅ **载入正确**（误差 0.000e+00） | 修订版这条 P0 为**虚警**，现有推理路径有效 |
| **Q2** DWT/IDWT 单元闭环误差 | 🔴 **rel_L2 = 60.9%** | PR 性质被"可学习"彻底破坏，六成信号丢失 |
| **Q3** 门控 CT 偏置 | 🔴 **饱和在人工上限**（mean w = +0.3995，上限 0.4） | 网络请求更多 MRI 但被架构摁住，解释 SSIM_MRI 长期偏低 |
| **Q4** 全图统计 SSIM vs 局部窗口 | 🟠 **真实差距被掩盖 9 倍**（0.03 → 0.30） | 现用指标严重美化结果 |

**三者存在因果链**：

```
可学习滤波器漂移 → 闭环丢 61% 信号 → 重构出的融合图结构错误
                                        ↓
门控顶死 0.4 上限 → MRI 想进进不来 → MRI 信息进一步丢失
                                        ↓
全图统计 SSIM → 把上面两个问题一并掩盖
```

---

## Q1：可学习滤波器在推理时是否被正确载入

**修订版原判断**（列为 P0）：训练用 `learnable_dwt=True`、推理用 `False`，参数载入 buffer 不会自动恢复，怀疑滤波器状态错误。

**实测结果**：

```
learnable 模型 key 数 : 208
fixed     模型 key 数 : 208
learnable 有而 fixed 没有: 无
fixed 有而 learnable 没有: 无

test.py 路径 load_state_dict(strict=True) -> 成功
|载入值 - checkpoint 值| 最大 = 0.000e+00
|载入值 - Haar 初始值|  最大 = 1.064e-01
```

**结论：Q1 不是问题。** PyTorch 的 `state_dict` 对 `nn.Parameter` 与注册 buffer 使用同一套 key，`load_state_dict` 按名匹配并复制数值，与二者类型无关。推理时使用的确实是训练得到的滤波器。

**副产品（有价值）**：checkpoint 中的滤波器偏离 Haar 已达 **0.1064**（Haar 的 LL 系数为 0.5，训练后为 ~0.60），证明"可学习"确实生效了——这正是 Q2 问题的来源。

---

## Q2：DWT/IDWT 单元闭环误差（PR 性质）

| 输入 | max\|err\| | rel_L2 |
|---|---|---|
| 随机图 | 0.6697 | **60.9%** |
| 常量图 0.5 | 0.3695 | 67.8% |
| 脉冲图 | 0.1893 | 31.3% |
| 奇数尺寸 255×255 | 0.6714 | 60.9% |
| **Haar（未训练）对照** | **4.17e-07** | — |

**判读**：

1. **代码路径本身正确**——未训练 Haar 的闭环误差 4.17e-07，符合 FP32 预期（1e-5 ~ 1e-6 量级）。
2. **问题出在"可学习"**——训练后闭环相对 L2 误差达 61%，即分解再重构走一圈，六成信号丢失。
3. **常量图误差 0.3695 说明 DC 分量不被保持**——即修订版所指的 DC leakage。滤波器系数和已偏离 1。

**这证实了原计划中"滤波器漂移"的假设**（修订版要求"先测单元重构再归因"，现已实测）。

> ⚠️ 注意：`models/dwt_layer.py` 中 `MultiLevelDWT` 与 `MultiLevelIDWT` 持有**两组独立**的可学习参数，逆变换并非正变换的伴随/逆，PR 性质无任何结构保证。

---

## Q3：门控融合的 CT 偏置

**融合式**（`models/gated_fusion.py:52-59`）：

```python
weight = self.weight_net(concat) * 0.4      # 末层 tanh，故 weight ∈ [-0.4, 0.4]
fused  = ct + weight * (mri - ct)           # 等价于 (1-w)*CT + w*MRI
```

**系数范围**：CT ∈ [0.6, 1.4]，MRI ∈ [-0.4, 0.4]。

**实测 w 分布**（7 个子带全部像素）：

| 分位 | w |
|---|---|
| 0.00 | −0.4000 |
| 0.01 | +0.3953 |
| 0.25 | +0.4000 |
| 0.50 | +0.4000 |
| 0.75 | +0.4000 |
| 0.99 | +0.4000 |
| 1.00 | +0.4000 |

```
min = -0.4000   max = +0.4000   mean = +0.3995
w > 0.49（等权平均）占比:  0.00%
w < 0（反向外推）   占比:  0.02%
```

**判读**：修订版指出权重范围受限，但**实际比其描述更严重**——门控不是"被限制"，而是**饱和在人工上限**。25 分位即达 0.4000，均值 0.3995，整个门控网络在一致地请求"更多 MRI"，而 `*0.4` 这个手写常数把它摁住了。

**这直接解释了 `SSIM_MRI` 长期停留在 0.46**：不是网络不想注入 MRI，是架构不允许。等权平均（w=0.5）在当前参数化下**数学上不可达**。

> 上述仅反映融合块本身；后续 `refine` 与 EAR 有部分补偿能力，故不能声称整个网络完全无法输出 MRI。

---

## Q4：全图统计 SSIM vs 局部窗口 SSIM

`test.py:26-40` 的 `compute_ssim` 使用 `np.mean` / `np.var` 全图统计，等价于**窗口 = 整张图的退化 SSIM**。

| 图 | 方法 | 全图口径 | 局部口径 | 差值 |
|---|---|---|---|---|
| 10.png | Average | 0.7629 | 0.7333 | +0.0296 |
| 10.png | MGF-Net | 0.7244 | 0.5125 | +0.2119 |
| 6.png | Average | 0.6764 | 0.7331 | −0.0567 |
| 6.png | MGF-Net | 0.6463 | 0.3512 | +0.2951 |

**均值**：

| 口径 | Average | MGF-Net | 差距 |
|---|---|---|---|
| 全图统计（现用） | 0.7197 | 0.6853 | **+0.0343** |
| 局部窗口（标准） | 0.7332 | **0.4318** | **+0.3014** |

**判读**：修订版指出"Average 对同时接近两源的指标天然有利，不能用单一源参考指标判生死"——该论点成立。但换用标准局部窗口指标后，**真实差距从 0.03 拉大到 0.30**，即现用指标把问题掩盖了约 9 倍。

原计划"输给 Average"的警报方向正确，但依据的是被美化的口径，低估了严重程度。

> ⚠️ 本项为**探索性测量**，样本仅 2 对图像，不足以支撑任何发表结论。仅用于确认评价实现需要更换。

---

## 对行动计划的影响

1. **Q1 从 P0 降级**——不需要"由 checkpoint 配置重建模型"来修复滤波器载入；但仍建议按修订版要求记录并实测滤波器状态（已在本脚本中实现）。
2. **Q2 提升为 PR-LWT 的核心动机**——不再是纸面推理，而是实测 60.9% 的闭环损失。
3. **Q3 提升优先级**——中性门控对照（`w=sigmoid(g); fused=(1-w)*CT+w*MRI`）应从 P1 提前，因为它是当前最大的单点杠杆。
4. **Q4 确认 P0**——所有既有数字必须用局部窗口实现重算；旧的 0.7483 / 0.6886 不再作为任何成绩引用。

---

## 可复现性

```bash
cd D:/图像相关论文/MGF-Net
D:/DeveloperTools/miniconda/envs/mgfnet/python.exe diagnose_step1.py
```

脚本包含：Q1 滤波器载入核对、Q2 四类输入的闭环误差（含未训练 Haar 对照）、
Q3 门控权重分布统计、Q4 两种 SSIM 口径对比。
依赖：torch、numpy、Pillow、scipy（gaussian_filter）。
