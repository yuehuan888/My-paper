## 一、结论

**推荐：照公式用 Python 实现（clean-room 重写），并另做一次本地 Octave 对照生成金标准数值。推荐强度：明确推荐（对"随 MIT 仓库分发"这一硬约束而言是唯一解），但对"金标准从哪来"这一子问题只有倾向，因为存在若干我未能完全确认的点（见第六节）。**

关键理由一句话：MATLAB 那条路**并不能解决你的核心约束**。领域事实标准 `Linfeng-Tang/Evaluation-for-Image-Fusion` 仓库**没有 LICENSE**（我 curl `main` 分支 LICENSE 得 HTTP 404），即无许可＝不可再分发；你把它装到本地跑出来的数字是干净的，但审稿人拿不到那套 `.m`，仓库仍然不可复现。所以"装 MATLAB/Octave"只应作为**一次性的本地校验手段**，不能作为交付物。

---

## 二、事实基础

### 2.1 有没有可用的现成 Python 实现？

**没有一套现成的、许可干净的、覆盖你全部 10 项指标的 Python 包。** 我自己复核确认的情况：

| 类别 | 结论 | 我实际验证的方式 |
|---|---|---|
| PyPI/conda 包 | 不存在 | 第一轮已穷尽（simple index 889,485 包名匹配 `qabf` → 0 命中；conda-forge noarch repodata 全量） |
| 指标覆盖 ≥5 且许可明确的仓库 | **只有 1 个：`Yanyin-Guo/LSFDNet`** | 我 curl `https://raw.githubusercontent.com/Yanyin-Guo/LSFDNet/main/LICENSE` → HTTP 200，内容为 `MIT License / Copyright (c) 2025 Guo Yanyin`（真实署名，非占位符）。覆盖 EN/SD/SF/PSNR/MSE/VIF/CC/SCD/Qabf/Nabf/AG/MI/SSIM/MS-SSIM |
| 覆盖最全的 Python 实现 | 多个，但**全部无许可** | `Z-zachary/Image-evalution`：我 curl LICENSE → **404**，README 全文只有一行 `# Image-evalution`。`house3173`、`Hastings37`、`caoke-963`、`Zhaozixiang1228/MMIF-CDDFuse`、`slrl123/UniFuse`、`Zhen-yu-Liu/AdaSFFuse`、`fd-qhwang/WaveFusion` 同 |
| 带 MIT 但版权栏是空模板 | `mark-bear/fusion_metric` | 我 curl LICENSE → HTTP 200，首行是 `MIT License`，第二行原文 **`Copyright (c) [year] [fullname]`** — 占位符未填，无具名版权人；README 自述 copied from gitcode |
| MIT 且实现正确但只有 2 项 | `sewar`（VIFP/SSIM/PSNR/MSSSIM/UQI…）、`image-similarity-measures`、`scikit-image` | 第一轮 + 第二轮已逐行读源码确认；skimage 是 **BSD-3-Clause** |
| 纯 IQA、与融合无关 | `IQA-Metrics-Toolbox`（MIT 但只有一个 SSIM 对灰度图直接崩）、`pansharpening`（零指标 + MIT/Apache 声明自相矛盾）、`piq`/`pyiqa` | 第一轮 |

**对本项目更直接的一条实测**：`D:\图像相关论文\MGF-Net\utils\__init__.py` 第 1 行是 `from .metrics import ssim, psnr, vif_p`，但 **`utils/metrics.py` 不存在**（`ls utils/` 只有 `__init__.py`）。我用 mgfnet 环境实跑 `import utils` → `ModuleNotFoundError: No module named 'utils.metrics'`。也就是说**这个指标模块是计划中的、尚未开始写的**，当前 `utils` 包处于导入即崩的状态。仓库现有的相关代码只有 `compare_baselines.py` 里内联的 `compute_ssim` / `compute_psnr`（全图统计，正对应你 pending 的 #12）。

环境侧（`D:\DeveloperTools\miniconda\envs\mgfnet`，我实测）：numpy 2.2.6 / scipy 1.15.3 / scikit-image 0.25.2 / torch 2.5.1+cu121 / opencv 4.13.0 / Pillow 12.2.0 / pandas 2.3.3 / tqdm 4.68.3 都在；**缺** openpyxl、natsort、sewar、piq、pyiqa。`which matlab` 与 `which octave` 均为空 → 本机确实两样都没有。

### 2.2 现成 MATLAB 实现的可获得性与许可状况

我下载并解包了事实标准仓库 `Linfeng-Tang/Evaluation-for-Image-Fusion`（codeload `main`，HTTP 200，97,523,809 字节，含 TNO 图片）：

- 目录 `Evaluation/` 覆盖完整：`entropy_fusion.m`、`SD_evaluation.m`、`SF_evaluation.m`、`MI_evaluation.m` + `Hab.m`、`analysis_Qabf.m`、`analysis_SCD.m`、`vifp_mscale.m`、`CC_evaluation.m`、`PSNR_evaluation.m`、`MSE_evaluation.m`、`AG_evaluation.m`、`analysis_nabf.m`、`analysis_ms_ssim.m`、`analysis_fmi.m`。驱动 `Evaluation_for_Single_Image.m` 第 29 行一次返回 17 个值：
  `[EN, SF,SD,PSNR,MSE, MI, VIF, AG, CC, SCD, Qabf, Nabf, SSIM, MS_SSIM, FMI_pixel, FMI_dct, FMI_w]`
- **代码总量极小**：我 `wc -l` 得到 `entropy_fusion.m` 21 行、`SD_evaluation.m` 5 行、`SF_evaluation.m` 7 行、`MI_evaluation.m` 19 行、`Hab.m` 29 行、`analysis_Qabf.m` 103 行、`analysis_SCD.m` 11 行、`vifp_mscale.m` 98 行、`CC_evaluation.m` 4 行 —— **合计 297 行**。SCD 全文就是一行 `r=corr2(fus-img2,img1)+corr2(fus-img1,img2);`，Qabf 头部写明 `C.S.Xydeas and V.Petrovic, Electronics Letters`，参数 `L=1;Tg=0.9994;kg=-15;Dg=0.5;Ta=0.9879;ka=-22;Da=0.8;`。
- **无外部工具箱依赖**：我把 `Evaluation/*.m` 全部拼起来统计外部调用，只有 `filter2 / edge / conv2 / fspecial / imfilter / corr2 / entropy / rgb2gray / imread / ssim`，**没有 matlabPyrTools**。对比之下 `zhengliu6699/imageFusionMetrics` 打包了 `matlabPyrTools.zip`（含 mex 源码）来跑 `metricWang/PWW/Yang`，那条路要编译 mex，可移植性差得多。
- **许可**：仓库根 LICENSE 在 `main` 分支 **HTTP 404**。但逐文件看头部时发现**两个文件自带明确的宽松许可**：
  - `vifp_mscale.m` 头部：`Copyright (c) 2005 The University of Texas at Austin` … `Permission is hereby granted, without written agreement and without license or royalty fees, to use, copy, modify, and distribute this code (the source files) and its documentation for any purpose, provided that the copyright notice in its entirety appear in all copies of this code`（Sheikh & Bovik，LIVE/UT Austin）
  - `analysis_ms_ssim.m` 头部：`Copyright(c) 2015 Kede Ma, Kai Zeng and Zhou Wang`，同样是"provided that this copyright notice and the original authors' [notice appear]"式授权
  - 其余（`entropy_fusion.m`、`SD_evaluation.m`、`SF_evaluation.m`、`analysis_Qabf.m`、`analysis_SCD.m`、`Hab.m`、`CC_evaluation.m` 等）**无任何许可头**。

这两条是本次调查里唯一新增的、有可操作价值的事实：**VIF-P（`vifp_mscale.m`）和 MS-SSIM 参考实现是可以合法 vendor 进你的 MIT 仓库的**（保留原版权声明 + 在论文里引用 Sheikh-Bovik 2005 / Wang SSIM）。其余算法仍需自行按论文重写。

---

## 三、两条路的逐项对比

| 维度 | 路 A：Python 照公式重写 | 路 B：装 MATLAB/Octave 跑 .m |
|---|---|---|
| **许可与可分发** | **可行**。算法（Xydeas-Petrovic 边缘保持、Aslantas-Bendes SCD、Qu 互信息、Wang SSIM、Sheikh-Bovik VIF）属公开公式，不受版权保护，表达才受保护；clean-room 重写后你可自行以 MIT 发布。另外可直接 vendor `vifp_mscale.m` 与 `analysis_ms_ssim.m`（我确认二者头部有明确再分发授权）。 | **不可行（致命）**。`Evaluation-for-Image-Fusion` 根 LICENSE 我实测 404；`zhengliu6699/imageFusionMetrics`、`thfylsty/Objective-evaluation-for-image-fusion`、`liuuuuu777/ImageFusion-Evaluation` 同。无许可＝All rights reserved，把 `.m` 放进 MIT 仓库即侵权；不放进仓库则审稿人无法复现，硬约束照样落空。 |
| **可复现性（审稿人自己跑）** | **强**。`pip install` 你的仓库即可，mgfnet 环境已有 numpy/scipy/skimage/torch，无外部依赖。 | **弱**。要么审稿人自备 MATLAB（商业授权，多数单位有但非人人有，且版本差异会影响 `fspecial`/`edge` 等），要么自备 Octave + `image`/`statistics` 包的对应版本。而且他脚下那份 `.m` 无许可，你无法随仓库附上，只能让他自己去第三方仓库找——这本身就不可复现。 |
| **一次性成本** | 约 250–350 行 Python（含 VIF-P 移植），估计 2–3 个工作日 + 半天验收。已有环境，零采购。 | 装 Octave（winget 有 `GNU.Octave 11.3.0`，免费）约 1 小时；装 MATLAB 约 1 天 + 授权成本（学术版通常数千元/年，我未查具体报价）。但省下的只是那 250 行。 |
| **长期维护** | 自己拥有代码，指标定义要改（换 MI 变体、换 SSIM 窗）当场改，CI 里可加不变量测试。 | 依赖一个你无法修改分发的外部 MATLAB 版本；你的 `utils/__init__.py` 已经写过 `from .metrics import ssim, psnr, vif_p`，一旦决定走 MATLAB 路线，Python 侧仍然要写一层 subprocess 包装，等于**两条路都要维护**。 |
| **出错风险** | 集中且已知（见第四节逐项陷阱）。最危险的是 MI 的求和/平均（差 2 倍）与 SSIM 的窗/协方差归一化；SD/SF 因量纲敏感反而不易漏。 | **更低**。这是领域标准脚本，值是"审过稿的"。但注意：它算的 VIF 是 `vifp_mscale`（像素域 VIF-P），**不是** Han 2013 的融合专用 VIFF——如果你最终要报 VIFF，MATLAB 路给不了，`house3173` 的 `VIFF_Function.py` 才有但那个仓库无许可。 |
| **被质疑的风险** | 中。审稿人会问"你的 SSIM 常数是不是 0.01/0.03、窗是不是 11×11 高斯 σ=1.5"。回答方式是把参数和引用写进论文 + 提供 `tests/` 里的不变量测试，可完全化解。真正的雷是自研版本与文献表格对不上——必须做金标准校验。 | 低（值本身），但**换成许可风险**。"指标代码随 MIT 仓库分发"这条你自己写下的约束直接不成立；审稿人或后期读者一旦问起 `.m` 的来源，你无法给出许可依据。 |
| **与现有 Python 工具链契合度** | **高**。mgfnet 环境已具备全部依赖，`for` 循环遍历上百张图零成本；`compare_baselines.py` 里已有的 `compute_ssim`/`compute_psnr` 可以直接替换成正确实现（对应你的 pending #12）。 | **低**。必须落盘成图片 → subprocess 调 Octave → 解析文本输出，上百张图的 I/O 与进程启动开销大，中间文件清理、路径转义、并行化都要自己写，且 Windows 下 Octave 的中文路径/编码容易出问题（你的工作目录就是 `D:\图像相关论文\MGF-Net`）。 |

**补充一条决定性的定量依据**：MATLAB 路的全部价值就是"值可信"。但那 297 行里，**只有 2 个文件（`vifp_mscale.m` 98 行、`analysis_ms_ssim.m`）是你可以合法带走的**，其余 ~200 行的算法你无论走哪条路都得自己写。为了这 200 行去装一整套 MATLAB 生态、并且把交付物永久绑在一个不可分发的依赖上，性价比是负的。

---

## 四、如果推荐 Python 移植（本节即推荐方案）

### 4.1 实现顺序

**第 0 层 — 先把地基做对（半天）**
统一 I/O 约定并**写死**在文档里，否则后面每个指标都会踩一遍：
- 输入一律转 `float64`；**内部一律在 [0,255] 域计算**（这是最关键的一条约定，SD/SF/PSNR/SSIM 的常数全绑定这个域）
- 灰度化用 `cv2.cvtColor(..., cv2.COLOR_BGR2GRAY)` 或 skimage，与论文里"转 YCbCr 取 Y"的做法对齐（VIF-Benchmark 里是 `RGB2Gray.m`/`Y2RGB.m`，即 YCbCr 的 Y，不是简单的加权平均——这点要在文档里写清楚）
- 文件名配对准入：CT/MRI/Fused 三元组显式配对 + 尺寸/通道校验（对应你的 pending #13）

**第 1 批 — 简单且无歧义（半天）**，先跑通端到端管线：
`EN`、`SD`、`SF`、`PSNR`、`CC`。这 5 项公式短、无参数歧义，能立刻给你一个可运行的批量脚本骨架。

**第 2 批 — 中等（1 天）**：
`MI`、`SCD`、`Qabf`。这三项都需要**先做一次"定死变体"的决策**（见下），代码量各 20–60 行。

**第 3 批 — 最难、放最后（1 天）**：
`SSIM`（局部窗口）→ `VIF`。

`SSIM` 最省力的正确做法**不是**自己从零写高斯滑窗，而是直接调 `skimage.metrics.structural_similarity(gaussian_weights=True, sigma=1.5, use_sample_covariance=False, data_range=255, win_size=11)` —— 第二轮已确认 skimage 文档明示这组参数才是匹配 Wang MATLAB `ssim.m` 的用法，默认参数（`win_size=7` + `uniform_filter` + 样本协方差）会差在千分位。skimage 是 BSD-3-Clause，与本仓库兼容。**但注意**：融合论文报的一般是 `0.5*(SSIM(A,F)+SSIM(B,F))` 或两源之和，这个外层组合要自己写，且**必须均值不要求和**（`mark-bear` 就是写成了求和，实测 1.0222 > 1，数学上不可能）。

`VIF` 有两条路：
- **推荐**：移植 `vifp_mscale.m`（98 行，Sheikh-Bovik 4 尺度像素域 VIF-P）。**这个文件有明确再分发许可**，你可以合法地把移植结果按 MIT 发布，只需在文件头保留 UT Austin 版权声明并在论文中引用 Sheikh & Bovik 2005。或者更省事：直接用 `sewar`（MIT，第一轮已确认 `vifp` 实现存在、参数 `N=2^(4-scale+1)+1`、`sigma_nsq=2`）。但 `sewar.ssim` 对 float 输入会崩（`np.iinfo(np.float64)` 抛 `ValueError`），只取它的 VIFP。
- **不要做**：Han 2013 的融合专用 VIFF（ComVidVindG）。第三轮已判定其 CSF 权重表/块大小/GSM 参数在论文中不完备，且**没有任何许可清晰的 Python 实现**可对照。若论文必须报 VIFF，明确写"采用 VIF-Benchmark 的 VIFF 定义"并接受只能用 Octave 本地算这一项。

### 4.2 每个指标的关键陷阱（引自第三轮评估 + 我的实测）

| 指标 | 陷阱 | 我的实测/依据 |
|---|---|---|
| **EN** | 直方图 bin 数；log 底数 | 我在 256×256 uint8 随机图上实测：**bins=256 → 7.9976；`np.histogram` 默认 10 bins → 3.3214（静默给假值）；换 log e → 5.5431**。必须显式 `bins=256, range=(0,256)`，必须 `log2`，必须掩掉 `p=0`（否则 NaN） |
| **SD** | [0,1] vs [0,255] 量纲 | 实测同图：`float[0,1]` 域算 SD=73.859，×255 后得 18834.023（荒谬）。ddof 差异（N-1 vs N）在全图上约 1.0000077 倍，可忽略 |
| **SF** | 分母写法 | `1/(MN)` 与 `1/(M(N-1))` 差约 0.1%，两种都见过；选定后写进论文。**禁用 `np.roll` 做循环差分**（会在边界造出假高频）。RF/CF 命名互换不影响结果（对称） |
| **CC** | 必须 Pearson | 去均值 + 分母带根号；`sewar.scc` 是拉普拉斯高通变体，不是标准 CC，不能混用 |
| **PSNR** | 峰值与源的组合方式 | **该系列代码通病**：MSE 在 0–1 尺度算却除以 255 取对数，导致固定虚高 `20*log10(255)=48.13 dB`。第二轮实测：`Z-zachary` 报 61.06（标准 12.93）、`mark-bear` 报 63.647（正确 15.516）。融合 PSNR 标准是**两源平均**，`house3173` 的 `PSNR_function` 收了 `img2` 却完全不用，直接抄会与文献不可比 |
| **MI** | **选型风险全场最高** | 求和 `MI(F,X)+MI(F,Y)`（典型 2.0–3.5）vs 平均（1.0–1.8）**差 2 倍且都"看似合理"**。另有归一化/Kvalseth D2、FMI、MI+CE 等变体。必须先定死并引文献（Qu/Zhang/Yan 2002 原式，或 Hossny 2008 doi:10.1049/el:20081754）。实现用 256×256 联合直方图，**注意整型输入**（浮点会退化成 65536 bin） |
| **SCD** | 原论文公式印刷错误 | Aslantas-Bendes 2015 Eq.10 **分母缺根号**，照抄会得到远超 1 的值。必须用标准 Pearson + 交叉配对 `corr(F-S2,S1)+corr(F-S1,S2)`，合理区间约 1.3–1.9，**>2 基本判定为 bug**（两个 Pearson 各 ≤1，理论最大 2） |
| **Qabf** | 三个雷区 | (1) 方向差必须 `1-|Δα|/(π/2)` 后再 clamp 到 0；(2) min/max 比值条件分支；(3) **平坦区 0/0 产生 NaN 并沿 sum 传播成全图 NaN** —— 必须显式处理。常数用 `L=1,Tg=0.9994,kg=-15,Dg=0.5,Ta=0.9879,ka=-22,Da=0.8`（我已从 `analysis_Qabf.m` 原文确认这组值）。Sobel 符号约定无害（xy 同翻符号 Δα 不变） |
| **SSIM** | 窗与协方差 | 用 skimage 的 `gaussian_weights=True, sigma=1.5, use_sample_covariance=False, data_range=255`；**当前仓库用全图统计算 SSIM 不是"变体"而是错**（对应 pending #12）。另注意 `mark-bear` 的 ssim.py 有 dtype 陷阱：uint8 输入被 `to_tensor` 除以 255、float32 不除，同一张图差 52% |
| **VIF** | VIF-P ≠ 融合 VIFF | `vifp_mscale` 是 Sheikh-Bovik 像素域 VIF，4 尺度、`σ_nsq=2`（绑定 [0,255]）；融合论文常报的 VIFF 是另一个东西。二者不能互换署名 |

### 4.3 验收方案

**这是整个方案成立与否的关键**，分三层，从便宜到贵：

**L0 — 不变量与单调性（必须做，10 分钟，进 CI）**
这些测试不需要任何外部参考值就能抓出绝大多数移植错误：
- `F = A` 时：`SSIM(A,F) = 1`、`CC = 1`、`PSNR = inf`、`Qabf(A,A,A) = 1`、`MI(A,F) = H(A)`
- 常量图：`EN = 0`、`SD = 0`、`SF = 0`（**同时是最狠的 NaN 测试**——Qabf 的 0/0 在这里必炸）
- 值域钳制：`Qabf ∈ [0,1]`、`SSIM ≤ 1`、`CC ∈ [-1,1]`、`SCD ∈ [-2,2]`
- 单调性：向 `F` 逐步加高斯噪声，`SSIM / Qabf / PSNR / VIF` 必须**单调下降**
- 尺度一致性：同一图以 `uint8` 与 `float[0,1]` 两种方式喂入，结果**必须完全相同**（这一条直接抓 SD 量纲与 SSIM dtype 两类 bug）
- 边界形状：1×1、奇数边长、边长不是 11 的倍数（局部窗口 SSIM 的 `mode='valid'` 边界）
- 顺序无关性：交换 CT/MRI 两源，`MI` 与 `CC` 的均值形式应不变，`Qabf` 应基本不变

**L1 — Octave 金标准对照（本地一次性，绝不进仓库）**
用 winget 装 `GNU.Octave 11.3.0`（我已 `winget search --source winget octave` 确认存在），加 `image` 包（我确认 `octave.sourceforge.io/image/function/fspecial.html` 与 `edge.html` 均 HTTP 200），跑 `Evaluation/` 下那套 `.m`，在你**自己项目的真实 CT-MRI 融合图上**生成一组金标准数值。然后断言 Python 与 Octave 的偏差在容差内。
- **容差定多少要诚实**：EN/SD/SF/CC/SCD/MI 应能到 1e-6 相对误差；Qabf 因 Sobel/atan 的浮点累加，1e-4 更现实；VIF-P 与 SSIM 因卷积边界实现不同，1e-3 ~ 1e-2 属正常，若差到 1e-1 说明参数错了。
- **许可说明**：本地跑无许可的 `.m` 用于产生对照数字，在版权上是灰色地带（严格说 All rights reserved 连本地使用也未授权）。`vifp_mscale.m` / `analysis_ms_ssim.m` 因有明确授权无此问题。**数字本身是事实，不受版权保护**，你可以放心把"我们的实现与参考实现偏差 < 1e-6"写进论文。
- **这份 Octave 脚本不要 commit**，`.gitignore` 掉，且不要把它写进论文的 reproducibility 声明（否则等于告诉审稿人"请去下载一个无许可的仓库"）。

**L2 — MIT 生态交叉验证（可选，但很便宜）**
- `sewar`（MIT）的 `vifp` ↔ 你的 VIF-P：应高度一致
- `scikit-image`（BSD-3）的 `structural_similarity(gaussian_weights=True, sigma=1.5, use_sample_covariance=False)` ↔ 你的 SSIM
- 这一层的作用是"我的实现不是自说自话"，且两个依赖都是许可干净的，可以正大光明写进 `requirements.txt` 与论文

**L3 — 文献量级对照（最后一道闸）**
把你算出的值域与已发表表格对照：SD 应在 40–80（[0,255] 域）、SF 约 10–20、EN 约 6–8、MI 约 2–3.5（求和版）、Qabf 约 0.3–0.7、SSIM 约 0.5–0.9、SCD 约 1.3–1.9。**任何一个飞出这个区间就说明量纲或变体选错了**——这一条能抓住"数字漂亮但对不上"的情况（比如 `house3173` 的 SD=0.319、SF=0.119 是 /255 归一化的结果，与文献表格不可比）。

### 4.4 工作量估计

| 阶段 | 内容 | 估计 |
|---|---|---|
| 0 | I/O 约定 + 校验 + 批量驱动骨架 | 0.5 天 |
| 1 | EN / SD / SF / PSNR / CC | 0.5 天 |
| 2 | MI / SCD / Qabf（含变体定死与文献引用） | 1 天 |
| 3 | SSIM（skimage 参数封装 + 两源组合）+ VIF-P 移植 | 1 天 |
| 4 | L0 不变量测试套件 | 0.5 天 |
| 5 | Octave 环境搭建 + L1 金标准对照 + 调容差 | 1 天（含 winget 安装与 image 包调试） |
| 6 | L2 交叉验证 + 论文写法说明（每个指标注明公式来源与参数） | 0.5 天 |
| **合计** | | **约 5 个工作日**，其中硬骨头是 Qabf 的 NaN 边界与 Octave 环境的包依赖调试 |

对比之下，MATLAB 路省掉的大约只是第 2、3 阶段的一半（约 1.5 天），代价却是硬约束不成立。**不值得。**

---

## 五、如果推荐 MATLAB（备选，仅作为本地校验手段）

我**不推荐**把它作为交付路径，但如果你只想要"花 1 小时换一个不可质疑的金标准"，可以这样做：

**装什么**
- **优先 Octave，不要 MATLAB。** 我实测 `winget search --source winget octave` → `GNU.Octave 11.3.0` 可用，免费、GPL，无授权成本与授权合规问题。MATLAB 是商业授权，本机无，且学校授权是否覆盖你的使用要看单位政策（**我未查任何报价或高校授权条款，这部分是我不掌握的**）。
- **重要坑：不要试图 conda 装 Octave。** 我查了 `api.anaconda.org/package/conda-forge/octave`：最新 10.3.0，`subdirs = ['linux-64', 'linux-aarch64', 'linux-ppc64le', 'osx-64', 'osx-arm64']` —— **没有 `win-64`**。所以 `conda install -c conda-forge octave` 在 Windows 上必然失败，只能走 winget/官方安装包/chocolatey（`community.chocolatey.org/packages/octave` 我确认 HTTP 200）。
- **还需要 Octave 的 `image` 包**（提供 `fspecial`、`edge`、`imread`、`rgb2gray`、`imfilter`）。我确认 `octave.sourceforge.io/image/function/fspecial.html` 与 `edge.html` 均 HTTP 200，包存在。但 `rgb2gray`、`corr2`、`imfilter` 我在 Octave Forge 对应包页与核心文档里**没能通过直接抓取确认**（`rgb2gray.html` / `corr2.html` 均 404），需要在真机上 `pkg load image; which rgb2gray` 验证 —— 见第六节。

**怎么和 Python 衔接**
- 落盘策略：Python 端把 CT/MRI/Fused 按固定命名写入临时目录 → `subprocess` 调 `octave-cli --eval "..."` → Octave 侧写 CSV → Python 读回。
- 上百张图建议**一张图一次 Octave 进程、批量传目录**（不要在 Python 里逐张起进程，Windows 下进程启动约 1–2 秒，100 张×6 方法 = 600 次启动就是 10–20 分钟纯开销）。
- 路径一律用 ASCII 临时目录，**别把中文路径传给 Octave**（你的工作目录是 `D:\图像相关论文\MGF-Net`，中文路径在 Windows + Octave 下是高危项）。
- 只用它算你 Python 侧最没把握的项（Qabf / SCD / VIF），EN/SD/SF/PSNR/CC 没必要绕这一圈。

**许可怎么处理才不影响论文和仓库**
- **绝不把任何无许可 `.m` 提交进仓库**，包括放进 `tests/reference/` 这种"看起来无害"的位置。
- 唯一可以合法进仓库的两个文件：`vifp_mscale.m` 与 `analysis_ms_ssim.m` —— 前提是**完整保留其版权声明头**（我确认二者都有 "provided that the copyright notice ... appear in all copies" 条款），并在论文里引用 Sheikh & Bovik 2005 / Wang 等人的 SSIM。即便如此，仍建议优先用 `sewar`（MIT）与 `skimage`（BSD-3）的等价实现，省掉署名义务。
- 论文的 reproducibility 声明只能写"我们的评估代码以 MIT 许可随仓库提供"，**不要写"我们使用了 VIF-Benchmark 的 MATLAB 脚本"**，除非你把那套脚本换成了有明确许可的等价物。金标准对照的结论可以写（"与参考实现偏差 < 1e-6"），因为那是你测出来的事实。

---

## 六、诚实的不确定性

以下是我**没能确认**的，请注意别把它们当成已验证的事实：

1. **我没有真的安装 Octave，也没有真的跑过任何 `.m` 文件。** 所以"Octave 能跑通 `Evaluation/` 那套脚本"是**基于依赖分析的推断**（我确认脚本只用 `filter2/edge/conv2/fspecial/imfilter/corr2/entropy/rgb2gray/imread`，无 matlabPyrTools、无 mex），不是实测结论。真正的风险点是 `rgb2gray`、`corr2`、`imfilter`、`imread` 在 Octave 里的归属包，我抓取对应函数页得到 404，**未能确认它们是否随 Windows 版 Octave 开箱可用**。装完第一件事应该先跑 `pkg list` 确认。
2. **MATLAB 的成本、授权条款、高校覆盖情况，我完全没有查**，第五节里那部分只能算定性描述，不要引用为事实。
3. **LICENSE 的存在性检查我对 `main` 分支做了直接 curl**（`Evaluation-for-Image-Fusion` 404、`Z-zachary` 404、`LSFDNet` 200、`SeAFusion` 200、`mark-bear` 200 且占位符未填）。`master` 分支我只对 LSFDNet 与 Z-zachary 抽验（结果一致）。**我没有穷举每个仓库的每个分支**，理论上存在"默认分支外另有 LICENSE"的可能，但按惯例这种可能性很低。
4. **GitHub API 在本次调查中全程返回 403 rate limit**（`API rate limit exceeded for 31.223.184.214`），所以我**无法用 API 复核 stars、pushed_at、license.spdx_id 等元数据**。上面所有许可结论都来自**直接 curl raw 文件内容**，这一点是可靠的；但"仓库是否仍在维护"这类结论我没有独立验证，只能引用前两轮的说法。
5. **我没有在本机实测过任何第三方 Python 指标实现。** 第二轮报告里那些具体数值（Z-zachary 的 EN=7.0781/SD=33.2406、mark-bear 的 SSIM=1.0222、PSNR 虚高 48.13 dB 等）是上一轮的结果，我这一轮只复现了两个最基础的陷阱（EN 的 bin 数与 SD 的量纲，见 4.2 表内实测值），**其余数值我未独立复现**。
6. **`vifp_mscale.m` 与 `analysis_ms_ssim.m` 的授权条款我读了头部前若干行，没有逐字读完整个许可全文，也没有法律审查。** 我的理解是"保留版权声明 + 引用即可再分发"，但在把代码放进要投稿的仓库前，建议你自己读一遍那两段声明原文（`/tmp/mat/Evaluation-for-Image-Fusion-main/Evaluation/vifp_mscale.m` 第 3–17 行附近，以及 `analysis_ms_ssim.m` 头部）。
7. **我没有验证"算法本身不受版权保护"这一法律判断**，这是通行理解（思想/表达二分），但我不是律师，也没有查判例。若你的单位有法务，值得问一句。
8. **工作量的 5 个工作日是估算**，基于代码行数（MATLAB 参考实现 297 行 → Python 约 250–350 行）和陷阱数量推算，**没有实际写过一行**。

**一句话总结**：走 Python 自研 + Octave 本地对照。MATLAB 那条路省不了多少事，却会让你明确写在需求里的那条硬约束（"指标代码随 MIT 仓库分发、审稿人可复现"）无法成立——而且顺带发现，`utils/__init__.py` 里 `from .metrics import ssim, psnr, vif_p` 指向的 `utils/metrics.py` 目前根本不存在，这块本来就是空白，正是从零做对的最好时机。