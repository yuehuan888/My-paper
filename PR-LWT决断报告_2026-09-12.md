All checks done. Here is the report.

---

# PR-LWT 决断报告

分析基于我自己重跑的 9 个 `results.json`、40 张融合 PNG、9 个 `best.pth`，以及 `models/mgf_net.py`、`utils/metrics.py`、`losses.py`。复算脚本在 `D:/图像相关论文/MGF-Net/_verify/`（`affine_check.py`、`refine_check.py`）。凡是引用他人数字的地方我都独立复算过，复算不上的会标注。

---

## 一、结论

**三组在统计上确实"检不出差异"，但这不是"证明无差异"——这是一次功效低到几乎不可能检出任何东西的实验（30 个比较里有 0 个达到自身最小可检测效应量）。**

**但在"PR-LWT 是否有用"这个方向上，答案不受功效问题的困扰：没有一条证据支持 PR-LWT 改善融合质量，点估计一致偏向不利。** 而且真正的问题不在 PR-LWT——在于这套流水线**在结构上就看不见频域变换**：被操纵的变量占全部参数的 **0.025%**（24/94234），下游一个占 **21.1%** 的自适应头把变换之间的差异压缩了 **4.2–14.4 倍**（28.9/255 → 2.2/255）。

**决断：PR-LWT 不应继续作为论文的核心创新。** 但要写对理由——不是"PR 没用"，恰恰相反，PR 是本实验里唯一把闭环相对误差从 **64%–120%** 压到 **1e-7** 的方案，而且是结构上必然成立的。问题是**融合质量的评价协议对这个变换完全不敏感**。所以 PR-LWT 的正确定位是"可靠性/可审计性"贡献，不是性能贡献。

**同时必须指出一个比 A/B/C 大得多、且被这组实验完全掩盖的事实：一个零参数的平凡平均 0.5·(CT+MRI) 在 MI、CC、PSNR、VIF 四项上全面击败全部三个训练模型**（MI 3.41 vs 2.40，差 42%）。三者之间的最大差异只有 2.8%。这才是论文最该担心的事。

---

## 二、证据强度：哪些是"确实无差异"，哪些是"无法判断"

这是我实测的数字，不是转述。

### 2.1 三类证据的强度完全不同

| 层面 | 单元 | 结果 | 可信度 |
|---|---|---|---|
| 种子层（n=3, df=2 配对 t） | 训练模型 | **0/30 显著**，最小 p=0.0528（EN B−C） | 数字可靠，但**功效近零** |
| 逐图 n=120 池化（3 种子 × 40 图） | 图像 | 22/30 p<0.05 | **伪重复，p 值被严重高估** |
| 同种子内逐图 n=40 | 图像 | 78/90 p<0.05 | **伪重复，同上** |

关键的一步是我算了**最小可检测效应量（MDES，80% power, α=.05, 配对 t）**：

- 30 个（指标 × 条件对）格子的 MDES **中位数 = 均值的 2.583%**
- 实测到的**最大**效应 = **2.765%**（VIF B−C）
- **30 个格子里 0 个的实际效应达到它自己的 MDES**；最接近的 EN B−C 只有 0.78× MDES（这正好对应 p=0.053，因为显著门槛是 obs/MDES > 4.3027/5.3634 = 0.802）

**这是本报告最重要的一个数字。** 观测到的全部效应都落在探测下限以下。所以"无显著差异" = **完全没有能力检出**，而不是"差异被证明很小"。

分指标 MDES（占均值 %，三对平均）：SSIM 0.38、PSNR 1.55、EN 1.85、CC 2.09、MI 2.40、SD 4.16、Qabf 4.18、VIF 5.28、SCD 10.98、SF 11.90。PSNR/SSIM 这类保真指标在 n=3 下**永远测不出来**（要 200–600 颗种子）。

另一处口径更正：题面表格的 ± 是总体标准差 (ddof=0)。n=3 时 ddof=1 的放大倍数是常数 **√(3/2)=1.2247**，即 30 个格子一律 +22.47%（不是 17–27% 的区间）。论文里应写明口径。

### 2.2 真正"确实"的是闭环，不是融合质量

这一条**不受功效问题影响**，因为它是确定性的数值诊断，不是统计推断：

| 条件 | 闭环 rel_L2（三种子） | 我从 best.pth 用 CPU 独立重算（5 张图） |
|---|---|---|
| A 固定 Haar | 1.447e-07（三种子逐位相同） | 2e-8 ~ 8e-8 |
| B 无约束 | **1.196 / 0.641 / 0.931** | **1.19 ~ 1.21** |
| C PR-LWT | 1.51e-07 / 1.61e-07 / 1.88e-07 | 9.0e-8 ~ 1.9e-7 |

三点确认：
1. **B 的闭环误差超过 1**（seed0）意味着重构误差的范数**大于原图本身**——比"丢六成信号"的诊断更糟。
2. **A 与 C 的闭环是同一量级**（都是 float32 精度极限 1e-7）。C 并没有比未训练的 Haar "更可逆"——它只是**恢复了**Haar 本来就有的可逆性，同时多了 24 个自由度。这是 PR-LWT 定位的关键：它的价值是"让可学习这件事不破坏可逆性"，不是"提高可逆性"。
3. **C 的 PR 在 50 轮训练后依然成立**，theta 从 0 漂移到 max|theta|≈0.30–0.37 后闭环仍是 1e-7。这一点是干净的正结果。

### 2.3 逐图"系统性差异"是真的，但它不是融合质量

逐图配对确实检出 22/30 显著，且 EN A−B（40/40 图同号）、EN B−C（40/40）、SD A−C（40/40）三个在三种子上全部同号且各自显著。这些**不是噪声**。但它们的性质被两件事定性了：

**(a) 逐图 n=120 是伪重复。** 40 张测试图来自**只有 2 个病例**（37 与 41 各 20 张相邻 5mm 切片，我从 manifest 的 case_id 列核实过）。真正的独立重复单元是训练种子（n=3）。把同一模型内的 40 张相关切片当 40 次独立重复，会让 p 值虚高若干数量级——这正是需要避免的"把可检测当成真实"的同类错误。

**(b) 存活下来的效应全是无参考指标，而且经不起一次 affine 修正。** 我做了决定性检验：对每个条件做**逐图全局增益+偏移**匹配（只改对比度标定，不动任何结构、不动任何像素的相对关系），再重算指标：

```
met   |    B-A raw    C-A raw  |  B-A matched   C-A matched
EN    |    +0.04473   +0.00283 |    +0.03160    -0.00330 FLIP
SD    |    +0.28204   +0.51720 |    -0.07897 FLIP  +0.02104
SF    |    -0.68724   +0.14981 |    -0.85304      -0.09896 FLIP
Qabf  |    +0.00140   +0.00046 |    +0.00048      -0.00042 FLIP
VIF   |    +0.00275   -0.00331 |    +0.00307      -0.00277
MI    |    +0.01668   -0.00736 |    +0.01267      -0.00908
```

（raw 列与 `results.json` 的均值逐位吻合，说明我的重算管线可以对齐。）

**C 唯一的招牌优势 SD（+0.517）在一次对比度匹配后掉到 +0.021（消失 96%）；C 的 SF、Qabf、EN 全部翻号。** B 的 SD 也从 +0.282 翻成 −0.079。也就是说：所有可观察到的"胜出"里，SD/SF/EN/Qabf 那一部分是**全局对比度标定差异**，不是融合结构质量差异。

**小结（这是本报告的核心区分）：**
- **"闭环大幅改善"是确定事实。**
- **"三组融合质量无显著差异"也是事实，但它是功效不足的产物，不能升级成"等价"。**
- **"没有任何方向支持 PR-LWT"是稳健的**——三条独立证据同向：逐图点估计 B 在 10 项中 7 项名义最高；C 在 CC/PSNR/VIF/MI 上低于 A；affine 匹配后 C 的优势全部消失或翻号。

---

## 三、机制解释：为什么 PR-LWT 传不到输出

四条机制，前三条我有直接实测证据，第四条是推测。

### 3.1 参数量级：0.025% 对一个 21% 的自适应头（有证据）

我加载三个 checkpoint 实测模块参数（`models/mgf_net.py`）：

```
fusion      74337  (78.89%)
edge_refine 19873  (21.09%)
变换部分     A=0    B=32+32=64   C=24
```

B 相对 A 增加 0.068% 的参数，C 增加 0.025%。**被操纵的变量在整个模型中是一个小到几乎不可见的扰动项，而下游有一个占五分之一的、用残差结构连接的自适应头。**

### 3.2 下游头把差异压缩了 4–14 倍（有证据，我独立复现）

我写了个脚本直接跑三个 checkpoint，取 `reconstruct` 之后（过 `edge_refine` 之前）和最终输出（过之后），逐图算平均绝对差（8-bit 单位）：

```
             A-B      A-C      B-C
edge_refine 之前:  28.877    7.792   31.271
edge_refine 之后:   2.157    1.849    2.177
压缩倍数:           13.4x     4.2x    14.4x
```

`edge_refine` 自己对输出的改动量：A 33.8/255、C 36.4/255、B 6.5/255。**这个 19873 参数的头把图像搬动了 33–36/255，同时把三个条件之间的差异压到约 2/255。** 最终输出两两相关系数 0.9983–0.9986。

这条是"零结果"最硬的机制性解释：在 `edge_refine` 之前变换的差异是**明显的**（A−B 差 28.9/255），在此之后就只剩 **2/255**，而所有报告的指标都是在这个被压平之后的输出上算的。

### 3.3 损失函数的最优解与基无关（有证据）

`losses.py` 的 `MGFusionLoss` 是**无参考的源一致性目标**：SSIM(f,CT)+SSIM(f,MRI) + 10·L1 + 5·grad + 2·|L1(f,CT)−L1(f,MRI)|，**没有任何 ground-truth 融合图**。它的最优解是源图之间的整个区间，与用哪个正交基几乎无关。

我在 40 张测试图上实测了目标值：

| 输入 | loss |
|---|---|
| f = CT | 1.5808 |
| f = MRI | 1.6018 |
| f = 0.5(CT+MRI)（零训练） | 1.3085 |
| **A** | **1.2285** |
| **B** | **1.2358** |
| **C** | **1.2343** |

架构相对平凡混合的改进 ≈ 0.073–0.080；**三者之间的差距只有 0.0073，占该改进的 ~9%**。而且 `utils/metrics.py` 里 SSIM/PSNR/CC/VIF 是 `0.5·(·(F,S1)+·(F,S2))`、MI 是两项求和——**全部与训练目标同向**，因此三者收敛到同一最优区域是预期行为，不是意外。

### 3.4 变换本身只是一个线性层（部分推测）

我验证了 B 的扭曲确实很大：B 的非线性增益是强频带选择性的（LL2 振幅 ×2.22、HH1 ×0.48），三种变换在子带层面**无任何跨子带泄漏**（非对角全为 0）。但**用同一个固定 Haar 基去量三组融合输出，逐子带能量只差 1–5%**——说明融合模块+下游把扭曲的增益吸收掉了。

推测（无直接证据）：既然变换是线性的、且每个子带配独立的 FusionBlock，网络完全可以用 FusionBlock 的权重补偿基的变化。**可学习小波在这个架构里与"把 FusionBlock 的输入权重换一个初始化"在表达能力上高度重叠。** 这条我标注为**推测**，没有做权重层面的直接验证。

### 3.5 顺带发现的更严重问题（有证据，我必须报告）

我在 40 张测试图上算了平凡平均 0.5·(CT+MRI) 的全部 10 项指标。为排除"只是对比度不同"的可能，我又把它**逐图 affine 匹配到网络的 mean/std** 再算一遍：

```
met   |  avg 原始 | avg→匹配网络尺度 |  网络 A   | 结论
EN    |   3.8027  |     3.7704      |  4.0661   | 网络赢
SD    |  64.0400  |    71.8273      | 72.9100   | 网络赢
SF    |  18.1149  |    20.6858      | 32.9261   | 网络赢
Qabf  |   0.3767  |     0.4789      |  0.6101   | 网络赢
MI    |   3.4135  |     3.4111      |  2.4005   | 平凡赢（差 42%）
CC    |   0.8505  |     0.8502      |  0.8256   | 平凡赢
PSNR  |  15.6072  |    15.5128      | 15.0007   | 平凡赢
VIF   |   0.2853  |     0.2722      |  0.2163   | 平凡赢
SSIM  |   0.7500  |     0.7551      |  0.7533   | 匹配后平凡赢
SCD   |   0.9157  |     1.1543      |  1.0255   | 匹配后平凡赢
```

**这不是尺度假象**：把平凡平均缩放到网络自己的对比度后，它仍然在 MI/CC/PSNR/VIF 上击败全部三个模型。网络真正的净优势只在 **EN / SD / SF / Qabf** 四项（这几项衡量的是"锐/信息量大/边缘强"）。

对照一下量级：**网络 vs 平凡平均在 MI 上差 42%，而 A/B/C 之间的最大差只有 2.8%——相差 15 倍。** 论文若把 CC/VIF/PSNR 作为融合保真度的证据报出来，这个平凡基线会是个大麻烦。

---

## 四、对论文路线的影响

### 情形 1：若"确实无差异"成立
那么 PR-LWT 作为性能创新**必须放弃**。可写的核心变成两条，而且这两条都有我实测的数字支撑：
1. **一次干净的反向发现**：「完美重构是必要但远非充分的——闭环误差 64%–120% 的变换（B）在 10 项指标中 7 项名义最好，训练损失在 3 个种子上全部最低（1.6416/1.6591/1.6663）」。
2. **PR-LWT 作为可审计性工具**：它是唯一能在保持可学习的同时把闭环误差锁在 float32 极限的方案，因此是**做上面这个审计的前提**。

### 情形 2：若"功效不足"
那么既不能声称有差异，也不能声称无差异。**当前就处于这个情形**——0/30 效应达到自身 MDES。这时正确的动作是**先修协议再下结论**，而不是反过来。需要：
- 至少要过 80% 功效的种子数（我算过：EN B−C 需 1.3 颗、EN A−B 2.5 颗、VIF 3.5–4.2 颗；但 PSNR 212 颗、SSIM 264 颗、SCD 630 颗——这些"永远测不出来"），或改用 **TOST 等价检验并预设等价界**；
- 削弱/旁路 `edge_refine`，让变换的效应可见（现在它被压缩 4–14 倍）；
- 换掉无参考损失（加 ground-truth 或显著性引导项）；
- 把测试集扩到 ≥8–10 个病例，并按病例聚类做推断；
- 按 100 轮 LR 计划训到收敛（现在是 100 轮的 schedule 截断到 50 轮，milestone 70/90 从未触发，三组 best_epoch 同为 [50,45,45]，ep41–50 损失斜率仍为负）。

### 情形 3：若"确有差异"
现有数据**不支持**这个情形。若有差异，方向也是**不利于 C**：最像真差异的一条是 VIF B−C = +0.00606（+2.80%，三种子同号，B 更好），而不是 C 更好。

### 推荐路线

**推荐情形 2 的处置，但决策按情形 1 做。** 即：**立即停止把 PR-LWT 当作性能核心创新**（因为收益上限低、方向不利、且就算有差异也被自己的下游头吃掉了），同时**不要让论文声称"三种变换无差异"**（功效不足，会被审稿人一眼看穿）。

**论文的核心建议改为：一个频域可逆性的审计框架 + "闭环好≠融合好"的反向结论，PR-LWT 作为该框架下唯一同时满足"可学习"和"结构可逆"的实例。** 这个定位的好处是：唯一的强数字（B 的 64–120% 闭环 vs 零代价的融合质量）是确定的、可复现的、并且对社区有真价值——它质疑的是一个被广泛默认的假设。

但请注意：**这条路线目前也还没有被"证实"**，它需要的对照（旁路 edge_refine、有 ground-truth 的损失、多病例测试集）一个都还没做。在补上这些之前，无论选哪条路线，证据强度都不足以支撑一篇论文的核心主张。

---

## 五、诚实的局限

**本组实验没能回答的问题：**

1. **"三种变换等价"没有被证明，只是没被检出。** MDES 中位数 2.58%，0/30 效应达标。任何"等价/无差异"的措辞都必须配上这个数字。
2. **测试集只有 2 个病例。** 所有"逐图显著"的 p 值都是伪重复下的膨胀值。能外推到新病例的有效单元数是 2，不是 40，也不是 120。这条限制无法用统计方法绕过，只能换数据。
3. **三组都远未收敛。** 50 epoch 是 100 epoch schedule 的截断，末段仍在下降。比较发生在一个可能截断了真实效应的点上，**我无法从现有数据判断差异会不会在收敛后显现**。
4. **A vs B 不是干净对照。** 我实测确认 lifting 与 legacy 的 **LH/HL 语义是互换的**（lifting LH2 ≡ legacy HL2，max|diff|=2.09e-07；同名配对差 1.04）。所以 A vs B 同时改变了两件事：约束性与频带命名约定。而且设计里缺 (legacy, learnable=0) 这一格——**但注意，即使补上这一格，由于 band 互换它也不是干净的**，得先把命名统一。只有 **A vs C 是干净的单变量对照**（同为 lifting，仅 learnable 翻转）。
5. **一处我测出来不稳定的量，标注为不确定**：小波层占总梯度范数的比例，我自己的单次运行得到 B ≈ 18.5%、C ≈ 0.99%（另一路分析得到 47% 与 4.8%）。**方向一致（B 的变换吃掉显著更多的梯度），但具体数值随 batch 和损失尺度变化很大，不可引用。**
6. **affine 匹配实验的量化精度有限**：融合 PNG 是 8-bit 量化的，匹配后被比较的差异约 2/255，而量化噪声 ±0.5/255。所以 SD 那种大幅翻号（+0.282 → −0.079）是可靠的，**但 2/255 量级的微小差值不可信**。
7. **未做**：`edge_refine` 旁路消融（证明压缩机制的直接手段）、`train_loss` 在完整训练集上的重算、PR-LWT 验证测试 `tests/test_lifting_pr.py` 的实际执行（我读的是代码与 checkpoint 数值，未跑该测试文件）。

**下一步最该做的三件事，按优先级：**

1. **旁路 `edge_refine` 重跑 A/B/C**。这是唯一能判定"零结果是因为流水线看不见变换，还是因为变换真的不影响"的实验。我的压缩倍数（4.2×/13.4×/14.4×）已经预测了结果，但这个预测必须被验证。
2. **换损失函数 + 补 `(legacy, learnable=0)` 格并统一 LH/HL 命名**。前者让目标真正对变换敏感，后者才能把"无约束"从"实现方式"里分离出来。
3. **扩病例**。测试集从 2 个病例扩到 ≥8–10 个，并把种子上到 ≥10 颗。在此之前，**任何关于"显著/不显著"的结论都不该写进论文**——这既包括"无显著差异"，也包括逐图层面那些 p=1e-13 的"显著差异"。

**最后一点自我约束**：本轮最大的诱惑是把逐图层面的 22/30 显著当成"差异确实存在"，或者反过来把 0/30 当成"证明等价"。两者都是同一类错误——**把"检测能力"当成了"效应大小"**。上面所有数字里，只有闭环误差（确定性诊断）、参数量、edge_refine 压缩倍数、以及平凡基线对比这四项不受这个陷阱影响，应当被当作报告的硬骨架。