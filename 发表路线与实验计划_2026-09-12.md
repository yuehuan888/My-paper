# MGF-Net 发表路线与实验计划

> **制定日期**：2026/09/12
> **适用项目**：MGF-Net（多尺度门控频域融合网络），医学 CT-MRI 图像融合
> **目标**：发表一篇论文（期刊优先，接受率与方向对口优先于影响因子）
> **周期**：约 6 周（2026/09/12 – 2026/10/24）
> **算力**：RTX 3050 Laptop GPU (4GB VRAM)，PyTorch 2.6 + CUDA 12.4
> **投入**：每周 20 小时以上，单人

---

## 〇、本文件与其他文档的关系

| 文档 | 关系 |
|---|---|
| `前沿调研报告_图像融合2024-2026_复核补充版.md` §5.3 | **其路线已被本文件取代**。该节的 Phase 1–3（6+ 个月，冲 IF/TMM/TCSVT → TIP/TMI → CVPR/ICCV 2027）是博士生规模的计划，与"先发一篇"冲突。本文件保留其文献调研价值，废弃其路线规划 |
| `MGF-Net/工作进展报告_MGF-Net.md` | 记录了 v1 的设计与结果，仍有参考价值，但其中 §5.4 与 EMFusion 的对比表和 §6.3 的改进方向需按本文件重做 |
| `MGF-Net/outputs/figures/*.txt`、`outputs/test_results.txt` | **数字互相矛盾，均不可直接引用进论文**。Phase A 完成后全部作废重算 |

---

## 一、目标与约束

| 项 | 内容 |
|---|---|
| 总目标 | 发表一篇论文即可，不追求高影响因子 |
| 路线 | **B = 修地基 + 只加一个低成本创新点**，不再叠加新模块 |
| 会场 | **期刊**（不赶 deadline，周期长但门槛稳） |
| 创新点数量 | **1 个**（PR-LWT），损失函数改动不宣传为创新点 |
| 成败判据 | 方法在标准指标上**明确优于平凡基线（Average）**，且实验协议经得起审稿人核查 |

### 关键认知（决定后续所有取舍）

**6 个对比基线全部只需推理，不需要训练。** U2Fusion、SwinFusion、CDDFuse、SeAFusion、MEF-Net 官方均提供预训练权重，4GB 显存跑 forward 完全够用。**不要因为显存不足而砍基线**——基线数量不足是这类投稿最容易被拒的理由之一。

---

## 二、现状诊断：阻碍发表的四个硬伤

2026/09/12 通读 `MGF-Net/` 全部源码与产出物后确认：**阻碍发表的不是创新性不足，而是以下四条。** 原调研报告规划的 Mamba / 退化鲁棒 / 任务闭环，一条都解决不了它们。

### 硬伤 1：方法输给了"直接取平均"🔴

来源：`MGF-Net/outputs/figures/method_comparison.txt`

| 方法 | SSIM_Avg |
|---|---|
| **Average（简单像素平均，无网络）** | **0.7737** |
| WeightedAvg | 0.7623 |
| **MGF-Net v2.1** | **0.7483** |
| EMFusion | 0.7449 |

审稿人看到这张表会在 30 秒内拒稿。**这是第一优先级要解决的问题，优先级高于任何新模块。**

### 硬伤 2：评价协议不是领域标准 🟠

- 现用指标：`SSIM_CT / SSIM_MRI / PSNR_CT / PSNR_MRI`（融合图分别对两张源图算）——**自创口径**
- 领域标准：**EN、SD、SF、MI、VIF、Qabf、SSIM、SCD**（外加 CC、PSNR）
- 现用基线：`Average`、`WeightedAvg`、`LaplacianPyr` —— 这些是 1990 年代的经典方法，是"打不过就别投"的**地板**，不是竞争对手

### 硬伤 3：没有数据划分，测试集含训练图 🟠

- `outputs/figures/metrics_summary.txt` 中 10 行有 **8 行标注 `train`** —— "Overall Avg 0.7483"是在训练集上算的
- 训练集仅 8 对，`config.py` 靠 `oversample = 25` 扩到 200 样本/epoch
- `outputs/test_results.txt`（2 张测试图）给出 0.6886，**与上面的 0.7483 互相矛盾** —— 同一份工作有两套数字本身就是不能投的状态

### 硬伤 4：损失函数在设计上把模型推向"平均" 🔴

来源：`MGF-Net/losses.py` 的 `MGFusionLoss`

```python
loss_l1  = 0.5 * (l1(fused, ct) + l1(fused, mri))   # 驱动像素级折中
loss_bal = |l1_ct - l1_mri|                          # 强制对两模态误差相等
```

"对 CT 的误差和对 MRI 的误差尽量相等"——**这正是像素平均的定义**。模型收敛到接近 Average 是合理结果，不是训练不充分。

整个损失中唯一鼓励"超越源图"的是 `GradientLoss`（令融合图梯度匹配两源的**逐像素最大值**），但权重 5 < L1 的权重 10。

### 附带发现的两处代码缺陷（PR-LWT 要修的）

| # | 位置 | 问题 |
|---|---|---|
| a | `models/dwt_layer.py` | `MultiLevelDWT` 与 `MultiLevelIDWT` **各自持有独立的 `nn.Parameter` 滤波器**，无正交性约束。Haar 能逆变换依赖分析滤波器正交，一旦两组参数各自漂移，正交性丢失，**即使两张输入图完全相同，重构也不是原图**。疑似 PSNR 仅 ~14dB 的主因 |
| b | `models/gated_fusion.py` + `models/edge_refine.py` | `CrossBandAttention` 用 sigmoid（值域 (0,1)）乘融合后的小波系数——**只能衰减不能增强**；再叠加 `edge_refine` 末尾的 `clamp(refined, 0, 1)`，整体压低输出动态范围。PSNR 偏低的帮凶 |

---

## 三、战略：为什么是路线 B

### 3.1 对原调研报告结论的修正

原报告 §5.1 的判断链是：

> 发现"频域 + Mamba"已被 FDPMambaFuse (NN 2026)、AdaSFFuse (TMM 2026)、Shuffle-Mamba (TCSVT 2025) 占据 → **所以 MGF-Net 要叠 7 个维度的全栈创新去差异化**

**这个推论方向反了。** 正面战场被占，正确的做法是**缩到一个小而清楚的角落**，不是把所有维度都做一遍。后者是博士论文规模，且每多一个维度就多一个被审稿人挑死的面。

### 3.2 对"可学习小波已被占用"的再判断

原报告 §5.2 指出 AdaSFFuse (TMM 2026) 已采用自适应小波，故"MGF-Net 的 LFD 不构成卖点"。**该判断部分成立，但结论要改**：

- 无约束的可学习滤波器确实不新鲜 ✅
- 但**带完美重构保证的可学习小波，在医学图像融合里没有** ❌ 这正是 PR-LWT 的立足点

---

## 四、唯一创新点：PR-LWT

**PR-LWT（Perfect-Reconstruction Learnable Wavelet，完美重构约束的可学习小波）**

### 4.1 技术方案

用 **lifting scheme（提升格式）** 参数化小波，替换现有无约束的 `DWTLayer` / `IDWTLayer`。

**一维提升格式：**

```
分解：
    x → x_e (偶数样本), x_o (奇数样本)        # 仅切片，无卷积
    d = x_o − P(x_e)                          # 预测步 → 高频细节
    s = x_e + U(d)                            # 更新步 → 低频近似

逆变换（结构上是正变换的镜像）：
    x_e = s − U(d)
    x_o = d + P(x_e)
    x = interleave(x_e, x_o)
```

**关键性质：无论 P、U 被训练成什么样，逆变换都精确存在。** 因为逆变换不是另一组独立参数，而是把 P、U 反向减回去——可逆性由**结构**保证，而非由训练过程中的"希望它保持正交"保证。

**二维扩展：** 沿行、列各做一次可分离的一维提升，得到 LL/LH/HL/HH 四个子带；级联 2 级得到 7 子带（保持现有架构的 `{LL2, LH2, HL2, HH2, LH1, HL1, HH1}` 接口不变）。

**参数化与初始化：**

- P、U 用 3-tap 一维卷积（沿切分方向），每级每方向 3 个参数 → 参数量可忽略，契合"轻量"卖点
- **初始化直接取 Haar 的特例**：`P_taps = [0, 1, 0]`（恒等预测）、`U_taps = [0, 0.5, 0]`。这样第 0 个 epoch 时网络等价于固定 Haar 小波，训练从这里平滑地自适应——初始化有原理支撑，不是随手设的
- 边界处理：现有 `mgf_net.py` 已把输入 pad 到 4 的倍数，偶奇切分无边界问题，**这部分无需改动**

### 4.2 为什么选它（一箭三雕）

| 作用 | 说明 |
|---|---|
| **修的是真 bug** | 从结构上根治缺陷 (a)，不再依赖"参数别漂太远" |
| **构成贡献而非"修 bug"** | AdaSFFuse (TMM26)、WaveFusion (TCSVT25) 都是无约束可学习滤波器，**没有完美重构保证** |
| **自带核心消融表** | 固定 Haar ／ 无约束可学习（现状）／ PR-LWT（新）三行一列，PSNR 与 SSIM 的对比就是论文最有说服力的一张表 |

### 4.3 诚实定位（必须这么写，否则会被挑）

lifting scheme 本身是小波社区 1990 年代的成熟工具，深度学习领域也有人用过。**论文中不能宣称"提出了新的小波变换"**。

正确的说法是：

> 我们把可学习小波的可逆性从"训练中希望它成立"变成"结构上保证成立"，并证明这一约束是医学融合保真度的关键因素。

### 4.4 验收标准

**单元测试**：`idwt(dwt(x))` 与 `x` 的 PSNR 应 > 60dB（理论上应为无穷大，仅受浮点精度限制）。

⚠️ 注意是**在 DWT/IDWT 单元层面测**，不是整个网络层面。整个网络的输出还要经过融合块和边缘精炼，本来就（且应该）不等于输入。

对照现状：现有实现在同一测试下因滤波器漂移，重构误差显著。

### 4.5 风险与退路

这是全案**唯一存在"技术路线可能走不通"风险**的部分，故安排为第一个任务。

- **退路**：若 lifting scheme 在现有 7 子带架构上集成不畅，退而给现有 `MultiLevelDWT`/`MultiLevelIDWT` **绑定参数（共享 + 翻转）并加完美重构正则项** `‖idwt(dwt(x)) − x‖`。效果弱一些，但足以支撑故事

---

## 五、实验计划

### Phase A —— 地基（第 1–2 周，09/12 – 09/26）

| # | 任务 | 产出 | 依赖 |
|---|---|---|---|
| A1 | 下载完整 Harvard/AANLIB 数据集，目标 ≥100 对 CT-MRI（PET-MRI、SPECT-MRI 子集另存）。可从 ECFusion / U2Fusion / SeAFusion 官方仓库的数据链接入手 | `data/full/` + **固定 80/20 划分的 ID 列表文件**（要写进论文附录，审稿人必查） | — |
| A2 | 接入 **VIF-Benchmark**（Linfeng-Tang 官方脚本），跑通 EN/SD/SF/MI/VIF/Qabf/SSIM/SCD（+CC/PSNR）。**不要自己实现指标**——指标算错是这类投稿最常见的硬伤 | `eval/` 目录，一条命令出完整指标表 | A1 测试集 |
| A3 | 6 个真实基线推理 + 算指标：U2Fusion、SwinFusion、CDDFuse、SeAFusion、MEF-Net（或 IFCNN）、EMFusion。**全部只需推理，用官方预训练权重**。`Average`/`WeightedAvg`/`LaplacianPyr` 降级为附录 sanity check | `results/baselines.csv` | A1, A2 |
| A4 | 修 `test.py` 计时逻辑（现把模型加载算进第一张图：`test_results.txt` 中 6.png 显示 0.010s 而总体平均 0.500s）；补测参数量 / FLOPs | 可信的速度与复杂度表 | — |

> **A4 不依赖任何数据，可立即执行。** A1 下载耗时较长时与 A4 并行。

### Phase B —— 让方法真的赢（第 3–4 周，09/27 – 10/10）

| # | 任务 | 预期效果 |
|---|---|---|
| B1 | **换损失函数**：借鉴 WaveFusion 的显著性引导损失 + CDDFuse 的 base/detail 分项损失，替换 `losses.py` 中推向平均的 L1 + balance 组合 | SSIM_MRI 0.46 → 0.65+，SSIM_Avg 甩开 Average(0.7737) |
| B2 | **实现 PR-LWT**（见第四节） | PSNR 14dB → 正常区间 |
| B3 | **修 `CrossBandAttention`**：改为残差形式 `1 + tanh(...)`，允许增强；复查 `edge_refine.py` 末尾 clamp | 恢复动态范围 |
| B4 | **消融表**：逐个去掉 PR 约束 / CMGF / EAR / CrossBandAttn / 各损失项 | 论文核心表；同时判断哪些模块该砍 |

> **B4 很可能给出意外结果。** 参照 v1（23K 参数）→ v2（65K 参数）参数量涨 3 倍，但 `method_comparison.txt` 显示相对 EMFusion 的提升微乎其微。推测 `CrossBandAttention` 与 `EAR` 可能并未贡献——若果真如此，**砍掉它们退回精简版，论文反而更好写**。

### Phase C —— 写作与投稿（第 5–6 周，10/11 – 10/24）

1. **标题定位**（不要吹"新范式"，要具体）
   - 英文：*Reconstruction-Constrained Learnable Wavelet Gating for Lightweight Medical Image Fusion*
   - 中文：重构约束的可学习小波门控轻量医学图像融合
2. **主打三个数字**：参数 <100K ／ 推理 <0.05s ／ 消费级 GPU 可训练
   - ATDFusion (PR 2026) 证明"小数据 + 轻量"是一个成立的故事线
3. **实验章节骨架**：照 ECFusion (Sci Rep 2025)、ATDFusion、UniFuse (ICCV 2025) 三篇的结构写——数据划分、8 指标、6–8 基线、消融表、复杂度表、视觉对比图。**这个"抄"零成本，但决定能不能过审**
4. **Limitation 主动写**：数据集规模、单一模态对、未做下游任务验证。自己写比被审稿人挖出来好

---

## 六、目标期刊

| 优先级 | 期刊 | 理由 | 注意 |
|---|---|---|---|
| **首选** | **Scientific Reports** | ECFusion (2025) 同款 scope，验证过这个档次可中。审稿 3–5 月，不要求 SOTA | ⚠️ **APC 约 $2700–2900**，投稿前确认经费 |
| 备选 1 | **Biomedical Signal Processing and Control** (Elsevier) | 医学图像融合高频发文刊，审稿 2–4 月 | 通常也有 APC，低于 SR |
| 备选 2 | **The Visual Computer** (Springer) | 对新颖度要求最低，方法 + 实验完整即可 | 周期偏长（4–6 月） |
| 备选 3 | **Signal, Image and Video Processing** (Springer) | 保底选项 | 影响因子低 |
| 进阶 | **Neurocomputing** | SRGM (2025) 发于此，难度高一档 | 留作被拒后的升级目标 |

> ⚠️ 具体 IF 与 APC 请以投稿当日官网为准。**若无经费支付 APC，直接从备选 1/2 里选。**

---

## 七、可借鉴清单

筛选标准：**1 周内做得完 且 直接改善数字**。

### ✅ 建议借鉴

| 论文 | 可复用的东西 | 怎么用 | 成本 |
|---|---|---|---|
| **WaveFusion** (TCSVT25) | 显著性引导损失 | 替换把模型推向平均的 L1 项。解决 `SSIM_MRI` 仅 0.46 的最直接手段 | 1 天 |
| **CDDFuse** (CVPR23) | base/detail 分解 + 分项损失 | 低频子带保强度、高频子带保边缘，替代全局 L1 | 2 天 |
| **SeAFusion** (2022) | 梯度/残差损失 | 已有，但需让权重压过 L1 | 半天 |
| **ECFusion** (Sci Rep25) | **不是模块，是模板** | 边缘增强 + 跨尺度 Transformer 发在 Sci Rep，证明本工作 scope 够发。照其实验章节骨架写 | 0 |
| **ATDFusion** (PR26) | "医学小数据下轻量适配"叙事 | 8 对数据是劣势，讲成"极小样本下的轻量方案"就变成优势 | 0（写作） |
| **LUT-Fuse** (ICCV25) | 推理速度/参数量对比表 | 23K–65K 参数是真实优势，做成表格是硬卖点 | 半天 |
| **SIBA** (ICCV25) | 源图注意力可视化 | 把 gating 权重图可视化，作为定性证据 | 1 天 |

### ❌ 现阶段不做（留作第二篇的素材）

| 方向 | 代表论文 | 为什么不做 |
|---|---|---|
| Mamba | Shuffle-Mamba, FDPMambaFuse | 集成 + 重跑全部实验 ≥3 周；3050 4GB 训不动；**且做完大概率仍打不过 Average** |
| 扩散模型 | DreamFuse, DDFM, ReCoFuse | 4GB 显存跑不起来 |
| 任务闭环 | CLDyN, MRFS | 需 BraTS 数据集 + 分割标签，额外 1–2 月，够单独发一篇 |
| DPO / 奖励模型 | DPOFusion, EVAFusion | 需人类偏好标注数据 |
| 退化鲁棒 | UniFuse, DMFusion | 好方向，但是**第二篇**的内容 |
| 缺失模态 | DCMIF | 另一个故事，单独发 |

---

## 八、待查清事项

| # | 事项 | 处理 |
|---|---|---|
| 1 | `outputs/figures/method_comparison.txt` 中的 `EMFusion 0.7449` 来源不明 —— EMFusion 原码为 TF1.x，`工作进展报告_MGF-Net.md` §6.2 也写了"直接对比需额外适配" | **解释不了就必须删掉该行**，绝不能带进论文（学术诚信问题） |
| 2 | `工作进展报告_MGF-Net.md`（0.7483）与 `outputs/test_results.txt`（0.6886）数字矛盾 | Phase A 完成后统一口径重算，旧数字全部作废 |
| 3 | 数据集下载源尚未确定 | 从 ECFusion / U2Fusion / SeAFusion 仓库的数据链接入手 |
| 4 | Scientific Reports 的 APC 经费 | 投稿前确认 |

---

## 九、任务清单与依赖

```
#1  PR-LWT ← 起点，风险最高          #2  修 test.py 计时（立即可做）
#3  查 EMFusion 数字来源              #4  下载数据集并固定划分
                                            ↓
                                     #5  接入 VIF-Benchmark 指标框架
                                            ↓
                          #6  跑 6 个基线 ／ #7  换损失函数
                                            ↓
                              #8  修 CrossBandAttention
                              #9  消融表 ← 依赖 #1 与 #7
                              #10 写作与投稿
```

---

## 十、踩坑提醒

1. **不要在 Phase A 完成前进入 Phase B。** 当前所有数字建立在 2 张测试图上，换完损失函数也无从判断好坏。
2. **不要为了"看起来更创新"而加模块。** 本计划的前提是创新点只有一个。加第二个就等于回到被废弃的顶刊路线。
3. **不要因为 4GB 显存砍基线。** 基线只需推理。
4. 论文中所有数字必须来自同一次、同一划分、同一评价脚本的运行。**跨文档抄数字是本项目已经犯过的错误**（见第八节 #2）。
