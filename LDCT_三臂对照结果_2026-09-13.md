# LDCT 三臂对照实验：PR-LWT / 无约束可学习 / 固定 Haar

> **日期**：2026-09-13
> **任务**：回答"**小波变换的完美重构约束是否带来收益**"
> **数据**：AAPM-Mayo 2016 LDCT 3mm B30 配对，10 患者
> **划分**：按患者 — train 7 人(1600 片) / val L291(343) / test L506+L067(435)
> **口径**：口径 A（`(HU+1024)/4096` → 评测 clip `[-160,240]`，data_range=400）
> **规模**：3 臂 × 3 种子 × 30 轮

---

## 一、结论

**无约束地学习小波变换，相比固定 Haar 毫无收益；加上完美重构约束后，学习才产生收益。**

| 臂 | n | TEST PSNR | TEST SSIM | 相对 identity |
|---|---|---|---|---|
| **pr**（PR-LWT） | 3 | **30.8506 ±0.027** | **0.8765 ±0.000** | **+2.988** |
| unconstrained（旧式可学习） | 3 | 30.5344 ±0.039 | 0.8726 ±0.000 | +2.671 |
| fixed（固定 Haar） | 3 | 30.5616 ±0.039 | 0.8728 ±0.000 | +2.699 |
| *identity（不处理）* | — | *27.8630* | — | *0（基准）* |

**两个关键对比**：

| 对比 | 差值 | 比值（差 / 种子标准差） | 判读 |
|---|---|---|---|
| pr − unconstrained | **+0.316 dB** | ≈ **8×** | 高度显著 |
| pr − fixed | **+0.289 dB** | ≈ **7×** | 显著 |
| unconstrained − fixed | +0.027 dB | ≈ **0.7×** | **无法区分** |

逐患者一致（两患者、三种子同向）：

| 患者 | pr | unconstrained | fixed |
|---|---|---|---|
| L506 | 31.88 / 31.91 / 31.96 | 31.62 / 31.63 / 31.71 | 31.65 / 31.65 / 31.72 |
| L067 | 29.82 / 29.84 / 29.87 | 29.45 / 29.46 / 29.53 | 29.53 / 29.45 / 29.57 |

---

## 二、机制：闭环误差被直接测到

三臂的**变换闭环相对 L2 误差**（训练后，各 50 步）：

| 臂 | 小波参数 | 训练后闭环 rel_L2 |
|---|---|---|
| pr | 24 | **9.3e-08** |
| fixed | 0 | 1.1e-07 |
| unconstrained | 64 | **9.9e-01** |

**解释**：

> 无约束学习会**摧毁小波自身的可逆性**（99% 信号丢失）。网络因此被迫
> 在做去噪的同时补偿变换损失——这个额外负担抵消了"学习"本应带来的好处，
> 使其退化到与固定 Haar 无异。
>
> PR 约束在**允许自适应**的同时**保住了可逆性**——这才是收益的来源。

这条机制不是推测：闭环误差是**确定性数值诊断**，可直接测量，不依赖统计推断。

---

## 三、为什么本结论比融合项目硬

| 维度 | 融合（MGF-Net） | 本项目（LDCT） |
|---|---|---|
| 真值 | ❌ 无 unique ground truth | ✅ 同患者实测全剂量 CT（数值张量） |
| 指标 | 源参考型，**已知退化**（MI 最优解是照抄单源） | PSNR/SSIM，**口径已与独立测量逐位核对** |
| 效应量 | 近零（0/30 达 MDES） | **8 倍种子标准差** |
| 机制 | 被下游 edge_refine 压缩 4–14 倍 | **闭环误差直接可测** |

融合项目中"PR 是否有效"始终说不清——因为指标本身退化，无法区分"PR 没用"
与"指标测不出"。本项目在**真值明确、口径统一、指标无歧义**的舞台上给出了明确答案。

---

## 四、与已发表方法的对比

**L506 单患者**（口径 A 的文献常用测试患者）：

| 方法 | 参数量 | PSNR | SSIM |
|---|---|---|---|
| identity | 0 | 29.2489 | 0.8759 |
| **本项目（PR-Wavelet）** | **2,159** | **31.864** | **0.903** |
| RED-CNN (TMI 2017) | ~1 M | ≈ 32.93 | — |
| CTformer (PMB 2023) | ~1.4 M | ≈ 32.9 | — |

> 我们用约 **1/500 的参数**，达到已发表强基线 1.1 dB 以内。
>
> ⚠️ **公平性说明**：已发表数字使用 **9 患者训练 / 1 患者测试**；
> 本项目为 **7 训练 / 1 验证 / 2 测试**，训练数据更少，该对比**对我们不利**。
> 若按同样划分重跑，差距可能更小。

---

## 五、复现

```bash
cd D:/图像相关论文/LDCT
PY=D:/DeveloperTools/miniconda/envs/mgfnet/python.exe

# 数据（需先下载并解压 AAPM-Mayo，见 data/download.sh 与 README）
$PY data/prepare_aapm.py

# identity 地板
$PY experiments/train.py --baseline-only

# 三臂
for w in pr unconstrained fixed; do
  $PY experiments/train.py --tag w3_${w}_s0 --wavelet $w \
     --epochs 30 --patch 128 --batch-size 8 --seed 0
done
```

每次运行的完整配置（含数据量、参数构成、LR 衰减点、代码版本）在
`experiments/runs/<tag>/config.json`；逐样本指标在 `results.json` 的
`test_per_sample` 与 `test_per_patient`。

---

## 六、诚实边界

1. **种子数 n=3**。效应量达 7–8 倍标准差，但正式发表宜补到 ≥5 种子。
2. **测试集 2 个患者**。跨样本比较的独立单元是**患者**（n=2），非切片（435）。
   按文献惯例应补一个 L506-only 的单患者划分以对齐已发表数字。
3. **未做显著性检验**。差值/标准差之比是粗略判据；正式应做配对检验
   （以种子为独立单元，或按患者聚类）。
4. **未与真实基线方法直接对比**。上表 RED-CNN / CTformer 的数字引自文献，
   非我们复现；口径虽已对齐，但训练划分不同。
5. **闭环误差的测量是各 50 步训练后**，非完整 30 轮训练后。完整训练的
   三臂闭环应从各自 checkpoint 复测。
6. **单数据集**。结论是否推广到 LoDoPaB / 其他 CT 数据未验证
   （LoDoPaB 已在本地下载，可作扩展）。
