# MGF-Net 发表路线与实验计划：评估及修订版

> 修订日期：2026-09-12  
> 对应原稿：`发表路线与实验计划_2026-09-12.md`；原稿保留，不覆盖。  
> 目标：形成可审查、可复现的医学 CT–MRI 图像融合论文，不承诺录用。  
> 范围：依据已读的计划、调研材料、MGF-Net 核心代码与结果文本，以及少量一手资料完成重点评估；按用户要求停止扩大检索。未重训模型、未复跑指标、未实测现有权重的重构误差；未完成全部相关论文的新颖性排查。

## 一、先说结论：方向能做，原计划不能直接照抄执行

**保留“先修实验基础，再验证一个小而清楚的研究问题”的路线；取消“加上 PR-LWT 就够发表”的预设。**

当前工作可以作为原型继续推进，但尚不足以支持投稿。主要缺口是：独立评价协议、与适配任务的强基线比较、可验证的方法贡献，而不只是“打赢 Average”。

三种结果应区别处理：

1. **修正实验和实现后，PR-LWT 相对固定 Haar、无约束学习及相关方法有稳定收益**：可以形成以可重构分析–合成为核心的轻量融合论文，进入投稿准备。
2. **质量与代表方法接近，但实测速度、总参数和显存明显更好**：可转为质量–效率权衡论文；要证明权衡，不要求所有质量指标第一。
3. **只比原来有问题的实现好，或只胜过 Average，不能证明优于合理实现/合理效率基线**：贡献仍不足。此时继续修基础或重选问题，不靠换标题、堆模块、换低档期刊解决。

**六周应定义为完成首轮实验及投稿决策的冲刺期，不是六周内发表。** 数据整理、复现或结果验证不顺时延长；录用时间不在研究者控制内。

## 二、原方案必须纠正的关键判断

| 原稿判断 | 本次评估与修改 |
|---|---|
| “阻碍发表的不是创新性不足” | 没有依据。实验可信度和创新性都未过关，必须分别验证。 |
| “SSIM_Avg 输给 Average，审稿人会立刻拒稿” | 过度推断。Average 对同时接近两源的指标天然有利；融合没有唯一真值，不能用单一源参考指标判生死。Average 是必要 sanity check，不是发表门槛的全部。 |
| “分别对两源计算 SSIM/PSNR 是自创口径” | 不准确。源参考指标可以作为辅助，但必须定义公式与聚合方式，不能解释为融合真值保真度。真正明确的问题是当前 SSIM 实现采用整图均值/方差，而不是常用的局部窗口 SSIM。 |
| “0.7483 与 0.6886 互相矛盾” | **这两项并不矛盾。** 前者为训练 8 对 + 测试 2 对的混合平均；后者为 2 对测试平均。两文件的测试逐图数值一致。错在把不同集合的结果混用，不能把 0.7483 当测试成绩。 |
| “没有数据划分” | 当前代码有 train/test 路径；问题是只有 8/2 对、没有验证集与病例级隔离证据，且比较脚本混合汇总 train/test。不能断言训练脚本一定训练了测试图。 |
| “EMFusion 0.7449 来源不明” | `compare_baselines.py` 已包含 TF1 兼容推理、FNet 调用及 `3200.ckpt` 恢复路径。存在可追溯实现线索，不能据此指控造假；仍需记录版本、核对官方预处理和复跑。 |
| “L1 + balance 正是像素平均的定义” | 数学上不成立。双源等权 L1 的逐像素最优解是两值之间的整个区间；全图平均误差相等不要求每个像素为中点。它可能造成折中，但不是唯一原因的证明。 |
| “梯度损失权重 5 小于 L1 权重 10，所以应让前者压过后者” | 系数不能跨不同量纲直接比较。应看损失值及对参数的梯度贡献，再通过验证集小范围选权重；过强梯度项可能放大噪声/伪边缘。 |
| “滤波器漂移是 PSNR 约 14dB 的主因” | 独立可训练分析/合成滤波器确实没有结构性 PR 保证，但本次没有实测漂移。对不同模态算约 14dB 不等于重构链出错。先测单元重构，再归因。 |
| “sigmoid 注意力和 clamp 必然整体压低动态范围” | sigmoid 对该处系数只衰减属实；但网络其他层可以补偿。clamp 截断越界值，不等于所有值都降低。先看中间输出分布、饱和比例和消融，再修改。 |
| “医学融合里没有带 PR 保证的可学习小波” | **尚未证实，删除首次性断言。** 提升小波早已用于医学融合；这不直接否定“可学习+特定任务”的差异，但足以说明不能把提升格式本身当原创。 |
| “6 个基线均只需官方权重，4GB 一定够” | 预训练域、权重可用性、依赖、输入尺寸及训练重叠都要核实。推理省显存不等于任意模型都能在 4GB 跑；跨域权重也不等于公平的医学对照。 |
| “8 对数据可包装成小样本优势” | 不可以。小样本贡献要有按病例数变化的学习曲线、同预算对照及独立测试；重复采样不增加独立样本量。 |
| “某刊同方向发过，所以本工作够发；某刊保底” | 均删除。方向匹配不等于达到贡献和证据要求，也没有可保证录用的期刊。 |

### 原计划遗漏的一个更直接的问题：CT 偏置门控

`models/gated_fusion.py:52–59` 实际为：

```python
weight = self.weight_net(concat) * 0.4
fused = ct + weight * (mri - ct)
fused = fused + self.refine(fused)
```

其中 `weight_net` 末层是 tanh，所以直接融合路径的 MRI 系数在 **[-0.4, 0.4]**，CT 系数在 **[0.6, 1.4]**，不是对称的双模态选择。忽略后续 refinement 时，甚至无法取到 MRI，也无法实现等权平均；负权重还会朝远离 MRI 的方向外推。

**这是明确的结构性偏置，应在 PR-LWT 前做低成本对照。** 它可能影响 MRI 信息保持，但仍须实验确认；后续 refinement 有补偿能力，不能声称整个网络完全不能输出 MRI。

建议先比较：原门控 vs `w=sigmoid(g(...)); fused=(1-w)*CT+w*MRI`，保持其余设置一致。不要把这一常规修正包装为新方法贡献。

## 三、修订后的研究问题与方法边界

### 3.1 唯一主问题

> 在轻量医学融合中，相比固定 Haar 和无约束学习，结构可逆的可学习分析–合成变换，能否改善融合质量、数值稳定性或质量–效率权衡？

这是**待验证假设**，不是已经成立的结论。不要预写“PSNR 14 → 正常”“SSIM_MRI 必到 0.65”“证明 PR 是关键因素”。

合理的贡献应由以下证据形成：

- 明确的结构设计和可逆条件；
- 固定其余网络与训练协议的受控比较；
- 独立数据上的融合质量/效率收益和失败案例。

既有 WaveFusion、AdaSFFuse 的相关机制必须在相关工作中讨论，不能仅凭名称或摘要断言其“完全没有重构约束”。已查看的 AdaSFFuse v1 正文未给出显式 PR 条件，但这一结果不能替代对全部近邻工作的检索。

### 3.2 PR-LWT 可以试，但先修正技术细节

一维 lifting 的基本关系可保留：

```text
分析：d = x_o - P(x_e)；s = x_e + U(d)
合成：x_e = s - U(d)；x_o = d + P(x_e)
```

需要补充：

1. **共享同一组 P/U，保持相同边界规则与确定性计算。** 不在对应的正逆步骤之间引入随机 dropout、不一致归一化等状态变化。二维逆变换要按正变换的相反方向顺序执行。
2. **Haar 初始化缺少归一化说明。** `P=identity, U=0.5*identity` 得到 `d=x_o-x_e, s=(x_e+x_o)/2`；要匹配正交归一化 Haar，应输出 `LL=sqrt(2)*s, H=d/sqrt(2)`，逆变换先撤销缩放。二维每方向都要匹配，不能直接声称原稿初始化与当前 Haar 系数一致。
3. **参数量算清。** 每个方向的 P/U 各 3 taps，即 6 个参数；两级、每级两方向、跨模态共享、无 bias 且不共享级/方向时共 24 个，不是“每级每方向 3 个”。若增加通道或不共享，重新计算。
4. **pad 到 4 的倍数只解决切分长度。** 3-tap 卷积仍需明确端点延拓、短尺寸处理及裁剪；正逆一致不等于无需边界设计。
5. **可逆不等于良好小波，也不等于稳定。** 应查看滤波器频响、常量输入的高频响应和数值幅度。需要保持高频零直流时，可约束线性无偏置预测滤波器系数和为 1；其他小波性质不能自动获得。
6. **PR 仅保证未修改系数的分析–合成闭环。** 融合会改变系数，PR 不保证融合信息全保留，更不保证诊断价值。双源压成单图也不能据此称“无损融合”。
7. **退路应是固定 Haar，而不是“共享+翻转+正则就保证 PR”。** 一般转置卷积只是伴随而非逆；重构正则只约束样本上的误差，不能代替结构证明。

单元验收：随机图、常量图、脉冲图、奇偶尺寸；随机扰动参数与训练后参数都测；报告最大绝对误差和相对 L2 误差。对有界参数、[0,1] 输入、FP32 可先以 `1e-5` 量级为工程容差，再根据规模和边界实现解释；AMP 单列误差，不能只凭 PSNR>60dB 宣布理论验证。

相同双源输入时，整网是否近似保持输入也是有价值的诊断，不应预先说整网“应该不等于输入”；但它与变换单元 PR 是不同性质。

## 四、先建立可信的实验协议

### 4.1 数据：先确认独立性，再谈 100 对

- Harvard/AANLIB 是病例图谱资源，不应把不同论文整理的下载包当成统一、无歧义的官方划分。
- 建立清单：`pair_id、case/group_id、源URL、模态、尺寸、文件哈希、许可/引用、split`；保留原始图像，预处理另存。
- 核对同病例、对应切面、配准质量；两张同名图不自动意味着正确配对。
- **按病例/患者划分训练、验证、测试**，同病例邻近切片和增强版本不可跨集合。病例数足够时可约 70/15/15；比例不是硬标准。
- 病例太少则采用按病例分组交叉验证，明确每折内部的验证与选模。只有未知来源切片时，最多做探索性基准，不能声称验证了患者泛化；优先补溯源或独立病例集。
- 哈希排重不能检出全部近重复、裁剪或相邻切片；需结合来源和视觉检查。100 张同一病例切片不等于 100 个独立病例。
- 优先解决 CT–MRI 主任务；另一个可溯源来源或预先留出的病例组用于泛化验证。没有外部数据时如实收窄结论，不把同源随机切片称作外部验证。
- 预训练基线也需检查其训练数据是否覆盖拟用测试病例；无法排除时明确标记，不能当成严格未见样本公平比较。

### 4.2 指标：统一定义，不追求“八项全部涨”

- 采用成熟实现，固定版本；人工核对少量例子及常量/相同输入边界情况，不能认为用了别人代码就一定正确。
- 建议主要质量证据：Qabf、VIF、SCD，并结合局部窗口 SSIM、MI 和固定显示范围的视觉对照；EN、SD、SF 作为补充。
- EN/SD/SF 可以因噪声或过度锐化增加，不是越大越好的一票判据；PSNR 对源图只作辅助，不能称为融合真值重建精度。
- 明确灰度、输入范围、SSIM 窗口与边界、MI bins、VIF 双源聚合、是否裁剪、量化/保存流程、背景处理。测试图统一使用全图或预先定义 ROI，不能为某方法临时改变。
- 保存浮点输出与展示图；正式评分采用同一种表示，报告是否 clip/量化。不得给某方法单独 min–max 拉伸提高观感或分数。
- 展示每图结果及病例级聚合；病例多切片先在病例内平均。报告 3 个随机种子的变异，必要时提供病例配对差异的 bootstrap 区间；很小的病例数下不夸大统计显著性。
- 测试集只用于冻结方案后的最终评价。小改动、损失权重和 early stopping 由验证集决定，不能反复看测试表调参。

### 4.3 基线：按相关性与公平性选择，不按数量凑数

| 角色 | 建议 |
|---|---|
| 必留 sanity check | 直接输出 CT、直接输出 MRI、Average、可信的传统小波融合；可放附录，但需实际分析 |
| 经典/通用对照 | EMFusion、IFCNN/U2Fusion 中可可靠复现者 |
| 代表性深度对照 | SwinFusion、CDDFuse 中任务适配明确者 |
| 最接近工作 | WaveFusion、AdaSFFuse 或其他已核实的近年小波/轻量医学方法，至少认真比较一项 |

目标可以是约 5–7 个学习方法，但**没有“满 6 个就能发表”的规则**。少量真正相关、可核查的对照优于大量错域方法。

分开报告两种协议：

- **官方预训练迁移**：注明训练域、训练规模、权重来源和重叠风险；用于部署/迁移比较，不冒充同数据训练优劣。
- **统一训练/适配**：在相同训练/验证划分上，选择最接近且预算允许的 1–2 个模型作为关键受控比较，保留其合理训练策略并披露预算。不能要求所有方法机械使用相同损失或相同 epoch。

先用 batch=1 和实际输入尺寸试跑、测显存，再确定方案。必要时使用 CPU或临时借用计算资源，速度表按设备分开；分块推理要披露接缝、重叠和额外开销。SeAFusion 等跨域方法可作补充，不能靠它们凑医学核心基线；名称含混的 MEF-Net 先确认具体论文、任务和权重再纳入。

## 五、代码修正顺序：先排偏置，再试 PR

本次只修订计划，**以下代码任务尚未执行**。

| 优先级 | 文件/位置 | 应做的事 |
|---|---|---|
| P0 | `data/dataset.py:46–60`；`test.py:85–99` | 不再靠排序后 zip 配对；按显式 ID 配对，检测缺失、数量与尺寸不一致，并报错而不是静默截断。 |
| P0 | `compare_baselines.py:88–94, 199–220` | 测试表只从 test manifest 产生；train/val 可另表，禁止混合为论文总分。 |
| P0 | `test.py:26–40`；`compare_baselines.py:30–36` | 更换整图统计的 SSIM，统一为固定版本的局部窗口实现，并重算所有方法。 |
| P0 | `train.py:109–117, 204–217` | 增加验证流程；当前 best 是每 10 epoch 保存点中训练损失最小者，不是验证最佳。固定随机种子，保存完整配置、数据划分哈希和代码版本。 |
| P0 | `config.py:32`；`test.py:65,81–82` | 训练 learnable=True、推理 False 的标志不一致；参数载入 buffer 不会自动恢复 Haar。由 checkpoint 配置重建模型，并实测滤波器状态，不仅看布尔开关。 |
| P1 | `models/gated_fusion.py:52–59` | 对照 CT 偏置门控与中性 sigmoid 门控；先不加新模块。 |
| P1 | `models/dwt_layer.py` | 测固定 Haar、现有权重的闭环误差；多通道代码将输入 reshape 成单通道却设 groups=C，C>1 路径有形状问题，目前 C=1 不受此问题影响。 |
| P1 | `models/gated_fusion.py:63–87`；`edge_refine.py:98–99` | 分别试移除注意力、恒等初始化残差注意力及 EAR；统计 clip 饱和率。只保留有证据的项，不默认全部改动都有利。 |
| P1 | `test.py:116–125` | 模型加载已在计时外，原稿这一诊断不对。真正需补的是预热与 CUDA 同步/事件计时，CPU 另测。 |
| P2 | `models/mgf_net.py:83–84` | 当前只统计 requires_grad 参数。论文同时报告总参数、可训练参数、推理显存与 MACs/FLOPs 口径。 |

速度建议：固定输入 256×256、batch=1、dtype、设备和功耗条件；预热 50 次，计时约 200 次并同步，报告中位数/均值及波动。模型纯前向与含 I/O 的端到端延时分列。不要先承诺小于 0.05 秒。

## 六、最小消融矩阵

先完成中性门控和正确评估，冻结一个尽量简单的骨干、训练协议与损失。

| 组别 | 变换 | 其他模块/损失 | 回答的问题 |
|---|---|---|---|
| A | 固定归一化 Haar | 相同 | 合理固定小波是否已经足够？ |
| B | 原式无约束可学习分析–合成 | 相同 | 学习本身带来什么收益/不稳定？ |
| C | 结构 PR 的可学习 lifting | 相同 | PR 方案相对 A/B 是否值得？ |
| D（有余力） | 无约束学习 + 重构正则 | 相同 | 硬结构保证与软正则有何区别？ |

再进行独立控制实验：旧/中性门控、去掉 balance、EAR 开/关、注意力开/关。先单种子筛查，把最终 A/B/C 和主模型做至少 3 个种子；不必对所有模块组合做昂贵全排列。

损失修改一次只改变一类目标。可研究局部显著性加权的源强度项和边缘项，但需给出显著性定义、数值范围与引用；不可把 CDDFuse 的特征解耦目标不加辨别地套到现有小波子带。对 MRI 噪声和 CT 强边缘同时检查，避免“梯度越大越好”。

**公平性注意：A/B/C 不只是一个开关，它们也改变参数化。** 固定骨干、初始化尺度和调参预算，报告参数差异；只有 C>B 而 C≈A，说明可能只是避免原实现缺陷，不能证明可学习 PR 优于固定变换。

## 七、精简执行时间表与停止条件

按原计划每周约 20 小时，安排六周冲刺；日程以阶段验收优先，延误则顺延，必要时扩到 8–10 周。这是工作估算，不是发表承诺。

| 阶段 | 工作 | 验收与下一步 |
|---|---|---|
| 第 1 周：证据与数据 | 归档旧配置和结果；核对配对与病例来源；建立 train/val/test manifest；统一指标；复核 EMFusion 推理流程 | 能说明每个数字来自哪一划分、哪一权重、哪一脚本。病例独立性解决不了，暂停正式训练与投稿承诺。 |
| 第 2 周：可信轻量基线 | 固定 Haar + 中性门控；选模改用验证集；测旧变换闭环、偏置门控/注意力/clip | 先回答“之前的问题是不是偏置、指标或实现造成”。逐项修改，不把所有变化捆绑。 |
| 第 3 周：PR 可行性 | 实现并测试 lifting；跑 A/B/C 初步消融；完成最接近工作的定向核对 | 若 PR 不比固定 Haar 有稳定价值，停止以 PR 为唯一创新，不进入完整 PR 论文写作。 |
| 第 4–5 周：证据补齐 | 适配基线、关键同划分对照、3 种子、病例/外部泛化、视觉失败案例、复杂度与速度 | 检查质量或效率优势是否真实，且不是错域权重、测试泄漏或单种子偶然性。 |
| 第 6 周：整理与投稿决策 | 写方法、实验、局限；检查引用、许可证、伦理与数据声明；依据结果选刊 | 达到下面门槛再投稿；未达到就延长实验或更换研究问题，不按日历硬投。 |

### 第 3 周与投稿前的决策门槛

以下是本项目的内部质量门槛，不是期刊官方统一要求：

- 数据划分、代码、权重、评价与逐图结果可以追溯，没有已知训练/验证/测试混用。
- 关键结论不是仅由单个 SSIM 或单个种子支撑；没有为赢分牺牲明显的源信息或制造伪影。
- PR 路线需要对固定 Haar 的独立收益；若收益主要是效率，则以实测 Pareto 权衡表述，不声称全面质量领先。
- 有任务匹配的强对照，尤其是最接近的方法；局限披露不能代替必要的关键比较。
- 图示使用预先约定的病例抽样规则，并展示失败案例；门控图只能说明模型响应，不能证明医学正确性。
- 只在图像融合层面有证据，就只声称图像融合效果。没有分割、医生评价或临床研究时，不写“提高诊断准确率”“满足临床部署”。

## 八、投稿路线：根据证据选刊，不按“保底”选刊

| 期刊 | 修订建议 |
|---|---|
| Scientific Reports | 可作为候选，但不能因同类文章已刊出就推断本稿可录用；开放获取经费必须确认。本次官网费用页未取得可核验价格，不把原稿美元金额当已确认预算。 |
| Biomedical Signal Processing and Control | 与医学信号/图像处理方向相关，但官网强调方法质量和医学应用相关性，不是自然更容易。官网本次显示订阅发表不收出版费；可选 OA 的 APC 为 USD 3,470（不含税），原稿“通常 APC 更低”的说法应删除。 |
| The Visual Computer | 作为视觉计算方向候选，需要评估具体方法贡献与读者匹配；删除“新颖度要求最低”。当前费用和出版模式在本次读取中未核实，投稿前看官网。 |
| Signal, Image and Video Processing | 可保留为图像处理方向候选，删除“保底”。当前费用/模式未核实，不能先认定免费。 |
| Neurocomputing | 只有形成更强方法学贡献时再考虑；被拒后自动升级投稿没有逻辑，应依据拒稿理由重新判断。 |

BSPC 官网本次显示 submission-to-acceptance 203 天、decision-after-review 66 天：这些是不同阶段的历史统计，不是个人稿件承诺，更不能用“首次决定”代表录用时间。不要保留原稿的固定“2–4 月即可”等预期。

投稿前确认：单位认可目录/预警要求、经费和税费、作者贡献及署名、代码与图像许可、数据与伦理声明、AI 辅助写作披露要求；不能同时一稿多投。普通图谱实验可以讨论方法适用性，但不可伪造临床验证或伦理审批。

## 九、下一步只做这三件事

1. **冻结评价协议**：病例分组、验证选模、局部 SSIM、逐图记录，旧的 0.7483 不再当测试成绩。
2. **跑可信固定 Haar + 中性门控基线**：先排除 CT 偏置和实现因素，不急着换复杂损失。
3. **做固定 Haar / 无约束学习 / PR-LWT 三组受控试验**：第三周就决定这条研究问题是否值得继续。

一句话：**不是“按步骤做完就能发表”，而是“先用最少实验确认贡献，再用完整证据支持投稿”。这条修订路线比原计划更稳，但最终能否录用仍取决于实际结果、论文质量和审稿。**

## 十、本次使用的主要证据

### 本地材料

- 原计划；`前沿调研报告_图像融合2024-2026_复核补充版.md`；`图像融合汇总表_勘误与补全_2026-09-12.md`；`wavefusion_readme.md`。
- `MGF-Net/工作进展报告_MGF-Net.md`：历史原型描述，不能把旧版数值直接移作新版证据。
- `MGF-Net/models/{mgf_net,dwt_layer,gated_fusion,edge_refine}.py`、`losses.py`、`train.py`、`test.py`、`compare_baselines.py`、`config.py`、`data/dataset.py`。
- `MGF-Net/outputs/test_results.txt` 与 `outputs/figures/metrics_summary.txt`：相同两张测试图的 SSIM_Avg 分别为 0.7337、0.6435，均值 0.6886；混合十图平均为 0.7483。

### 在线一手依据（核对日期：2026-09-12）

1. AdaSFFuse v1 正文：https://arxiv.org/html/2508.15505v1 — 第 III-B 节分析/合成说明；第 IV-A 节列其整理的 CT–MRI 160 训练对、24 测试对。这是该论文实验划分，不能直接称官方统一划分，更不能当患者数。
2. 既有提升小波医学融合工作：*A Medical Image Fusion Algorithm Based on Lifting Wavelet Transform*，2010，https://doi.org/10.1109/AICI.2010.337 — 说明 lifting 用于医学融合不是新起点；不据此宣称精确相同的可学习方案已存在。
3. WaveFusion 官方仓库：https://github.com/fd-qhwang/WaveFusion — 存在小波实现和融合损失；本次并未完成全部数学约束核对，因此不保留“完全无重构约束”的断言。
4. Harvard 原始病例图谱：https://www.med.harvard.edu/aanlib/ — 以病例/序列组织；重打包数据需追溯来源和许可。
5. BSPC 官方页面：https://www.sciencedirect.com/journal/biomedical-signal-processing-and-control — 范围、订阅/OA 两种选项与本次可核实费用、时长统计。
6. 待投稿前确认页面：https://www.nature.com/srep/open-access 、https://link.springer.com/journal/371 、https://link.springer.com/journal/11760 。本次未取得可用的完整费用信息，不提供未经核验的现价。
