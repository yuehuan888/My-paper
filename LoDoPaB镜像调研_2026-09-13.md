All checks in. Here is the report.

---

# LoDoPaB-CT 下载加速：实测结论报告

## 一、结论

**存在一个比 Zenodo 更快的完整镜像：ModelScope 的 `OmniData/LoDoPaB-CT`。** 它是**逐字节一致**的完整副本（含全部 observation），本轮同窗口实测 **1.20 MB/s**，是 Zenodo 同窗口中位数 **0.74 MB/s** 的 **1.6 倍**，且稳定性远好于 Zenodo（5 次实测全部落在 1.15–1.25 MB/s，而 Zenodo 单次波动达 2 倍）。

全量 55.3 GB 预计耗时：**ModelScope 约 12.8 小时**，Zenodo 约 20.8 小时，**省约 8 小时**。

推荐做法：**以 ModelScope 为主源、Zenodo 为校验/兜底源**，全程保留代理（实测 ModelScope 走代理 1.20 MB/s 反而快于直连 1.02 MB/s，与"国内 CDN 该直连"的猜测相反）。

---

## 二、实测速度对比表

本轮实测环境：Windows 11，代理 `http://127.0.0.1:7890`，每段 20 MiB（20,971,520 B）HTTP Range 请求，吞吐 = 实收字节 ÷ curl 报告耗时。**下表所有数字均为本轮同一时间窗口内的原始实测值。**

| 源 | 文件 | 实测 B/s | 换算 MB/s | 稳定性 | 可行性 |
|---|---|---|---|---|---|
| **ModelScope（代理）** | observation_train | 1,149,775 | **1.150** | 5 次全部 1.15–1.25，σ 极小 | **推荐主源** |
| **ModelScope（代理）** | observation_train | 1,207,339 | **1.207** | | |
| **ModelScope（代理）** | observation_train | 1,248,999 | **1.249** | | |
| **ModelScope（代理）** | ground_truth_train | 1,171,533 | **1.172** | | |
| **ModelScope（代理）** | observation_test | 1,239,295 | **1.239** | | |
| ModelScope（直连，不走代理） | observation_train | 1,092,347 | 1.092 | 2 次 | 反而更慢 |
| ModelScope（直连，不走代理） | observation_train | 945,520 | 0.946 | | |
| Zenodo（代理） | observation_train | 723,980 | **0.724** | 4 次 0.71–1.40 | 唯一权威源 |
| Zenodo（代理） | observation_train | 751,782 | **0.752** | 波动近 2 倍 | |
| Zenodo（代理） | ground_truth_train | 709,558 | **0.710** | | |
| Zenodo（代理） | ground_truth_train | 1,403,244 | **1.403** | 异常高值 | |
| AAPM Box 3mm B30（代理） | full_3mm.zip | 1,275,665 | **1.276** | 2 次 0.93–1.28 | 替代数据集 |
| AAPM Box 3mm B30（代理） | full_3mm.zip | 929,135 | **0.929** | | |

**关键对照（同窗口）**：ModelScope 中位 **1.203 MB/s** vs Zenodo 中位 **0.738 MB/s**。倍数 = **1.63×**。

### 完整性验证（这是推荐它的前提）

1. **Content-Length 完全一致**：ModelScope `observation_train.zip` 报 `Content-Length: 29938738627`，与 Zenodo 官方字节数**逐位相同**。
2. **同偏移 md5 一致**：在偏移 1,000,000,000 处各抓 1 MiB，两者 md5 均为 `3e7da5afe7ffa9106635cfb875a7a4cb`。
3. **支持断点续传**：在偏移 5G / 15G / 20G / 25G / 29G 五次请求全部返回 **206** 且恰好 1,048,576 B，`Accept-Ranges: bytes`。
4. **HTTP 400 偶发**：同一偏移有一次返回 `code=400 size=237`，重试即成功——说明需要重试逻辑，不能裸跑。

结论：ModelScope 是**真镜像，不是子集、不是派生**。

---

## 三、推荐方案

### 下载命令

关键点：CDN 直链的 `auth_key` 只有分钟级有效期，**必须走 `/repo` 端点现取现用**，不要缓存 302 之后的直链。

```bash
export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890
BASE='https://modelscope.cn/api/v1/datasets/OmniData/LoDoPaB-CT/repo?Revision=master&FilePath='

for f in observation_train ground_truth_train \
         observation_validation ground_truth_validation \
         observation_test ground_truth_test; do
  until [ "$(stat -c %s ${f}.zip 2>/dev/null || echo 0)" -ge 1 ]; do
    curl -L -C - --max-time 7200 --retry 30 --retry-delay 5 --retry-all-errors \
         -o "${f}.zip" "${BASE}raw%2F${f}.zip"
  done
done
```

把 `-C -` 放在**外层 shell 循环**里而不是单条长 curl，是因为分钟级过期的签名 URL 会在长连接中途断掉；每次重新调用会重新解析 `/repo` 拿到新 key 并从上一次断点续传。

### 预计耗时（按实测 1.203 MB/s）

| 文件 | 字节数 | 预计耗时 |
|---|---|---|
| observation_train.zip | 29,938,738,627 | **6.9 h** |
| ground_truth_train.zip | 15,940,074,239 | **3.7 h** |
| observation_validation.zip | 2,944,573,582 | 41 min |
| observation_test.zip | 2,996,574,366 | 41 min |
| ground_truth_test.zip | 1,582,139,537 | 22 min |
| ground_truth_validation.zip | 1,567,086,812 | 22 min |
| **合计** | **55,316,349,278** | **约 12.8 h** |

对比 Zenodo 同一批文件按 0.738 MB/s 中位需 **20.8 h**。

### 如果 ModelScope 也慢下来

Zenodo 兜底（已验证 `?download=1` 会失效，必须用 `/api/...content`）：

```bash
curl -L -C - --retry 30 --retry-delay 5 --retry-all-errors \
  -o observation_train.zip \
  "https://zenodo.org/api/records/3384092/files/observation_train.zip/content"
```

**不要做的三件事**（均有实测依据）：
- **不要加大并行度**。16 路并行 Zenodo 仅 2.09 MB/s，4 路反而更差；瓶颈在代理路由不在段数。
- **不要直连 Zenodo**。上轮实测直连仅 0.145 MB/s，比走代理慢约 7 倍。代理必须留着。
- **不要把段切碎**。每条新连接要付 TLS+TTFB ≈ 4.2 s（Zenodo 1.738 s + 2.492 s），16 MiB 段按 0.8 MB/s 传输 20 s 就要付 12% 建连开销。建议 **32–64 MiB/段**。

**一个未验证的提速猜想**：ModelScope（1.20）+ Zenodo（0.74）属不同主机，按主机路由的限制下理论可叠加到约 1.9 MB/s，接近本机代理观测到的约 2 MB/s 天花板。**但我没有实测双源并行**，是否真能叠加不确定，建议先用两个小文件试一次再决定。

---

## 四、替代数据集（若 LoDoPaB 不可行）

**首选：AAPM-Mayo 2016 LDCT Grand Challenge（AAPM 官方 Box 公开目录）**

- URL：`https://aapm.app.box.com/s/eaw4jddb53keg1bptavvvd1sf4x3pe9h`，**免登录、免协议、支持 Range**
- 规模：3mm B30 配对 = full_3mm.zip (715,555,504 B) + quarter_3mm.zip (766,762,517 B) = **1.482 GB**
- 真值：2398 对严格 1:1 的 (quarter-dose, full-dose) 512×512 切片，已由中央目录逐患者核对，非靠文件名推测
- 许可：2016 挑战赛研究用途条款（**非 CC-BY**）
- **本轮实测：1.276 / 0.929 MB/s**（另有上轮 0.344–1.46 MB/s 的更宽区间）
- 预计耗时：1.482 GB ÷ 1.10 MB/s ≈ **22 分钟**（慢时可到 1.2 h）
- 注意：512×512，需 resize 或中心裁剪到 362×362

直链格式：`https://aapm.app.box.com/index.php?rm=box_download_shared_file&shared_name=eaw4jddb53keg1bptavvvd1sf4x3pe9h&file_id=<ID>`，3mm B30 的 ID 为 `f_856956352254` / `f_856961526983`。另有 1mm B30 配对 3.894 GB、1mm D45 配对 4.258 GB。

**次选：Zenodo 13362750（腹部 full/25% 剂量真配对 + 胰腺分割）**

- 5.795 GB，**CC-BY-4.0**（许可最干净），41 例，含两位医师双剂量分割
- 上轮实测 0.650 MB/s → 约 **2.5 h**
- 缺点：腹部胰腺分割导向，非通用去噪基准

**再次：SwitchDrive GLIMPSE `LoDoPaB-CT.npz`（仅真值）**

- 19,292,397,474 B（17.97 GiB），比 Zenodo 全量小 2.85 倍
- 已核实含完整 42,895 个 362×362 float32 真值切片，train 35,820 / val 3,522 / test 3,553，划分与官方一致
- 许可 ODC-By，支持 Range
- 上轮实测中位 1.06 MB/s（8 轮 0.15–1.65），约 **4.8–12 h**
- **硬限制**：**不含任何 observation**。泊松噪声是随机的，官方观测无法逐位重生成，**不能用于复现已发表结果**；只有做自监督或自行 ODL 仿真低剂量对时才够用

**已排除（不必再试）**：TCIA LDCT-and-Projection-data（需人工签署 Restricted License，>27 GB）；Kaggle `andrewmvd/ct-low-dose-reconstruction`（19.69 GB + 登录墙）；HF `HajihajihaJimmy/aapm16_radon`（无 README、许可不清、且经抽样确认**包内无干净真值档**）；Zenodo 15024205 ConvU_NExT（训练集 normal dose、测试集才 LDCT，剂量不配对，用不了）。

---

## 五、诚实的不确定性

**没测到的：**
- **ModelScope 全量没下完**。完整性验证基于 1 个偏移的 md5 + 全文件 Content-Length + 5 个偏移的 206 探测，不是端到端校验。理论上仍可能是"前 30 GB 对、尾部错"，但结合 Content-Length 逐位相同，这个风险很低。
- **双源并行（ModelScope + Zenodo 同时跑）未实测**，叠加能否接近 2 MB/s 纯属推断。
- **openxlab / OpenDataLab 未测**。记录显示 106.8 GiB / 672 文件、托管在国内阿里云、直连延迟 <1 s，**本方向最有潜力的未测源**，但需免费注册拿 AK/SK，脚本无法自动化。其 114.7 GB 体积构成也未核实（可能是解压冗余）。
- **Kaggle 8 个变体全部未测**。下载端点对匿名请求一律 404（连完全公开的 iris 也是），必须先在账号页生成 API token。其中 `sadmanhafij/lodopab-dataset` 的文件名与你本地已有的 `ground_truth_test_000.hdf5` 完全一致，兼容现有 loader，值得拿到 token 后单独测。
- **hyper.ai BT 种子我本人未测**。上轮子代理用自写最小 BT 客户端经代理隧道实测 1.04–1.69 MB/s（3 次，SHA1 逐片校验通过），但**只有 1 个 100% 做种 peer**，该 peer 下线即断源。与 Zenodo 无实质速度优势，仅作独立备份。

**结论不牢的地方：**
- **本轮数字带竞争噪声**。测速期间可能有其他进程共用 7890 代理。Zenodo 那个 1.403 MB/s 的异常高值就是波动证据，我按中位而非均值做对比。
- **Box 只测了 2 次**，1.276 / 0.929 的区间代表性不足，上轮观测到过 0.344 MB/s 的谷底，实际耗时应按 22 min – 1.2 h 宽区间估计。
- **ModelScope "走代理比直连快"是在当前时点成立**。两者差距（1.20 vs 1.02）不大且各有 5 次/2 次样本，方向可能随时段翻转。建议开跑前两个都试一次。
- **上轮报告过一个矛盾**：有子代理称在密集并行下载约 40 分钟后代理的 HTTPS CONNECT 隧道完全失效（curl 全返回 exit 35，冷却与重试均无效），但另两个子代理在本轮全程正常。若遇到，**降低并发、单流串行**即可，不要加压。