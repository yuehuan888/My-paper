# MGF-Net 诊断阶段记录

> **日期**：2026-09-13
> **上游**：`MGF-Net_实验记录_2026-09-12.md`（审计 / 门控 / PR-LWT / 数据泄漏）
> **本文件**：保真度差距诊断 + γ 扫描 + 交叉验证基础设施
> **状态**：γ 扫描进行中

---

## 〇、阶段目标

上一阶段（09-12）的对抗分析留下一个**比 PR-LWT 大得多的问题**：

> 在 MI / CC / PSNR / VIF 上，**零参数的像素平均 `0.5·(CT+MRI)` 击败了训练模型**，
> 且差距（MI 差 30%）远大于模型之间的差距（最大 2.8%）。

本阶段的任务是**查清这个差距从哪来**。不查清它，无论论文以什么为核心都会被一击致命。

---

## 一、平凡基线的实测值（对照下界）

**脚本**：`experiments/evaluate_trivial_baselines.py`
**产出**：`experiments/baselines_test.json`

test 集 40 对（病例 37、41）：

| 指标 | Average | CT_only | MRI_only | 网络（base） |
|---|---|---|---|---|
| EN | 3.8027 | 2.4977 | 3.2943 | 4.0356 |
| SD | 64.0400 | 91.5817 | 55.5163 | 73.4189 |
| SF | 18.1149 | 31.9985 | 17.7701 | 32.5377 |
| **MI** | **3.4135** | 3.2977 | **4.0943** | 2.3999 |
| **CC** | **0.8505** | 0.7426 | 0.7426 | 0.8269 |
| **PSNR** | **15.6072** | inf | inf | 15.0325 |
| SSIM | 0.7500 | 0.7529 | 0.7529 | 0.7536 |
| **VIF** | 0.2853 | **0.5124** | **0.5150** | 0.2196 |
| SCD | 0.9157 | 0.7958 | 0.1201 | 1.0438 |
| **Qabf** | 0.3767 | 0.5423 | 0.5250 | **0.6139** |

### 指标分两类（解读的关键）

| 类别 | 指标 | 含义 |
|---|---|---|
| **无参考型** | EN / SD / SF | 只看**输出自身**的锐度与信息量，完全不参考源图 |
| **参考型** | MI / CC / PSNR / SSIM / VIF / SCD / Qabf | 衡量与源图的关系 |

**模型赢的全是无参考型（EN/SD/SF/Qabf），输的全是参考型（MI/CC/PSNR/VIF）。**
即：网络在输出一张又锐又高对比的图，但它偏离了两个源。

### 解读必须注意

1. **CT_only / MRI_only 是退化的**（直接复制单一模态，不是融合）。且源参考型指标
   无唯一真值——**CT_only 的 VIF 达 0.512**，比训练模型高一倍多，
   说明该指标可靠"贴近单一源"刷高。
2. **故应以 Average 为主要对照**（它至少是真融合）。
3. 但**这一现象本身值得记录**：融合领域通用的这套源参考型指标，
   存在可用退化手段刷高的结构性缺陷。

---

## 二、诊断实验（4 条件 × 2 种子）

**假设 1**：`edge_refine` 过度锐化，以源保真度换锐度
**假设 2**：loss 中的 balance 项 `|L1(F,CT) − L1(F,MRI)|` 把模型推向平均

### 相对 Average 的差值（正 = 优于平凡平均）

| 指标 | base | no_er（旁路edge_refine） | **no_grad（γ=0）** | no_bal（δ=0） |
|---|---|---|---|---|
| EN | +0.233 | +0.276 | +0.140 | +0.291 |
| SD | +9.38 | +9.23 | +3.07 | +14.11 |
| SF | +14.42 | +14.99 | +4.62 | +15.44 |
| MI | −1.014 | −1.021 | **−0.926** | −0.934 |
| CC | −0.0236 | −0.0254 | **−0.0054** | −0.0362 |
| PSNR | −0.575 | −0.549 | **−0.084** | −0.482 |
| SSIM | +0.0036 | +0.0043 | **+0.0082** | +0.0046 |
| VIF | −0.0657 | −0.0684 | **−0.0456** | −0.0550 |
| SCD | +0.128 | +0.114 | +0.052 | +0.160 |
| Qabf | +0.237 | +0.232 | +0.088 | **+0.246** |

### 结论

**假设 1：推翻。** 旁路 `edge_refine` 后**全部十项指标无变化**
（MI 2.3999→2.3920、Qabf 0.6139→0.6088，均在跨种子噪声内）。
它对最终指标的影响为零。

> **但由此得到一个可直接变现的结果**：该模块占 **20,000 参数（21%）**。
> 删除后参数量 **94,234 → 74,361**，指标不变。
> 已改为默认关闭（`--edge-refine` 才启用，保留供消融表用）。

**假设 2：证实。** 梯度损失

```python
L_grad = |∇F − max(∇CT, ∇MRI)|          # 权重 γ = 5
```

要求融合图的梯度**逐像素等于两个源的最大值**——即"处处取最强边"。
这在数学上不可能同时忠实于两个源。去掉后：

- PSNR 差距 −0.575 → **−0.084**（几乎抹平）
- CC 差距 −0.0236 → **−0.0054**（几乎追平）
- VIF 改善 31%，MI 改善 9%，SSIM **反超平均**
- 代价：Qabf +0.237 → +0.088，SF +14.4 → +4.6（**但仍优于 Average**）

**关于 balance 项的二次更正。** 我先是指出"它把模型推向平均"，被修订版批评后
我改口说"它在缓解 MI 差距、去掉会更差"——**两次都错了**。实测：

```
MI:  base −1.014  →  no_bal −0.934     （去掉反而更好）
Qabf/SD/SF: no_bal 均为最好一档
```

该项既没帮忙也没明显有害，但**不是**它造成 MI 差距。

### 我在本阶段犯的方法论错误（记录备查）

| 次数 | 我的推断 | 实际 |
|---|---|---|
| 1 | balance 项把模型推向平均（09-12） | 方向对但量级判断错 |
| 2 | balance 项在缓解 MI 差距，去掉会更差（09-13） | **方向错了** |
| 3 | edge_refine 造成保真度差距 | **推翻**，它影响为零 |
| 4 | LH/HL 命名已与旧代码对齐 | **错**，实为互换（测试用同一误解写参照故未发现） |

**共同的失败模式**：把"我构造的推理"当成"事实"，未做对照或未核对代码。
本阶段的应对是每一步都加**受控对照**与**独立复核**。

---

## 三、γ 扫描（进行中）

**目的**：画出锐度–保真权衡曲线，判断是否存在"支配点"
（即某个 γ 的 Qabf 与 MI **同时**优于另一个 γ）。

**配置**：γ ∈ {0, 1, 2, 5, 10} × 2 种子，`edge_refine` 已默认关闭。

**判读标准**：

| 情形 | 含义 | 后续 |
|---|---|---|
| 曲线上存在支配点 | 有免费午餐，选该点 | 用该配置跑交叉验证 |
| 曲线严格单调（此消彼长） | 同一自由度的两端，**说明模型表达能力不足**——只能在"像平均"与"锐但失真"之间滑动 | **需改架构**，调参无用 |

---

## 四、交叉验证基础设施（已就绪）

**问题**：单次划分只剩 2 个测试病例。对抗分析实测：n=3 种子下
**MDES 中位数 2.58%，而最大观测效应仅 2.77%**，30 个格子里 0 个达到自身 MDES。
且逐图 p 值是**伪重复**（20 张相邻脑切片来自同一病例）。

**方案**：按病例 5 折交叉验证。

| 折 | 测试病例 | 训练 | 验证 |
|---|---|---|---|
| 0 | [2, 32] | 7 病例 ~129 对 | 病例 16 |
| 1 | [16, 33] | 7 病例 ~130 对 | 病例 2 |
| 2 | [20, 34] | 7 病例 ~126 对 | 病例 2 |
| 3 | [21, 37] | 7 病例 ~128 对 | 病例 2 |
| 4 | [28, 41] | 7 病例 ~129 对 | 病例 2 |

**独立样本数：2 → 10**。聚合口径为**先在病例内对切片取平均，再跨病例统计**。

| 文件 | 作用 |
|---|---|
| `data/make_cv_splits.py` | 按病例生成 K 折（确定性、无随机） |
| `splits/ct_mri_cv5.json` | 5 折划分 |
| `experiments/run_cv.py` | 逐折运行 + 按病例聚合 + 多配置对比 |
| `run_experiment.py --fold N` | 单折运行 |

⚠️ 跨折的独立单元是**病例**（n=10）。n 仍偏小，t 值仅供参考。

---

## 五、当前模型配置（经本阶段清理）

```
CT, MRI
  ↓  小波分解（PR-LWT 提升格式，2 级 7 子带，24 参数，闭环 rel_L2 ≈ 1e-7）
  ↓  逐子带门控融合（中性 sigmoid 门控，w ∈ (0,1)，7 × 10.6K 参数）
  ↓  跨频段协调（SE 式注意力）
  ↓  逆变换（与正变换共享 P/U）
融合图

参数量 74,361（删除 edge_refine 后）
```

三处改动均有实测支撑：

| 改动 | 原状 | 依据 |
|---|---|---|
| 门控 `tanh(g)·0.4` → `sigmoid(g)` | 99.13% 像素贴 +0.4 上限，门控退化为常数 | 机制验证 + 10 项赢 9 项 |
| 分离式可学习小波 → PR-LWT | 闭环丢 61% 信号 | 单元验证（**但不改善融合质量**） |
| 删除 `edge_refine` | 占 21% 参数 | 旁路后指标无变化 |

---

## 六、未解决 / 下一步

| 优先级 | 事项 | 说明 |
|---|---|---|
| 高 | γ 扫描结果判读 | 见 §三 判据 |
| 高 | 用选定配置跑 5 折交叉验证 | 得到 10 个病例级独立测量 |
| 高 | 训练是否收敛 | 50 epoch 是 100 轮 schedule 的截断，milestone 70/90 从未触发 |
| 中 | 重设计梯度损失 | 用"不超过源的最大梯度"而非"等于" |
| 中 | 基线对比 | 从 MIF 池选 MMDRFuse / GeSeNet / CDDFuse |
| 低 | PET-MRI / SPECT-MRI 扩展 | 按 `build_manifest.py` 同法 |

### 诚实边界

**所有数字目前都基于 2 个测试病例、40 张图，以及 2 个种子。**
γ 扫描能给出**趋势方向**，但**任何具体数值都不足以写进论文**。
定论需要 5 折交叉验证 + 更多种子。
