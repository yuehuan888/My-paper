# ============================================================
#  可逆小波域低剂量 CT 去噪（PR-LWT）—— 版本控制排除规则
#
#  原则：只跟踪「源代码 + 文档 + 配置 + 实验结果」；
#        数据集、第三方材料、可重新生成的产物一律排除。
# ============================================================

# ---------- 数据集（体积大，且都是公开可下载的） ----------
# LDCT/data 共约 5 GB（aapm/ aapm_h5/ raw_aapm/），获取方式见 LDCT/data/download.sh
#
# 教训（2026-09-14）：上一版只排除了 raw/ 与 extracted/，而 aapm/、aapm_h5/、
# raw_aapm/ **仍会被提交** —— 实测 4769 个文件，含 1.68 GB 的 .h5。
# 而且 `*.hdf5` 这个模式**并不匹配** `.h5`。故改为排除整个 data/ 再逐个放回脚本。
LDCT/data/*
!LDCT/data/dataset.py
!LDCT/data/prepare_aapm.py
!LDCT/data/download.sh
!LDCT/data/download_modelscope.sh
# 2026-09-15 新增：AAPM 官方 Box 从本机不可达，改用 Kaggle 镜像；
# 该脚本含实测的 Range 偏移与单实例锁，是复现数据的关键，必须入库。
!LDCT/data/download_aapm.sh
*.hdf5

# ---------- 训练与调试产物（可由脚本重新生成） ----------
# ⚠️ 但 experiments/runs/<tag>/ 下的 config.json / results.json / history.json /
#    DONE **必须入库** —— 它们是论文所有数字的来源。
LDCT/outputs/
*.pth
*.ckpt
*.npz
*.log
*.tmp

# LaTeX 编译中间件（main.pdf 保留，便于直接查看）
LDCT/paper/latex/*.aux
LDCT/paper/latex/*.log
LDCT/paper/latex/*.out
LDCT/paper/latex/*.toc

# ---------- 第三方材料（版权原因，禁止上传） ----------
# 他人论文 PDF（约 300 MB）
前沿论文下载/
# EMFusion 官方代码与权重（Xu & Ma, Information Fusion 2021）
EMFusion/
emfusion_unpacked/
EMFusion An unsupervised enhanced medical image fusion network.pdf
# 从他人仓库复制的 README
wavefusion_readme.md
# 论文下载辅助脚本（指向上述第三方材料）
dl_parallel.ps1
resume_dl.ps1

# ---------- 构建产物 ----------
__pycache__/
*.py[cod]
*.so
*.egg-info/
.venv/
venv/
env/

# ---------- 本地工具链 / 编辑器 ----------
.claude/
.workbuddy*/
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ---------- 临时与草稿 ----------
skills-lock.json
s2_recofuse.json
~$*
*.html
ecf.txt
ecf_raw.html

# ---------- Office 文档（内容与同名 .md 重复，二进制无法 diff） ----------
*.docx

# ---------- 调研 agent 的临时抓取产物 ----------
# 个别已知的中间文件（HTML 转储已在上面按 *.html 排除）
ecf.txt
