# MGF-Net 项目状态（2026-09-13 凌晨）

> 这是一份**索引页**。接手时先读这里，再按需展开下面的分册。
> 本轮所有工作已提交推送至 https://github.com/yuehuan888/My-paper

---

## 一、一句话现状

**融合这条线走到了明确的结论点**：做了三处有实测支撑的改进，
但发现「源参考型指标大多退化」——其中一部分"模型输给平凡平均"是指标假象，
一部分是架构局限，当前证据无法完全分开。

**据此建议换方向**，首选「低剂量 CT 去噪」（技术资产复用率最高、真值最硬）。
**但这需要和你讨论后再定**，所以本轮到此打住，全部落盘。

---

## 二、分册索引

| 文件 | 内容 |
|---|---|
| `MGF-Net_实验记录_2026-09-12.md` | 审计四问 / 指标重建 / 数据集与泄漏 / **门控饱和** / **PR-LWT** |
| `MGF-Net_诊断阶段_2026-09-13.md` | 平凡基线 / `edge_refine` 与梯度损失的假设检验 / 交叉验证基建 |
| `MGF-Net_γ扫描与指标退化_2026-09-13.md` | **γ 扫描判读** / **指标景观与退化分析** / 方向影响 |
| `PR-LWT决断报告_2026-09-12.md` | 13-agent 对抗分析：PR-LWT 不能作性能创新 |
| `对比协议调研_2026-09-12.md` | 领域对比协议（无标准划分、基线池按任务分离等） |
| `替代方向调研_2026-09-13.md` | 若换方向做哪个：息肉分割 / 半监督分割 / ISIC / **低剂量CT去噪** / BUS-BRA |

---

## 三、这三轮做出来的东西

### 3.1 有实测支撑的模型改进（已合入代码）

| 改动 | 原状（实测） | 改进后 |
|---|---|---|
| 门控 `tanh(g)·0.4` → `sigmoid(g)` | **99.13% 像素贴在 +0.4 上限**，门控退化为常数 | 权重铺满 [0.007, 0.977]，std 涨 9.4 倍；10 项指标赢 9 项 |
| 分离式可学习小波 → **PR-LWT** | 闭环相对误差 **60.9%**（丢六成信号） | **1.40e-07**，且可逆性由**结构**保证 |
| 删除 `edge_refine` | 占 **21% 参数** | 旁路后十项指标无变化 → 参数 94,234 → **74,361** |
| LR 衰减点 | 硬编码 `[40,70,90]`，50 轮时 70/90 从不触发 | 按轮数比例缩放 |
| 训练轮数 | **86% 的运行末 10 轮斜率仍为负**（未收敛） | 已可修复（待重跑） |

### 3.2 发现（有数据支撑）

1. **门控饱和**：残差式门控的手工缩放常数 κ 会使门控退化为常数，
   融合实际变成固定比例线性混合。"像素级自适应"从未发生。**已修正。**
2. **PR ≠ 数值稳定**：float32 下条件数随参数幅度急剧恶化
   （|taps| 1.0→3.6e-07，5.3→5.2e+00）；换 float64 则误差降到 6.3e-07。
   故提升了小波参数必须**有界化**。
3. **指标退化**：MI 的最优解是 α=0.10（几乎照抄 MRI），VIF 的最优解是纯 MRI
   ——**它们不衡量融合能力**。模型唯一超越全部线性混合的指标是 Qabf。
4. **数据泄漏效应远小于预期**：病例级重叠确实存在（10 个病例 9 个跨集），
   但可测虚高仅 **1–3%**。

### 3.3 基础设施（可迁移）

| 资产 | 可迁移性 |
|---|---|
| `experiments/run_cv.py` | **高**——病例级 K 折 + 病例内先平均再跨病例统计 |
| `config.py` | **高**——4GB 显存的正确配置 |
| `models/lifting_dwt.py`（PR-LWT） | **高**——低剂量 CT 去噪可直接用 |
| `utils/metrics.py` | 低——融合专用 |
| `models/mgf_net.py` | 低——融合专用 |

---

## 四、我在这两轮犯的错误（记录备查）

| # | 我的推断 | 实际 | 教训 |
|---|---|---|---|
| 1 | PR-LWT 把闭环改善 430 万倍 | **对比对象错了**——固定 Haar 也是 1e-7，C 只是恢复了 Haar 固有可逆性 | 对比要选对基线 |
| 2 | 数据泄漏导致 90% 污染 | 虚高仅 **1–3%** | 把"污染程度"当成"虚高程度" |
| 3 | `edge_refine` 造成保真度差距 | 旁路后**指标无变化** | 机制测量 ≠ 结果影响 |
| 4 | balance 项拉向平均 / 缓解 MI 差距 | **两次都错** | 该质疑的是推理本身 |
| 5 | LH/HL 命名已与旧代码对齐 | **实为互换** | ⭐ **测试用同一误解写参照，抓不到该误解** |

**共同失败模式**：把"我构造的推理/度量"当成"事实"。
**本轮的应对**：每一步都加受控对照 + 独立复核 + 效应量/方差意识。

---

## 五、下一步（需你决定）

### 决策点 1：是否换方向

| 选项 | 内容 | 理由 |
|---|---|---|
| **A（推荐）** | 转到**低剂量 CT 去噪**（LoDoPaB-CT） | 真值是数值张量，**根除指标退化问题**；PR-LWT 可 1:1 迁移；CC-BY-4.0；dival 官方库自带下载/评测/榜单；4GB 够用 |
| B | 写成"融合指标退化性"方法学论文 | 发现有意思，但**只有 10 病例、2 测试病例**，基准论文说服力不足 |
| C | 换融合数据集（SynthRAD2023 MRI→CT 合成） | 代码改动最小，真值是同患者实测 CT；仍是"跨模态"故事 |
| D | 息肉分割 / 半监督分割 / ISIC 皮肤 | 调研排名靠前，但**技术资产复用率低**，基本从零开始 |

### 决策点 2：若继续留在融合

需要先做（都已就绪，只是没跑）：
1. 5 折交叉验证（约 85 分钟）——把独立样本数从 2 提到 10
2. 训练到收敛（修好 LR 调度后重跑）
3. 真实基线对比（MMDRFuse / GeSeNet / CDDFuse）

但**即使这些都做完，"MI 输给 Average"这个问题依然在**（已证明不是损失函数造成的）。

---

## 六、随时可复现

```bash
cd D:/图像相关论文/MGF-Net
PY=D:/DeveloperTools/miniconda/envs/mgfnet/python.exe

$PY tests/test_metrics_invariants.py    # 指标 L0，应 8/8
$PY tests/test_lifting_pr.py            # PR-LWT，应全 ✅
$PY diagnose_step1.py                   # 审计
$PY experiments/evaluate_trivial_baselines.py --which test
$PY experiments/analyze_gamma.py --include-baseline
$PY experiments/run_cv.py --report-only # 交叉验证汇总
```

每次运行的配置（含划分、数据来源、代码版本）在 `experiments/runs/<tag>/config.json`。
