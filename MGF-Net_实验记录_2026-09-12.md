# MGF-Net 实验记录

> **日期**：2026-09-12
> **会话目标**：把项目从"跑不出来"推进到"有真实结果、可迭代"
> **状态**：进行中 —— 门控与 PR-LWT 已完成并验证；数据泄漏实验在跑；融合质量对照未做

本文件是本轮工作的**完整记录**，供后续迭代接手。所有数字都标注了来源脚本，
可逐条复跑核对。

---

## 〇、一句话摘要

本轮把项目**从"一堆对不上的数字"重建为"有据可查的实验链条"**，并得到两个
可发表的发现（门控饱和、数据泄漏）与一个已验证的方法组件（PR-LWT）。
过程中**修正了多处此前流传的错误结论**，包括我自己前一轮报告中写错的 Q4。

---

## 一、起点：审计四个问题

**脚本**：`MGF-Net/diagnose_step1.py`
**报告**：`MGF-Net_审计报告_Step1_2026-09-12.md`

在改动任何代码之前，先量化现有实现的问题。

| # | 问题 | 结论 | 影响 |
|---|---|---|---|
| Q1 | 推理时滤波器是否正确载入 | ✅ **载入正确**（误差 0.000e+00） | 修订版列为 P0 的 `learnable_dwt` 标志不一致属**虚警** |
| Q2 | DWT/IDWT 单元闭环误差 | 🔴 **rel_L2 = 60.9%** | PR 性质被"可学习"彻底破坏 |
| Q3 | 门控 CT 偏置 | 🔴 **饱和在人工上限** | 门控退化为常数 |
| Q4 | SSIM 口径 | ⚠️ **原结论错误，已更正** | 见下 |

### Q4 勘误（重要）

原报告称局部窗口下 MGF-Net 的 SSIM 为 0.4318、Average 为 0.7332。**该结论错误。**

原因是诊断脚本从未调用 `model.eval()`，而模型含 **24 个 `BatchNorm2d` 层**，
`train()` 模式使用批次统计而非训练累积的 running statistics：

```
model.train():  SSIM_Avg = 0.3684   ← 原报告依据
model.eval() :  SSIM_Avg = 0.7114   ← 与 test.py 保存的图（0.7105）吻合
```

Q1/Q2/Q3 不受影响（Q1 无前向；Q2 只测 dwt/idwt 不经过 BN；Q3 已在 eval 下复测，结论不变）。
`diagnose_step1.py` 已同步修补。

**教训**：任何涉及模型前向的诊断脚本必须显式 `model.eval()` + `torch.no_grad()`，
且应与已有参考产物交叉核对。

### 更正后的真实格局

用 `test.py` 保存的融合图、以标准指标重算（2 对测试图）：

| 指标 | Average | MGF-Net v2.1 | 判断 |
|---|---|---|---|
| **Qabf** | 0.3790 | **0.5981** | MGF-Net 大幅领先 |
| SF | 24.29 | **44.13** | MGF-Net |
| SD | 61.71 | **70.42** | MGF-Net |
| EN | 4.0553 | **4.4389** | MGF-Net |
| **MI** | **3.9436** | 2.0763 | **Average 近 2 倍** |
| VIF | **0.2596** | 0.1777 | Average |
| CC | **0.8093** | 0.7623 | Average |
| PSNR | **14.49** | 13.78 | Average |
| **SSIM** | 0.7152 | 0.7109 | **≈ 持平** |

**SSIM 是持平的，不是原报告说的差 0.30。** 真实格局是**两组指标方向相反**：
MGF-Net 赢在锐度/边缘类（Qabf、SF、SD、EN），输在信息保真类（MI、VIF、CC、PSNR）。

一句话：**当前模型"看着锐利，但信息是丢的"**——与 Q2 的 61% 闭环损失自洽。

---

## 二、评价体系重建

### 2.1 调研结论（107-agent，含对抗验证）

**报告**：`对比协议调研_2026-09-12.md`

| 结论 | 依据 |
|---|---|
| 领域内**无标准划分** | Harvard/AANLIB 是教学图谱；各论文自组子集，CT-MRI 源图 160–184 对 |
| 最常被引用的数据集仓库**本身是坏的** | `wenzhezhai/Medical-image-fusion-dataset` README 声明 35/15，实际 ship 50+50/15+15，分辨率不一致，无 license/manifest/评价代码 |
| 基线池**按任务分离** | 医学融合（CDDFuse、EMMA、MMDRFuse、GeSeNet、FATFusion）与红外-可见光（LRRFNet、SegMIF、DCINN、SHIP）不可混用 |
| MI/VIF/Qabf **并非必备指标** | CMPB 2025 综述中 VIF 出现 0 次；TTTFusion/EH-DRAN 只用 PSNR/SSIM/FMI/FSIM/EN |
| **无人说明 MI 的两源聚合方式** | 四篇论文全未说明求和还是平均 —— 系统性可复现性缺口 |
| SSIM **不可跨论文比较** | ECFusion 报 0.2312–0.6865，同领域其他报 0.6–0.9 |

### 2.2 指标实现

**模块**：`MGF-Net/utils/metrics.py`
**测试**：`MGF-Net/tests/test_metrics_invariants.py`（L0，**8/8 通过**）

实现 EN / SD / SF / MI / CC / PSNR / SSIM / VIF-P / SCD / Qabf 十项。
统一 [0,255] 计算域（SD/SF/PSNR/SSIM/VIF 的常数均绑定该量纲）。

**为什么自研**：领域内没有许可干净的现成 Python 实现——标准实现
（`Linfeng-Tang/Evaluation-for-Image-Fusion`）无 LICENSE，不可随 MIT 仓库分发。

关键取舍：

| 指标 | 取舍 |
|---|---|
| SSIM | 用 skimage + 显式 `gaussian_weights=True, sigma=1.5, use_sample_covariance=False`。默认参数与 Wang 的 MATLAB `ssim.m` 不一致 |
| SCD | 标准 Pearson + 交叉配对。⚠️ 原论文 Eq.10 分母漏印根号，照抄会得到 >2 的错值 |
| Qabf | 显式处理平坦区 0/0，否则 NaN 沿 sum 传播成全图 NaN。采用**联合归一化**形式（经综述独立验证） |
| VIF | 是 VIF-P（Sheikh-Bovik），**非**融合专用 VIFF，二者不可互换署名 |
| MI | 明确采用**求和**（Qu et al. 2002），并写入文档——这正是领域内的空白点 |

L0 测试不需外部参考值即可抓错，已抓出三类问题（`as_255` 重复调用、SCD 差分图负值、
以及**我自己一条测试断言写错**：Qabf 的"完全保持"是 0.9748 而非 1.0，因为 sigmoid 上限是渐近逼近的）。

### 2.3 待补的验收层

| 层 | 内容 | 状态 |
|---|---|---|
| L0 | 不变量测试 | ✅ 完成 |
| L1 | MATLAB 金标准对照 | ⏳ 等 MATLAB 装好 |
| L2 | `sewar`(MIT) / `skimage`(BSD-3) 交叉验证 | ⏳ 未做 |
| L3 | 文献值域对照 | ✅ 已做（见 §六） |

---

## 三、数据集：获取、溯源与泄漏发现

### 3.1 数据来源

`xianming-gu/ASFE-Fusion` 发布的 Harvard/AANLIB 数据集：

```
CT-MRI    184 对    PET-MRI  269 对    SPECT-MRI  357 对
全部 256×256，各模态 ID 完全对应
```

数量与 ASFE-Fusion (BSPC 2024) 论文声明**逐项吻合**，且 EH-DRAN、TTTFusion
沿用同一 184/357 谱系。**故与近年论文使用的是同一份数据。**

⚠️ 该数据为第三方重打包，**无 license 声明**，仅本地使用，不入库。

### 3.2 关键发现：ID 中含病例结构

```
id // 1000 = 病例号      id % 1000 = 切层号

CT-MRI 的 184 张 = 10 个病例 × 16–21 层
  病例 [2, 16, 20, 21, 28, 32, 33, 34, 37, 41]
  CT 与 MRI 的病例号完全一致
```

### 3.3 由此暴露的数据泄漏

领域惯例是"从 184 对中随机抽 24 对做测试"（ECFusion、ASFE-Fusion）、
或随机 30 对 × 3 次（EH-DRAN、TTTFusion）。复现该做法后：

```
病例级划分（本仓库做法）        切片级随机划分（领域惯例）
  训练病例 [2,16,20,21,28,32,33]     训练 [2,16,20,21,28,32,33,34,37,41]
  测试病例 [37,41]                   测试 [2,20,21,28,32,33,34,37,41]

              → 10 个病例中 9 个跨训练/测试
```

同一病例的相邻脑切片高度相似，故**测试集 90% 被训练数据污染**。
调研证实**没有任何一篇论文公布划分清单**，所以该问题一直未被发现。

**量化实验正在进行**（`leak_clean_*` vs `leak_slice_*`，3 种子 × 2 条件）。

### 3.4 产出

| 文件 | 内容 |
|---|---|
| `data/build_manifest.py` | 扫描数据集、解析病例/切层、生成清单与划分 |
| `data/manifest_ct_mri.csv` | 184 对逐图清单（病例号、切层号、路径、尺寸、哈希） |
| `splits/ct_mri_case_v1.json` | 病例级 7:1:2（train 123 / val 21 / test 40）+ 切片级随机对照 |

---

## 四、实验基础设施

**为什么需要**：原 `train.py` 拿**训练损失**当 best（`train.py:212`），
且没有验证集；`test.py` 的 SSIM 是全图统计的退化实现；`compare_baselines.py`
把训练图与测试图混在一起出总分。

| 文件 | 作用 |
|---|---|
| `experiments/run_experiment.py` | 单实验运行器：固定种子、**按验证集选模**、保存 best.pth 与完整 config（含划分、数据来源、代码版本） |
| `experiments/aggregate.py` | 跨种子汇总，输出均值±标准差与组间差值 |
| `experiments/inspect_gate.py` | 检查门控权重的实际分布（是否饱和/有无空间变化） |
| `evaluate.py` | 批量评价 CLI，按文件名显式配对，缺失/尺寸不符一律报错 |
| `data/dataset.py` | 支持显式 ID / 显式路径对，两侧文件集合不一致时报错而非静默截断 |

`run_experiment.py` 支持 `--split-key` 与 `--eval-split` 分离——
后者是量化数据泄漏的关键（用泄漏划分训练、在干净留出集上评价）。

---

## 五、发现 1：门控饱和（可发表）

### 5.1 问题

```python
fused = CT + tanh(g) * 0.4 * (MRI - CT)      # w ∈ [-0.4, 0.4]
```

`*0.4` 这个手写常数使 MRI 的系数被限制在 [−0.4, 0.4]，**等权平均（w=0.5）数学上不可达**。

### 5.2 机制验证

`inspect_gate.py`，131,072 个像素：

| | `residual_capped`（原式） | `neutral_sigmoid`（对照） |
|---|---|---|
| 值域 | [−0.3999, +0.4000] | [+0.0066, +0.9772] |
| **贴上限（±2%）占比** | **99.13%** | **0.00%** |
| **标准差** | **0.0142** | **0.1339** |
| 中位数 | 0.4000 | 0.5450 |
| w > 0.49 占比 | 0.00% | **81.26%** |

**结论：原式"门控"实际是常数**，标准差仅 0.0142，融合退化为
"固定 60% CT + 40% MRI"的线性混合。"像素级自适应选择"从未真正发生。

### 5.3 受控对照

只改一个变量（w 的定义），其余冻结。2 种子 × 60 轮：

| 指标 | residual_capped | neutral_sigmoid | 差值 |
|---|---|---|---|
| **SD** | 68.58 ±1.41 | **75.39 ±1.95** | −6.81 |
| **SCD** | 0.9943 ±0.033 | **1.1513 ±0.005** | −0.157 |
| **MI** | 2.0546 ±0.025 | **2.1864 ±0.043** | −0.132 |
| **SF** | 43.65 ±0.04 | **44.56 ±1.48** | −0.91 |
| **Qabf** | 0.5938 ±0.005 | **0.6140 ±0.007** | −0.020 |
| VIF | 0.1835 | **0.1985** | −0.015 |
| SSIM | 0.7136 | **0.7249** | −0.011 |
| CC | 0.7682 | **0.7730** | −0.005 |
| EN | 4.3436 | **4.3642** | −0.021 |
| PSNR | **13.837** | 13.644 | +0.193 |

**10 项中赢 9 项。** 赢的集中在"信息保真"类（MI/SCD/VIF）——正是落后于 Average 的那一类；
同时 **Qabf 也上升**，排除"锐度换保真"的此消彼长。效应量大于跨种子标准差。

### 5.4 意义

这是**结构性缺陷**而非调参问题。`fused = CT + tanh(g)·κ·(MRI−CT)` 这种
"残差注入 + 手工缩放常数"的设计在该领域很常见，本实验证明 κ 会使门控饱和。

**注意**：末层激活必须随门控类型变化。若对照版沿用 Tanh，
`sigmoid(tanh(g))` 会把 w 压进 [0.269, 0.731] 的窄带——比原式更不中性。

---

## 六、发现 2：PR-LWT（可发表）

### 6.1 动机：实测而非推理

审计 Q2 实测 checkpoint 的变换闭环：**rel_L2 = 60.9%**，常量图连 DC 都不保持。
原因是正/逆变换是两组**互相独立**的可学习参数，正交性漂移后无任何保证。

### 6.2 实现

**模块**：`MGF-Net/models/lifting_dwt.py`

用 lifting scheme 参数化：

```
分解：d = x_o − P(x_e)        s = x_e + U(d)
合成：x_e = s − U(d)          x_o = d + P(x_e)      # 与分解共用同一组 P、U
```

**可逆性由结构保证**，不依赖训练中"希望参数保持正交"。

Haar 初始化：`P = [0,1,0]`（恒等预测）、`U = [0,0.5,0]`，再施加 √2 缩放，
使初始化**严格等于正交归一 Haar 系数**。

### 6.3 关键发现：PR ≠ 数值稳定

无约束实现下，大幅扰动参数后闭环误差达 **17.17**。排查结果：

```
|taps|max ≈ 1.0 → max|err| ≈ 3.6e-07
|taps|max ≈ 3.5 → max|err| ≈ 2.3e-02
|taps|max ≈ 5.3 → max|err| ≈ 5.2e+00

同一组参数换 float64：12.19 → 6.26e-07     （差 1900 万倍）
```

float64 能修复 → **不是结构错误，是浮点条件数问题**。

即：**不加约束的可学习提升格式会漂到 float32 重构被摧毁的区间，
退回与原 bug 相同的失败模式**——只是原因不同。

**解法**：taps 参数化为 `init + 0.5·tanh(θ)`。θ 自由学习，taps 恒有界。
θ 取 ±1e3 时闭环仍稳定在 6.5e-05。

> 现有文献讨论"可学习小波"时只讲可逆性，**未见讨论数值条件数**。这一点本身有报告价值。

### 6.4 验证结果（`tests/test_lifting_pr.py`，全部通过）

| 场景 | 最差 max\|err\| |
|---|---|
| 初始参数 | 2.98e-07 |
| θ 大幅扰动（σ=50） | 6.4e-06 |
| 训练后（漂移 0.4756） | 3.0e-07 |
| 奇偶尺寸（255×255、63×63、127×253…） | 2.98e-07 |
| 极端 θ = ±1e3 | 6.5e-05 |
| AMP（单列） | 4.9e-04 |

初始化与正交归一 Haar 四个子带的偏差：LL 2.4e-07 / LH 5.96e-08 / HL 1.19e-07 / HH 0。

参数量 **24**（2级 × 2方向 × 2滤波器 × 3taps）。

### 6.5 集成效果

同一测试图，训练后闭环：

```
legacy : rel_L2 = 6.09e-01
lifting: rel_L2 = 1.40e-07      ← 相差约 430 万倍
```

模型参数量 94,234（legacy 94,274）——PR 的代价是**负 40 个参数**。

### 6.6 ⚠️ 边界声明（必须写进论文）

**PR 只保证"系数未被修改"时的分析–合成闭环。** 融合恰恰要修改系数，
故 PR **不保证**融合信息全保留，更不保证诊断价值。**不得称"无损融合"。**

**闭环好 ≠ 融合好。** 融合质量是否改善**尚未验证**，需跑
fixed-Haar / unconstrained / PR-LWT 三组受控实验。

---

## 七、论文素材盘点

| 素材 | 证据强度 | 状态 |
|---|---|---|
| **门控饱和的结构性缺陷与修正** | 强（受控对照 + 机制验证） | ✅ 完整 |
| **数据泄漏的发现与量化** | 待数据 | 🔄 实验跑着 |
| **PR-LWT：可逆性由结构保证** | 强（多场景验证） | ✅ 完整 |
| **PR ≠ 数值稳定**（条件数发现） | 强（float64 对照） | ✅ 完整 |
| 可追溯的病例级划分 | 强 | ✅ 完整 |
| 明确的 MI 两源聚合约定 | 强（领域空白） | ✅ 完整 |

前两项属**方法论层面**的贡献——不需要打败 SOTA 就能立住，对"先发出去"特别合适。

---

## 八、文献值域对照（L3）

| 指标 | 本实验 (neutral) | 文献区间 | 判断 |
|---|---|---|---|
| Qabf | 0.614 | 0.379–0.650 (ECFusion) | ✅ 接近上限 |
| SSIM | 0.725 | 0.231–0.687 (ECFusion) | ✅ 超出 |
| SCD | 1.151 | 0.91–1.58 (LKC-FUNet) | ✅ 区间内 |
| CC | 0.773 | 0.830±0.02 (ASFE-Fusion) | 🟡 略低 |
| MI | 2.186 | 2.58–3.62 / 3.147±0.16 | 🟠 偏低 |
| VIF | 0.199 | 0.336–0.671 (ECFusion) | 🔴 明显偏低 |
| EN | 4.364 | 5.486±0.27 / 10.73 | 🔴 偏低 |
| PSNR | 13.64 | 16.83±0.49 (EH-DRAN) | 🟠 偏低 |

**边缘类指标已达标或超出，信息保真类全面偏低** —— 与 Q2 的 61% 闭环损失自洽，
故 PR-LWT 正是针对性的修复。

---

## 九、已知问题与更正记录

### 9.1 本轮修正的错误结论

| 原结论 | 更正 | 来源 |
|---|---|---|
| Q4：MGF-Net 局部 SSIM 0.4318 vs Average 0.7332 | 实为 0.7109 vs 0.7152，基本持平 | 诊断脚本漏调 `eval()` |
| "0.7483 与 0.6886 矛盾" | 两数一致（混合十图 vs 两图测试），只是集合不同 | 修订版指出，已核实 |
| "第一张把模型加载算进计时" | 计时只在 forward 外层；真问题是缺 CUDA 同步与预热 | 修订版指出，已核实 |
| "L1+balance 正是像素平均的定义" | 数学上不严谨，逐像素最优解是整个区间 | 修订版指出，已接受 |
| LH/HL 子带命名 | 与旧代码相反，已修正为"先高度后宽度" | 自己的测试抓到 |

### 9.2 文档与代码对不上的数字

| 文档所写 | 代码实测 | 处理 |
|---|---|---|
| `工作进展报告` §3：v2 ≈ 65K 参数 | **94,274**（legacy）/ 94,234（lifting） | 以代码为准 |
| `工作进展报告` §5.2：SSIM_Avg 0.6775 / 0.7483 | 训练集与测试集混算 | 已作废 |
| `前沿调研报告` §5.1："WaveFusion 用 VIF-Benchmark 做评估" | VIF-Benchmark 是**方法合集**，不含指标 | 已更正 |

**建议**：论文定稿前做一次全量数字核对，所有数字从代码重算，不跨文档抄。

### 9.3 未解决

- `compare_baselines.py` 硬编码绝对路径 `D:/图像相关论文/EMFusion/...`（任务 #11）
- `test.py` / `compare_baselines.py` 仍在使用全图统计的 `compute_ssim`（任务 #12 剩余）
- `models/dwt_layer.py` 的 `groups=C` 在 C>1 时形状有误（当前 C=1 侥幸绕过）
- 数据集为第三方重打包且无 license，仅本地使用

---

## 十、待办

| 优先级 | 任务 | 说明 |
|---|---|---|
| 高 | 三组波形对照实验 | fixed-Haar / unconstrained / PR-LWT，验证对**融合质量**的影响 |
| 高 | 数据泄漏量化结果 | 实验在跑 |
| 高 | 基线复现 | 从 MIF 池选：MMDRFuse、GeSeNet、CDDFuse；VIF-Benchmark 提供 20 个方法的权重 |
| 中 | L1 MATLAB 金标准对照 | 等 MATLAB 装好 |
| 中 | 消除硬编码路径 | 任务 #11 |
| 中 | 切换 `test.py` 到新指标模块 | 任务 #12 剩余 |
| 中 | loss 修正 | 一次只改一类目标，权重由验证集选 |
| 低 | PET-MRI / SPECT-MRI 扩展 | 按 `build_manifest.py` 同法可做 |

---

## 十一、复现指南

```bash
cd D:/图像相关论文/MGF-Net
PY=D:/DeveloperTools/miniconda/envs/mgfnet/python.exe   # torch 2.5.1+cu121

# ---- 指标 ----
$PY tests/test_metrics_invariants.py          # L0，应 8/8 通过

# ---- PR-LWT ----
$PY tests/test_lifting_pr.py                  # 应全部 ✅

# ---- 审计 ----
$PY diagnose_step1.py

# ---- 数据清单与划分 ----
# 需先解压 hmif.tar.gz 到 data/harvard_full/
$PY data/build_manifest.py --modality CT-MRI --name ct_mri_case_v1

# ---- 单实验 ----
$PY experiments/run_experiment.py \
    --tag my_run --gate neutral_sigmoid --wavelet lifting \
    --split splits/ct_mri_case_v1.json \
    --seed 0 --epochs 50 --oversample 3 --val-interval 5

# ---- 汇总 ----
$PY experiments/aggregate.py --group-by gate_type
$PY experiments/aggregate.py --group-by wavelet

# ---- 机制检查 ----
$PY experiments/inspect_gate.py experiments/runs/<tag>/best.pth
```

每次运行的配置、划分、数据来源、代码版本记录在 `experiments/runs/<tag>/config.json`。

---

## 十二、诚实的局限

1. **融合质量尚未验证**。门控对照与 PR-LWT 的验证都在"机制层"，
   唯一一次融合质量对照（门控）用的是 2 对测试图的 screening 划分。
2. **PR-LWT 对融合质量的影响未知**。闭环好是必要条件，不是充分条件。
3. **数据泄漏实验未出结果**，该贡献尚待数据支撑。
4. **无外部基线对比**。目前所有对照都是内部受控对照，没有与已发表方法比。
5. **L1/L2 指标验收未做**，指标实现的绝对正确性只有 L0 不变量与 L3 量级支撑。
6. **数据集来源为第三方重打包**，溯源与许可均未澄清。
