# 实验报告：门控形式对照

> **日期**：2026-09-12
> **脚本**：`MGF-Net/experiments/run_experiment.py`、`inspect_gate.py`、`aggregate.py`
> **数据**：`splits/ct_mri_screen_v1.json`（train 6 / val 2 / test 2）
> **状态**：⚠️ **screening-only，样本仅 2 对测试图，不得写入论文**

---

## 一、结论

**原式的"残差注入门控"是饱和的、不具自适应能力；去掉手工缩放常数后，门控才真正铺开，且各项指标一致改善。**

这不仅是调参问题，而是**结构性缺陷**：

```
fused = CT + tanh(g) · κ · (MRI − CT)          κ = 0.4（手写常数）
```

实测该 κ 使门控权重几乎处处贴在上限，融合退化为固定比例的线性混合。改用

```
fused = (1−w)·CT + w·MRI,   w = sigmoid(g)
```

后权重铺满 [0, 1]，指标一致提升。

---

## 二、实验设置

**只改一个变量**，其余全部冻结（同一划分、同一种子、同一损失、同一训练轮数）：

| | `residual_capped`（原式） | `neutral_sigmoid`（对照） |
|---|---|---|
| 融合式 | `fused = ct + w·(mri−ct)` | `fused = ct + w·(mri−ct)` |
| w 的定义 | `tanh(g) × 0.4` | `sigmoid(g)` |
| w 的值域 | [−0.4, 0.4] | (0, 1) |
| 权重网末层激活 | Tanh | Identity（输出 logits） |
| `refine` 分支 | 相同 | 相同 |

> ⚠️ 末层激活必须随类型变化。若对照版沿用 Tanh，`sigmoid(tanh(g))` 会把 w 压进
> [0.269, 0.731] 的窄带——比原式更不中性。这一点容易漏。

另有固定项：Adam lr=1e-3，MultiStepLR(40/70/90, γ=0.5)，损失 α_ssim=1、β_l1=10、γ_grad=5，
batch=4，patch=128，oversample=25，60 epochs，按**验证集 SSIM** 选模。

---

## 三、定量结果（2 种子，test 集均值）

| 指标 | residual_capped | neutral_sigmoid | 差值 | 判断 |
|---|---|---|---|---|
| **SD** | 68.58 ±1.41 | **75.39 ±1.95** | −6.81 | ▲ 3.5× 标准差 |
| **SCD** | 0.9943 ±0.033 | **1.1513 ±0.005** | −0.157 | ▲ |
| **MI** | 2.0546 ±0.025 | **2.1864 ±0.043** | −0.132 | ▲ 3× 标准差 |
| **SF** | 43.65 ±0.04 | **44.56 ±1.48** | −0.91 | ▲ |
| **Qabf** | 0.5938 ±0.005 | **0.6140 ±0.007** | −0.020 | ▲ |
| VIF | 0.1835 ±0.002 | **0.1985 ±0.006** | −0.015 | ▲ |
| SSIM | 0.7136 ±0.001 | **0.7249 ±0.000** | −0.011 | ▲ |
| CC | 0.7682 | **0.7730** | −0.005 | ▲ |
| EN | 4.3436 | **4.3642** | −0.021 | ▲ |
| PSNR | **13.837** | 13.644 | +0.193 | ▼ |

**10 项中赢 9 项**，唯一落后的 PSNR 并非公认的融合质量指标。

关键点：**赢的集中在"信息保真"类（MI / SCD / VIF）**——正是审计中落后于 `Average` 的那一类；
同时 **Qabf 也上升**，说明不是"锐度换保真"的此消彼长。

---

## 四、机制验证：门控权重分布

`inspect_gate.py` 对训练后模型在测试图上统计 w 的实际分布（7 子带全部像素，131,072 个）：

| | residual_capped | neutral_sigmoid |
|---|---|---|
| 值域 | [−0.3999, +0.4000] | [+0.0066, +0.9772] |
| 理论范围 | [−0.4, 0.4] | (0, 1) |
| **贴上限（±2%）占比** | **99.13%** | **0.00%** |
| 贴下限（±2%）占比 | 0.01% | 0.02% |
| **标准差** | **0.0142** | **0.1339** |
| 中位数 | 0.4000 | 0.5450 |
| w > 0.49 占比 | 0.00% | **81.26%** |

**判读**：

- 原式门控 **99.13% 的像素贴在 +0.4 上限**，标准差仅 0.0142 ——
  它对整图输出几乎相同的权重，**融合实际退化为"固定 60% CT + 40% MRI"的线性混合**，
  "像素级自适应选择"从未真正发生。
- 中性门控权重铺满值域，标准差涨 **9.4 倍**，81% 的像素跨过等权点 ——
  网络得以按区域决定信源。

**这解释了定量结果**：门控此前被人为卡死，网络无法按需注入 MRI 信息，
因而 MI / VIF 等信息保真指标长期偏低。

---

## 五、与文献值域对照（L3）

| 指标 | 本实验 (neutral) | 文献区间 | 判断 |
|---|---|---|---|
| Qabf | 0.614 | 0.379–0.650 (ECFusion, 24 对) | ✅ 接近上限 |
| SSIM | 0.725 | 0.231–0.687 (ECFusion) | ✅ 超出 |
| SCD | 1.151 | 0.91–1.58 (LKC-FUNet) | ✅ 区间内 |
| CC | 0.773 | 0.830±0.02 (ASFE-Fusion) | 🟡 略低 |
| MI | 2.186 | 2.58–3.62 (ECFusion) / 3.147±0.16 (ASFE) | 🟠 偏低 |
| VIF | 0.199 | 0.336–0.671 (ECFusion) | 🔴 明显偏低 |
| EN | 4.364 | 5.486±0.27 (ASFE) / 10.73 (EH-DRAN) | 🔴 偏低 |
| PSNR | 13.64 | 16.83±0.49 (EH-DRAN) | 🟠 偏低 |

**边缘类指标（Qabf / SSIM）已达标甚至超出，信息保真类（MI / VIF / EN / PSNR）全面偏低。**

这与审计报告 Q2（变换闭环相对 L2 误差 **60.9%**）自洽：
**可学习滤波器的重构损失，是当前信息保真度不足的主要嫌疑。**

> 注：文献数值来自不同论义、不同测试集规模、不同指标约定，跨论文比较需谨慎
> （ECFusion 自己就指出 SSIM 的口径差异可达 0.23–0.69）。此处仅作量级参照。

---

## 六、本实验的局限

1. **测试集仅 2 对图**，种子仅 2 个。效应量虽大于方差，但样本量远不足以支撑发表结论。
2. **划分未做病例级隔离**，各样本的病例来源尚未溯源。
3. **无外部基线对比**（本实验只做内部受控对照）。
4. 文献值域对照跨论文，指标约定未必一致。

**下一步**：在正式数据集（目标 160+ 对，见《对比协议调研_2026-09-12.md》）上重跑本对照，
并补足种子数至 ≥3。

---

## 七、可复现性

```bash
cd D:/图像相关论文/MGF-Net
PY=D:/DeveloperTools/miniconda/envs/mgfnet/python.exe

# 运行对照（每组约 10 分钟）
$PY experiments/run_experiment.py --tag gate_residual_capped_seed0 \
    --gate residual_capped --seed 0 --epochs 60 --val-interval 5
$PY experiments/run_experiment.py --tag gate_neutral_sigmoid_seed0 \
    --gate neutral_sigmoid --seed 0 --epochs 60 --val-interval 5

# 汇总
$PY experiments/aggregate.py

# 机制验证
$PY experiments/inspect_gate.py experiments/runs/gate_neutral_sigmoid_seed0/best.pth
```

每次运行的配置、划分、数据哈希、代码版本记录在 `experiments/runs/<tag>/config.json`。
