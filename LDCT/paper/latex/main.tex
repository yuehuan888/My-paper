% ============================================================================
%  Does Learning a Wavelet Help?
%  Perfect-Reconstruction Constraints Enable Learnable Wavelets
%  for Low-Dose CT Denoising
%
%  IEEE conference format (IEEEtran).
%
%  v2.1 (2026-09-18) --- synced to paper/conference_paper.md v2.1. New in v2.1:
%    - new Sec. 5.5 "Where the accuracy comes from: structure, not capacity"
%      (3x budget re-run; architecture sweep; capacity ceiling; matched RED-CNN
%      at n=3; training-stability comparison)
%    - limitations 3 and 4 rewritten -- both are now tested, not open
%    - abstract + contributions synced (5 -> 6 items)
%
%  COHORT WARNING: everything in Sec. 5.5 is measured on the RE-ACQUIRED
%  421-slice test set, whose 30-epoch reference is the same-cohort fix_* batch.
%  Tables I-III are the 435-slice original acquisition. Mixing the two shifts
%  values by 0.03-0.06 dB. experiments/analyze_convergence.py asserts this
%  invariant before comparing anything.
%
%  v2.0 (2026-09-16) --- synced to paper/conference_paper.md v2.0. Changes vs v1.0:
%    - new Sec. 5.2.1 (independent replication on re-acquired data, n=10 per arm)
%    - new Sec. 5.4 (what the transform learns; the bound and its value)
%    - four hard errors fixed (see below)
%    - figures 4 -> 17, all embedded and cited
%
%  NOTE ON LENGTH: this file currently carries the *complete record*, not a
%  camera-ready. At the 5-page ICIP budget it is over by several pages; a
%  submission build would need the supplementary figures (Sec. "Supplementary
%  figures") and parts of Sec. 5.2.1 moved out. Kept intact here deliberately.
%
%  NUMBER PROVENANCE (v2.0): every table was recomputed from experiments/runs/*
%  before being written here. Three errors in the markdown draft were caught and
%  are NOT reproduced: (a) the bound table printed the n=3 subset under an n=10
%  caption; (b) the n=10 MDES was quoted as 0.064 dB where the repo's own script
%  gives 0.074 dB (and the draft's own /MDES column already implied 0.074);
%    (c) the round-trip error was 12--16% in the abstract/intro but 11--18% in
%  Sec. 5.3 --- the measured value is 11.4--17.9% per seed (n=10).
%
%  Generated from paper/conference_paper.md on 2026-09-14; v2.0 sync 2026-09-16.
%  BUILD:  tectonic main.tex      (or: pdflatex main; pdflatex main)
%
%  DOUBLE-BLIND NOTE: ICME / VCIP / ISM are double-blind. For those, comment
%  out the \author block and uncomment \author{} with no content.
% ============================================================================
\documentclass[conference]{IEEEtran}

% IEEE body text is Times. IEEEtran asks for the `ptm' family, but under a
% Unicode engine the LaTeX kernel defaults to TU encoding, and psnfss defines
% ptm metrics only for T1/OT1 -- so `TU/ptm/*' is undefined and every build
% silently fell back to Latin Modern (TU/lmr). Loading T1 brings the encoding
% back to where the Times metrics live. mathptmx then supplies them.
% Order matters: both before amsmath/amssymb.
\usepackage[T1]{fontenc}
\usepackage{mathptmx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{url}
\usepackage{array}
\usepackage[hidelinks]{hyperref}

\graphicspath{{../figures/}}

% Compact float spacing (page budget is tight)
\setlength{\textfloatsep}{6pt plus 1pt minus 2pt}
\setlength{\floatsep}{5pt plus 1pt minus 2pt}
\setlength{\intextsep}{5pt plus 1pt minus 2pt}
\setlength{\abovedisplayskip}{4pt}
\setlength{\belowdisplayskip}{4pt}

% ---------------------------------------------------------------------------
% Float handling for a float-heavy document (16 figures + 14 tables).
%
% Two reasons this matters and is not cosmetic:
%  1. LaTeX holds at most ~18 unprocessed floats. Past that it raises "Too many
%     unprocessed floats" and drops the rest. \extrafloats raises the ceiling.
%  2. IEEEtran's defaults cap how much of a page floats may occupy, which with
%     this many wide figures would push them all to the end of the file.
% These are the standard relaxations for a figure-heavy paper. They cost nothing
% on the page budget (which is not binding here) and prevent a failure mode that
% cannot be caught without a compiler.
% ---------------------------------------------------------------------------
\extrafloats{200}
\renewcommand{\topfraction}{0.90}
\renewcommand{\bottomfraction}{0.60}
\renewcommand{\textfraction}{0.10}
\renewcommand{\floatpagefraction}{0.75}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{4}

% The dbl* family is the one that actually governs this document: a two-column
% float (`figure*`/`table*`) is NOT controlled by \topfraction / \floatpagefraction
% but by \dbltopfraction / \dblfloatpagefraction / \dbltopnumber. All 24 wide
% floats here are two-column, so tuning only the single-column parameters changed
% nothing at all (verified: byte-identical page fill before and after). Default
% \dbltopnumber is 2, which is what was forcing one figure onto its own page.
\renewcommand{\dbltopfraction}{0.90}
\renewcommand{\dblfloatpagefraction}{0.75}
\setcounter{dbltopnumber}{4}

% With 23 full-width floats the pages cannot all be filled exactly, and
% \flushbottom would stretch the inter-paragraph glue to hide that ("Underfull
% \vbox ... badness 10000"). Letting the bottom edge float is the standard
% remedy for a float-dominated document and removes the visible gaps.
\raggedbottom

\begin{document}

\title{Does Learning a Wavelet Help?\\ Perfect-Reconstruction Constraints Enable
Learnable Wavelets for Low-Dose CT Denoising}

% --- Single-blind venues (ICIP, EUSIPCO): fill in the real list -------------
\author{\IEEEauthorblockN{Author One\IEEEauthorrefmark{1}, Author Two\IEEEauthorrefmark{1}}
\IEEEauthorblockA{\IEEEauthorrefmark{1}Affiliation\\ City, Country\\ Email}}
% --- Double-blind venues (ICME, VCIP, ISM): use the line below instead ------
% \author{}

\maketitle

\begin{abstract}
Making the wavelet transform learnable is a natural extension of wavelet-domain
image denoising. We show that for low-dose CT, this extension \textbf{fails by
default}: an unconstrained learnable analysis--synthesis pair drifts away from
invertibility during training, and the resulting network performs \textbf{no
better than a fixed Haar basis}. Our design resolves a \textbf{0.136~dB} effect
at $n=5$ seeds, and the unconstrained--fixed gap is \textbf{0.025~dB ---
5.6$\times$ below that floor} --- so we state the result as a \emph{bound} rather
than an absence: any real difference is smaller than \textbf{0.14~dB}, roughly
\emph{half} the benefit that perfect reconstruction later delivers. We trace the
failure to the transform's reconstruction error, which reaches
\textbf{11--18\%} after training. We show this is \textbf{causal, not merely
correlated}: injecting a controlled error of the same magnitude into the
perfect-reconstruction arm degrades it to the fixed-Haar level. Imposing perfect
reconstruction \emph{by construction} --- parameterizing the wavelet as a lifting scheme whose inverse
shares the forward filters --- makes learning pay off: \textbf{+0.30~dB over
unconstrained learning ($p=0.0005$, $d_z=4.67$) and +0.27~dB over fixed Haar
($p=0.0004$, $d_z=4.78$)} --- large effects, not marginal ones --- consistent
across two data splits. The resulting network reaches 31.97~dB on AAPM-Mayo 2016
within 1.0~dB of a same-protocol RED-CNN baseline at \textbf{857$\times$ fewer
parameters} (2,159 vs.\ 1.85M); adding a full-resolution residual branch ---
\textbf{305 parameters} --- brings it within \textbf{0.52~dB} at \textbf{750$\times$
fewer}.

Two further results locate the effect. First, the transform learns \emph{little}:
from any numerically intact starting point, training converges to a narrow band
of drift from Haar and, starting \textbf{at} Haar, moves \textbf{away} --- the
optimum is slightly off Haar, which is why adaptation buys so little here. We
also reject the natural explanation that the heads simply substitute for the
transform: the PR advantage does not shrink as head capacity grows by two orders
of magnitude. Quantitatively, the \textbf{first 305 architectural parameters are
worth +0.35~dB while the next 265,000 are worth +0.26~dB} --- 871$\times$ the cost
for a smaller gain --- which is the thesis stated as a number: here, accuracy
comes from structure, not capacity. Second, the failure mode is a \textbf{cliff, not a slope} --- once
the forward transform's round-trip error exceeds the signal, the gradient carries
no information and training cannot recover (drift~4.0 stays at 4.000 and
3.29~dB, where drift~2.0 recovers fully). Being inside the numerical envelope is
therefore binary, which is why bounding is necessary --- and why the bound's
value matters more than expected: our default costs \textbf{+0.10 to +0.15~dB},
about half the entire PR effect.
\end{abstract}

\begin{IEEEkeywords}
low-dose CT, image denoising, learnable wavelet, lifting scheme, perfect
reconstruction, invertibility
\end{IEEEkeywords}

% ============================================================================
\section{Introduction}
% ============================================================================

Low-dose CT (LDCT) reduces radiation exposure at the cost of increased noise.
Deep denoising networks such as RED-CNN~\cite{chen2017redcnn} are effective but
parameter-heavy ($\sim\!10^6$ parameters), which limits deployment on the
resource-constrained workstations common in smaller clinical sites. Compact
models are therefore attractive.

Wavelet-domain processing is a classical way to separate noise from structure,
and recent work has made the wavelet itself \textbf{learnable} to adapt to a
specific modality and dose~\cite{liu2018mwcnn,li2020wavecnet,huang2021linn,%
huang2022winnet,wang2026adasffuse,wang2025wavefusion}. This paper asks a simple
question:

\begin{quote}
\textbf{Does making the wavelet learnable actually help?}
\end{quote}

Recent invertible-wavelet denoisers~\cite{huang2021linn,huang2022winnet} build
perfect reconstruction (PR) into their architectures. Whether that structure is
\emph{what makes learning pay} has never been tested: prior work
\textbf{assumes} invertibility, so it cannot observe its absence. Our answer,
from a controlled three-arm study that makes invertibility \textbf{optional}
while holding capacity, split, schedule and evaluation fixed, is: \textbf{learning
pays only if the transform is kept invertible by construction.}

Parameterizing the wavelet as a \textbf{lifting
scheme}~\cite{daubechies1998factoring,sweldens1996lifting} --- the inverse
\emph{reuses} the forward prediction/update filters rather than being a second
set of parameters --- makes learning pay off: \textbf{+0.30~dB over unconstrained
learning and +0.27~dB over fixed Haar}, at large effect size ($d_z \approx 4.7$),
replicated on a second split and on SSIM.

The inverted case is equally informative. An unconstrained learnable
analysis--synthesis pair performs \textbf{statistically indistinguishably from a
fixed Haar basis}. We are careful how we state this: the design resolves a
0.136~dB effect at $n=5$ seeds, whereas the unconstrained--fixed gap is
0.025~dB --- \textbf{$5.6\times$ below that floor}. So we report a \textbf{bound},
not an absence: any genuine difference is smaller than \textbf{0.14~dB}, roughly
half of what PR delivers. A weak experiment could not have resolved the effect
that PR produces.

\textbf{Why} it fails is the substance of the paper. After training, the
unconstrained pair loses \textbf{11--18\%} of the signal in a forward--backward
pass, against \textbf{1.6e-07} for both invertible arms. We show this is
\textbf{causal, not merely correlated}: holding architecture, capacity and
protocol fixed, we inject a controlled round-trip error into the PR arm, and it
degrades \textbf{monotonically} to the fixed-Haar level (8.7\% error $\to$
30.5500~dB, against 30.5591~dB for an arm that never learned anything;
$p \le 0.007$ at every dose). Prior invertible-wavelet denoisers cannot observe
this state, because their designs make it unreachable.

\textbf{Contributions.}
\begin{enumerate}
\item \textbf{A missing control}, and the negative result it exposes: with
capacity and protocol held fixed, an unconstrained learnable wavelet gives no
benefit over fixed Haar --- reported as a \textbf{bound of 0.14~dB}, not as an
absence.
\item \textbf{A causal mechanism}, established by intervention rather than
correlation: a round-trip error of the same magnitude is \emph{sufficient} to
destroy the benefit, monotonically and significantly ($n=5$ seeds,
$p \le 0.007$ at every dose). Prior invertible-wavelet denoisers cannot observe
this state.
\item \textbf{A numerical envelope, and what happens outside it.} Structural PR
does not enforce itself in float32. We characterize the boundary in two ways. By
a \textbf{controlled sweep of tap magnitude}, the round-trip error grows from
3.6e-07 at $|{\rm taps}| \approx 1.0$ to 5.2e+00 at $\approx 5.3$
(\emph{a controlled analysis of the parameter regime, not a training-drift
measurement}). And by initialising the taps at a known distance from Haar, we
show that crossing this boundary is a \textbf{cliff}: at drift~4.0 the forward
transform emits noise, the gradient carries no information, and training stays
exactly where it started (final drift 4.000, 3.29~dB), whereas drift~2.0
recovers fully. Being inside the envelope is therefore not a matter of degree ---
which is why we bound the taps, and why the bound's value matters more than we
expected (Section~\ref{sec:learns}).
\item \textbf{What the transform actually learns.} From any numerically intact
starting point, training converges to a \textbf{narrow band of drift from Haar
($\approx$0.65--0.78)}; starting \emph{at} Haar, it moves away. The optimum is
therefore slightly off Haar, which is the concrete reason ``learning'' buys so
little here. We also test --- and reject --- the natural hypothesis that the heads
simply substitute for the transform: the PR advantage does not shrink as head
capacity grows by two orders of magnitude.
\item \textbf{Where the accuracy comes from: structure, not capacity.} A sweep of
the four architectural degrees of freedom that prior runs left untouched, plus a
$3\times$-budget re-run of every headline number. The PR advantage
\textbf{widens} with training (+0.2837 $\to$ +0.3349~dB) and the null result is
unchanged; only one lever pays (a full-resolution residual branch, \textbf{+0.35~dB
for 305 parameters}); finer decomposition and longer lifting filters do nothing.
Beyond that the returns collapse: \textbf{871$\times$ more parameters buy less
than those first 305}. Retraining RED-CNN to the matched budget and data ($n=3$)
puts the distance at \textbf{0.85~dB} (95\% CI [0.72, 0.98]) --- budget-insensitive,
and not closed by capacity.
\item \textbf{A 2,464-parameter network} that comes within \textbf{0.52~dB} of a
same-protocol, same-budget RED-CNN baseline at \textbf{$750\times$ fewer parameters}.
\end{enumerate}

% ============================================================================
\section{Related Work}
% ============================================================================

\subsection{Low-dose CT denoising}
Convolutional encoder--decoders such as RED-CNN~\cite{chen2017redcnn}
established the field, followed by adversarial~\cite{yang2018wganvgg,%
huang2022dugan} and, more recently, transformer- and diffusion-based methods
\cite{wang2023ctformer,zhang2021transct,wang2021tednet,chen2024litformer,%
gao2024corediff,gao2025need,chen2026founddiff,sun2026wdbdm}. These improve
fidelity but at $10^5$--$10^7$ parameters. A parallel line targets efficiency
instead: MLAR-UNet~\cite{tang2025mlarunet}, CT-Mamba~\cite{li2025ctmamba},
AMFA-Net~\cite{li2026amfanet}, FMDNet~\cite{yao2026fmdnet} and
MobileMamba-UNet~\cite{li2026mobilemambaunet}
all report competitive results at far lower cost, the last of these also
operating in a wavelet domain. We share the efficiency goal but ask a different
question --- not \emph{how small can a denoiser be}, but \emph{does making the
transform itself learnable help at all}.

\subsection{Wavelet-domain and multiresolution denoising}
Wavelet shrinkage~\cite{donoho1994ideal,donoho1995denoising} separates noise from
structure by thresholding subband coefficients. Multi-level wavelet
CNNs~\cite{liu2018mwcnn,li2020wavecnet} fold this structure back into deep
networks, and recent LDCT methods pair wavelets with diffusion~\cite{sun2026wdbdm}
or state-space models~\cite{li2026mobilemambaunet}. In all of these the transform
is a \textbf{fixed} operator; the learnable capacity sits in the network around
it.

\subsection{Learnable transforms and invertibility}
A separate line makes the transform itself learnable. LINN~\cite{huang2021linn}
learns lifting prediction/update steps around a \emph{fixed} undecimated Haar
transform; WINNet~\cite{huang2022winnet} additionally learns the splitting
operator, constrained to be orthogonal via a Cayley transform so that PR is
preserved by construction. Both therefore \textbf{build invertibility into the
architecture}. Classical filter-bank theory~\cite{vetterli1986filter,%
vetterli1995wavelets} supplies the PR conditions, the lifting
scheme~\cite{daubechies1998factoring,sweldens1996lifting} is the standard
construction that satisfies them by design, and orthogonality has separately been
used in CNNs to condition optimisation~\cite{wang2020orthogonal}.

\subsection{The closest prior result, engaged directly}
WINNet's Table~II ablation compares decimated and undecimated Haar, DCT of
several sizes, and learned Cayley operators on natural-image AWGN denoising, and
reports that the \textbf{learned} operator performs \emph{similarly to a fixed
DCT} --- whereupon they default to the fixed one. This superficially resembles
our negative result, so we address it here rather than leave it for a reviewer.
Two differences matter. First, their learned operator is constrained to be
orthogonal, so PR holds throughout: their comparison is between two
\emph{invertible} transforms, whereas ours is between invertible and
\textbf{non}-invertible ones --- the failure we diagnose is not reachable in
their design, and neither paper reports a round-trip measurement anywhere.
Second, theirs is a default-picking ablation without significance testing; ours
is a five-seed paired comparison with confidence intervals.

\subsection{Our position}
We propose neither a new wavelet nor a new architecture --- the lifting
construction in Section~\ref{sec:prlwt} is LINN's and WINNet's, and WINNet
already learns the filter taps while preserving PR. What is new is the
\textbf{missing control}. Prior invertible-wavelet denoisers \emph{assume}
structural invertibility and never test its absence, and so cannot report what
happens without it. Making a learnable transform's invertibility
\emph{optional}, holding capacity and training fixed, and measuring the
consequence --- and the six-order-of-magnitude round-trip gap that explains it
--- is the contribution. Section~\ref{sec:ablation} returns to the residual
tension with WINNet's ablation.

% ============================================================================
\section{Method}
% ============================================================================

\subsection{Overview}
The network operates in the wavelet domain:
\begin{equation}
\begin{aligned}
x &\;\xrightarrow{\;\text{PR-LWT}\;}
     \{\text{band}_b\}_{b=1}^{7}
     \;\xrightarrow{\;\text{heads}\;}\; \{\text{band}_b - \delta_b\}\\
  &\;\xrightarrow{\;\text{PR-LWT}^{-1}\;}\; \hat{x}
\end{aligned}
\end{equation}
where $x$ is the noisy input and $\delta_b = \text{head}_b(\text{band}_b)$ is a
per-subband residual prediction. For 2 levels this gives seven subbands
$\{\mathrm{LL}_2,\mathrm{LH}_2,\mathrm{HL}_2,\mathrm{HH}_2,\mathrm{LH}_1,%
\mathrm{HL}_1,\mathrm{HH}_1\}$.

\subsection{PR-LWT: a perfectly-reconstructible learnable wavelet}
\label{sec:prlwt}

We use the \textbf{lifting scheme}. In 1-D, with even/odd splitting,
\begin{equation}
\begin{aligned}
\text{Analysis:}\quad & d = x_o - P(x_e), & s &= x_e + U(d)\\
\text{Synthesis:}\quad & x_e = s - U(d), & x_o &= d + P(x_e)
\end{aligned}
\end{equation}

Crucially, \textbf{synthesis reuses the same $P$ and $U$} as analysis.
Invertibility is therefore a property of the \emph{structure}, not of the
learned values: for any $P,U$,
$\text{reconstruct}(\text{decompose}(x)) = x$ up to floating point.

The 2-D transform is separable (lifting along height, then width). We initialize
$P=[0,1,0]$ (identity prediction) and $U=[0,0.5,0]$, plus a $\sqrt{2}$ output
scaling, so that the initialization is \textbf{exactly orthonormal Haar}
(verified to $2\times10^{-7}$ on all four subbands).

\textbf{Parameter bounding is necessary, not optional.} We parameterize
\begin{equation}
P = P_{\text{init}} + 0.5\tanh(\theta_P),\quad
U = U_{\text{init}} + 0.5\tanh(\theta_U)
\end{equation}
Without this, the taps drift to $|P|\approx5$, at which point the float32
round-trip error degrades from $3.6\times10^{-7}$ to $5.2\times10^{0}$. Perfect
reconstruction holds unconditionally in exact arithmetic; bounding is what makes
it \emph{usable} in float32.

\textbf{Parameter count}: 2 levels $\times$ 2 directions $\times$ 2 filters
$\times$ 3 taps $=$ \textbf{24}.

\subsection{Subband denoising heads}
Each subband is processed by a lightweight head ($\text{conv}_{3\times3}
\rightarrow \text{ReLU} \rightarrow \text{conv}_{3\times3}$, 16 internal
channels) that predicts a \textbf{residual} (signed, no final activation). Seven
heads contribute 2,135 parameters; the complete network has \textbf{2,159}.

\subsection{Training}
Loss: $L_1$ between prediction and ground truth. Adam, lr $10^{-3}$, 30 epochs,
$128\times128$ random patches, batch size 8.

% ============================================================================
\section{Experimental Setup}
% ============================================================================

\subsection{Data}
\textbf{AAPM-Mayo 2016 Low Dose CT Grand Challenge}~\cite{mccollough2020tcia,%
moen2021ldctdata}, 3\,mm B30 paired subset: 10 patients, 2,378 quarter-dose /
full-dose $512\times512$ slice pairs. Slices are paired by
\texttt{ImagePositionPatient[2]} (z position); pairing by filename is
\textbf{not} possible because the slice-index field is identical on both sides.

\subsection{Evaluation protocol}
\label{sec:eval}
AAPM never defined a PSNR protocol (its official metric was radiologist
reading), and at least five mutually incompatible conventions coexist on this
dataset. We adopt the \textbf{RED-CNN / CTformer lineage} convention, used
verbatim by six public repositories:
\begin{quote}
\small
\textbf{preprocess}: $x = (\mathrm{HU} + 1024)/4096$, no clipping.\\
\textbf{evaluate}: denormalize to HU, clip both prediction and reference to
$[-160, 240]$, and take PSNR $= 10\log_{10}(400^2/\mathrm{MSE}_{\mathrm{HU}})$,
i.e.\ data\_range $=400$; SSIM per SSinyu/RED-CNN.\\
\textbf{aggregate}: per-slice metrics, then mean.
\end{quote}

The identity baseline under this protocol is L506 29.2489, L067 26.5576,
combined 27.8630~dB. \textbf{We do not claim these as an independent
confirmation}, and we caution against reading cross-paper PSNR on this dataset at
face value: independent re-analyses of the same cohort differ by up to
\textbf{$\sim$2~dB} (a third-party report gives 27.24~dB on L506 against our
29.2489), and the Eulig et al.\ benchmark places its identity-equivalent
reference 0.8~dB away. Every claim below is therefore made against
\textbf{same-protocol baselines} rather than against quoted numbers.

\subsection{Splits}
Two patient-level splits are used to show robustness
(Table~\ref{tab:splits}).

\begin{table}[t]
\centering
\caption{Patient-level splits. S2 aligns with the convention used by published
work so that numbers are directly comparable.}
\label{tab:splits}
\small
\begin{tabular}{@{}llll@{}}
\toprule
Split & Train & Val & Test\\
\midrule
\textbf{S1} (main) & 7 pat.\ (1,600) & L291 & L506+L067 (435)\\
\textbf{S2} (lit.) & 8 pat.\ (1,943) & L067 & \textbf{L506 only} (211)\\
\bottomrule
\end{tabular}
\end{table}

\subsection{The floor: doing nothing}
Identity (output $=$ input) is the floor every model must beat. All ten
patients, sorted by PSNR, are given in Table~\ref{tab:floor}.

\begin{table}[t]
\centering
\caption{Identity baseline, all ten patients. The spread
(25.50--29.25~dB, a 3.75~dB range) is larger than any effect we report, so
\textbf{all results are reported per patient}.}
\label{tab:floor}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lrrrl@{}}
\toprule
Patient & $n$ & PSNR & SSIM & Appears as\\
\midrule
L143 & 234 & 25.4991 & 0.7643 & train\\
L310 & 214 & 26.2577 & 0.7273 & train\\
L067 & 224 & 26.5576 & 0.7987 & S1 test / S2 val\\
L109 & 128 & 26.6052 & 0.8165 & train\\
L096 & 330 & 26.9097 & 0.7853 & train\\
L291 & 343 & 26.9805 & 0.8088 & S1 val / S2 train\\
L333 & 244 & 27.2508 & 0.8305 & train\\
L192 & 240 & 28.2896 & 0.8344 & train\\
L286 & 210 & 29.1479 & 0.8103 & train\\
L506 & 211 & 29.2489 & 0.8759 & test (both)\\
\midrule
\emph{S1 test, combined} & \emph{435} & \emph{27.8630} & \emph{0.8361} &\\
\emph{S2 test (L506)} & \emph{211} & \emph{29.2489} & \emph{0.8759} &\\
\bottomrule
\end{tabular}
\end{table}

% ============================================================================
\section{Experiments}
% ============================================================================

\subsection{Main results}
\label{sec:main-res}
Main results are in Table~\ref{tab:main}.

\begin{table*}[t]
\centering
\caption{Main results. Rows marked \emph{ours, same split} were trained by us
under the identical protocol. Our RED-CNN reaches 32.9471~dB on L506, within
0.02~dB of the independently published 32.93~dB for the same patient
--- an independent check that our implementation and evaluation convention
reproduce the reference result rather than merely resembling it.}
\label{tab:main}
\small
\begin{tabular}{@{}llrrr@{}}
\toprule
Split & Method & Params & PSNR & SSIM\\
\midrule
S1 & identity & --- & 27.8630 & 0.8361\\
S1 & \textbf{PR-LWT (ours)} & \textbf{2,159} & \textbf{30.8314 $\pm$ 0.0815} & \textbf{0.8762 $\pm$ 0.0011}\\
S1 & RED-CNN~\cite{chen2017redcnn} (ours, same split) & 1,848,865 & 31.8310 & 0.8824\\
\midrule
S2 & identity (L506) & --- & 29.2489 & 0.8759\\
S2 & \textbf{PR-LWT (ours)} & \textbf{2,159} & \textbf{31.9731 $\pm$ 0.0787} & \textbf{0.9041 $\pm$ 0.0008}\\
S2 & RED-CNN~\cite{chen2017redcnn} (ours, same split) & 1,848,865 & \textbf{32.9471} & \textbf{0.9090}\\
\midrule
S2 & RED-CNN~\cite{chen2017redcnn} (as published) & $\sim\!10^6$ & $\approx$32.93 & ---\\
S2 & CTformer~\cite{wang2023ctformer} (as published) & $\sim\!1.4\times10^6$ & $\approx$32.9 & ---\\
\bottomrule
\end{tabular}
\end{table*}

\textbf{The baselines marked \emph{ours} were trained by us under the identical
protocol} --- same splits, same patch size, batch size, optimiser, schedule and
evaluation --- so the comparison no longer rests on matching a published setup.
Two observations follow.

First, an \textbf{independent check of our protocol}: on the literature-aligned
split S2 our RED-CNN reaches \textbf{32.9471~dB on L506}, within \textbf{0.02~dB}
of the independently published 32.93~dB for the same patient~\cite{chen2017redcnn}.
Our implementation and our evaluation convention therefore reproduce the
reference result rather than merely resembling it.

Second, under that matched comparison, PR-LWT comes within \textbf{0.97~dB} (S2)
and \textbf{1.00~dB} (S1) of RED-CNN while using \textbf{857$\times$ fewer
parameters} (2,159 vs.\ 1,848,865). We state plainly that \textbf{PR-LWT does not
beat RED-CNN on PSNR}; the claim under test in this paper is about \emph{what
makes a learnable transform helpful}, not about attaining state of the art.
Section~\ref{sec:structure} revisits this comparison under a matched training
budget and matched data, where the distance becomes 0.85~dB with a seed-level
confidence interval --- and shows that no amount of capacity closes it. The
parameter/PSNR trade-off is plotted in Fig.~\ref{fig:pareto}, and a visual
comparison in Fig.~\ref{fig:visual}.

\subsection{Ablation: is invertibility the key?}
\label{sec:ablation}

\textbf{Three arms}, differing \emph{only} in the wavelet
(Table~\ref{tab:arms}). The arms answer two distinct questions: \emph{pr} vs.\
\emph{unconstrained} isolates the effect of \textbf{invertibility}; \emph{pr}
vs.\ \emph{fixed} isolates the effect of \textbf{learnability}.

\begin{table*}[t]
\centering
\caption{The three arms. Only the wavelet differs between them.}
\label{tab:arms}
\small
\begin{tabular}{@{}lllrr@{}}
\toprule
Arm & Wavelet & Learnable & Inv.\ guar. & Params\\
\midrule
\texttt{pr} & lifting scheme & \checkmark & \textbf{\checkmark} & 24\\
\texttt{unconstrained} & separate filt. & \checkmark & $\times$ & 64\\
\texttt{fixed} & orthonormal Haar & $\times$ & \checkmark & 0\\
\bottomrule
\end{tabular}
\end{table*}

Results (Fig.~\ref{fig:arms}; mean $\pm$ std over seeds; paired $t$-test with the
\textbf{training seed} as the independent unit) are in Tables~\ref{tab:arms-res}
and~\ref{tab:paired}.

\begin{table}[t]
\centering
\caption{Ablation results, PSNR in dB.}
\label{tab:arms-res}
\small
\begin{tabular}{@{}llrr@{}}
\toprule
Split & Arm & PSNR & $n$\\
\midrule
S1 & \texttt{pr} & \textbf{30.8314 $\pm$ 0.0815} & 5\\
S1 & \texttt{unconstrained} & 30.5341 $\pm$ 0.0305 & 5\\
S1 & \texttt{fixed} & 30.5591 $\pm$ 0.0256 & 5\\
\midrule
S2 & \texttt{pr} & \textbf{31.9731 $\pm$ 0.0787} & 3\\
S2 & \texttt{unconstrained} & 31.6788 $\pm$ 0.0307 & 3\\
S2 & \texttt{fixed} & 31.6936 $\pm$ 0.0394 & 3\\
\bottomrule
\end{tabular}
\end{table}

\begin{table*}[t]
\centering
\caption{Paired comparisons. The unit of analysis is the \textbf{training seed},
not the test slice: the test sets contain 435 (S1) / 211 (S2) slices drawn from
only 2 / 1 patients, and adjacent slices are highly correlated. Treating slices
as independent would inflate significance by orders of magnitude.}
\label{tab:paired}
\small
\begin{tabular}{@{}llrrrrrr@{}}
\toprule
Split & Comparison & $\Delta$ (dB) & $t$ & $p$ & 95\% CI & $d_z$ & $|\Delta|$/MDES\\
\midrule
S1 & \texttt{pr} $-$ \texttt{unconstrained} & \textbf{+0.2973} & 10.44 & \textbf{0.0005} & [+0.218, +0.376] & \textbf{+4.67} & \textbf{2.19}$\times$\\
S1 & \texttt{pr} $-$ \texttt{fixed} & \textbf{+0.2723} & 10.68 & \textbf{0.0004} & [+0.202, +0.343] & \textbf{+4.78} & \textbf{2.01}$\times$\\
S1 & \texttt{unconstrained} $-$ \texttt{fixed} & $-0.0251$ & $-2.02$ & 0.113 & [$-0.060$, \textbf{+0.009}] & $-0.90$ & 0.18$\times$\\
\midrule
S2 & \texttt{pr} $-$ \texttt{unconstrained} & \textbf{+0.2942} & 7.92 & \textbf{0.016} & [+0.134, +0.454] & \textbf{+4.57} & 1.21$\times$\\
S2 & \texttt{pr} $-$ \texttt{fixed} & \textbf{+0.2794} & 10.95 & \textbf{0.008} & [+0.170, +0.389] & \textbf{+6.32} & 1.15$\times$\\
S2 & \texttt{unconstrained} $-$ \texttt{fixed} & $-0.0148$ & $-1.25$ & 0.337 & [$-0.066$, \textbf{+0.036}] & $-0.72$ & 0.06$\times$\\
\bottomrule
\end{tabular}
\end{table*}

\subsubsection{Independently re-acquired data, ten seeds per arm}
\label{sec:replication}

Between the original experiments and this write-up the dataset had to be
\textbf{re-acquired from scratch}: the official AAPM Box distribution became
unreachable, the Kaggle mirror that replaced it was withdrawn mid-download, and
the copy used here was reassembled from a partial archive whose 3\,mm B30 region
contains holes (L067 is missing 13 slices, L506 one, giving a \textbf{421-slice}
test set instead of 435). Because the data therefore differs, every headline
result was re-run end to end.

This turned out to be useful: it is a \textbf{de facto replication}, and the two
protocol changes it forced are worth stating, because both are corrections.

\textbf{(a) The training protocol was not reproducible.} Random patch cropping
drew from an unseeded generator, so a given \texttt{--seed} did not reproduce a
run; repeated runs with the same seed differed by $\sim$0.04~dB. All numbers below
come from the corrected, seed-deterministic pipeline, which reproduces
\textbf{bit-identically} (verified: two runs of the same seed agree to ten decimal
places).

\textbf{(b) The re-run is better powered.} Ten seeds per arm instead of five,
which tightens the detection floor from \textbf{0.136~dB} to \textbf{0.074~dB}
(Fig.~\ref{fig:power-n10}). Fig.~\ref{fig:rep-arms} places the two acquisitions
side by side (Table~\ref{tab:repl-arms}); Fig.~\ref{fig:seed-conv} shows the
running mean and its 95\% CI as seeds accumulate.

\begin{table*}[t]
\centering
\caption{The same three arms on the two data acquisitions. The absolute levels are
not directly comparable across rows --- the re-acquired test set is 14 slices
smaller and its identity floor shifts accordingly (27.8630 $\to$ 27.9220~dB).
Every comparison we draw is like-for-like \emph{within} one acquisition.}
\label{tab:repl-arms}
\small
\begin{tabular}{@{}llrr@{}}
\toprule
Split & Arm & Original (435, $n$=5) & Re-acquired (421, $n$=10)\\
\midrule
S1 & \texttt{pr} & 30.8314 $\pm$ 0.0815 & \textbf{30.8575 $\pm$ 0.0740}\\
S1 & \texttt{unconstrained} & 30.5341 $\pm$ 0.0305 & 30.5427 $\pm$ 0.0460\\
S1 & \texttt{fixed} & 30.5591 $\pm$ 0.0256 & 30.5739 $\pm$ 0.0497\\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[t]
\centering
\caption{Paired comparisons on the re-acquired data ($n=10$ seeds), against the
original values. Both positive findings replicate with \emph{larger} effect
sizes. The third row changes \emph{character} and we report it as such.}
\label{tab:repl-paired}
\small
\begin{tabular}{@{}lrrrr@{}}
\toprule
Comparison & $\Delta$ (dB) & $p$ & $d_z$ & $|\Delta|$/MDES\\
\midrule
\texttt{pr} $-$ \texttt{unconstrained} & \textbf{+0.3148} & \textbf{$<$0.0001} & \textbf{+5.13} & \textbf{4.28}$\times$\\
\texttt{pr} $-$ \texttt{fixed} & \textbf{+0.2837} & \textbf{$<$0.0001} & \textbf{+5.04} & \textbf{3.85}$\times$\\
\texttt{unconstrained} $-$ \texttt{fixed} & \textbf{$-$0.0312} & \textbf{0.0163} & $-$0.93 & 0.68$\times$\\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig10_replication.png}
\caption{The two data acquisitions side by side (435 slices at $n=5$; 421 slices
at $n=10$). The ordering and the size of the gaps are unchanged; only the
absolute level shifts, with the identity floor.}
\label{fig:rep-arms}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig13_seed_conv.png}
\caption{Running mean and 95\% CI as seeds accumulate. The horizontal line is the
original $n=5$, 435-slice value --- the re-acquired data lands on it and the
interval narrows rather than drifting.}
\label{fig:seed-conv}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig14_power_n10.png}
\caption{Power analysis at $n=10$. The detection floor tightens from 0.136~dB to
\textbf{0.074~dB}, with the measured effects overlaid: the PR advantage sits
$3.9$--$4.3\times$ above the floor, the null gap at $0.68\times$ of it.}
\label{fig:power-n10}
\end{figure*}

\textbf{Both positive findings replicate, with larger effect sizes} ($d_z$ = 5.13
and 5.04 against 4.67 and 4.78), and the ordering is unchanged. The third row
\emph{changes character}, and we report it as such: on the original data the
\texttt{unconstrained} $-$ \texttt{fixed} gap was indistinguishable from zero;
here it is marginally significant \textbf{in the negative direction}.
Unconstrained learning is therefore not merely useless but slightly
\emph{harmful} --- though we would not press a $0.03$~dB effect that sits at
$0.68\times$ the detection floor.

\textbf{What this does and does not license.} The paired \emph{ratios} replicate
cleanly; the absolute levels are not directly comparable across the two
acquisitions, because the test set is 14 slices smaller and its identity floor
shifts accordingly (27.8630 $\to$ 27.9220~dB). Every comparison above is
like-for-like \emph{within} its own acquisition, which is the form in which we
use them.

\textbf{Three findings, identical on both splits:}
\begin{enumerate}
\item Imposing perfect reconstruction makes the learnable wavelet
\textbf{significantly better than fixed Haar} (+0.27~dB, $p<0.001$).
\item It also makes it \textbf{significantly better than unconstrained learning}
(+0.30~dB, $p<0.001$).
\item \textbf{Unconstrained learning is indistinguishable from fixed Haar}
($p=0.11$ / $0.34$; the CI straddles zero on both splits).
\end{enumerate}

\textbf{Effect sizes are large, not marginal.} The two significant comparisons
carry $d_z = +4.67$ and $+4.78$ (S1), far beyond the conventional ``large''
threshold of $0.8$. The claim is not that PR-LWT buys a small gain, but that it
moves the network by a large, consistent margin relative to seed-to-seed
variation.

\textbf{SSIM reproduces the same ordering.} The three-arm pattern is not a PSNR
artefact. On SSIM, \texttt{pr} reaches \textbf{0.8762 $\pm$ 0.0011} (S1) /
\textbf{0.9041 $\pm$ 0.0008} (S2), against 0.8725 / 0.8729 (S1) for the other two
arms: \texttt{pr} $-$ \texttt{unconstrained} $= \textbf{+0.0037}$
($p = 0.0008$, $d_z = +4.07$) and \texttt{pr} $-$ \texttt{fixed} $= \textbf{+0.0033}$
($p = 0.0016$, $d_z = +3.40$), with the same direction on S2 ($+0.0029$ both,
$p = 0.030$ / $0.015$).

One honest wrinkle: on SSIM the \texttt{unconstrained} $-$ \texttt{fixed} gap is
\emph{marginally} significant in the \textbf{negative} direction on S1
($-0.0004$, $p = 0.039$), whereas on PSNR it was not ($p = 0.113$); on S2 it is
exactly zero ($+0.0000$, $p = 0.985$). Unconstrained learning is therefore never
\emph{better} --- on one split it is detectably \emph{worse} --- but we do not
read a stable effect into a $4\times10^{-4}$ SSIM difference that vanishes on the
other split.

\begin{quote}
\small\textbf{Comparability note.} SSIM is computed with our own implementation
(\texttt{gaussian\_weights=True}, $\sigma = 1.5$, window 11,
\texttt{data\_range} $= 400$), used identically for every arm and for our RED-CNN
reproduction, so all comparisons \emph{within} this paper are like-for-like. Its
agreement with the SSIM of the reference RED-CNN/CTformer code was \textbf{not}
independently verified, so we do not compare our SSIM against \emph{published}
SSIM values. The PSNR protocol, by contrast, was verified against an independent
measurement (Section~\ref{sec:main-res}).
\end{quote}

\textbf{What this experiment can and cannot detect (Fig.~\ref{fig:power}).}
Because the null result carries much of the argument, we quantify the design's
resolution directly. For a paired $t$-test at 80\% power, the minimum detectable
effect (MDES) at $n=5$ is \textbf{0.136~dB} (S1); at $n=3$ it is 0.244~dB (S2)
--- the $|\Delta|$/MDES denominators in Table~\ref{tab:paired}. Two consequences
follow. The \textbf{PR benefit is detectable}: $+0.272$~dB is $2.0\times$ the S1
detection floor. The \textbf{null gap is not}: the $0.025$~dB
\texttt{unconstrained}$-$\texttt{fixed} gap sits at $0.18\times$ that floor,
i.e.\ $5.6\times$ \emph{below} what $n=5$ can resolve.

This sharpens Finding~3 beyond ``we could not distinguish them''. The correct
statement is a \textbf{bound}: any genuine difference between unconstrained
learning and fixed Haar is \emph{smaller than 0.14~dB}, whereas the benefit PR
delivers is $0.27$--$0.30$~dB --- roughly \textbf{twice the entire resolution of
the experiment}. The negative result is therefore not an artefact of a blind
experiment; the experiment was sharp enough to see the effect PR produces, and
saw nothing in its absence.

\textbf{Relation to WINNet's splitting-operator ablation.}
WINNet~\cite{huang2022winnet} reports the opposite conclusion for a superficially
similar comparison: a learned, orthogonality-constrained splitting operator
matched a fixed DCT on natural-image AWGN denoising, so they defaulted to the
fixed one. Our setting differs along several axes at once --- task (LDCT vs.\
natural-image AWGN), transform family (a 2-level lifting parametrization
\emph{initialized at} Haar, so learning departs from the fixed baseline rather
than searching over all orthogonal matrices of a matched size), and evaluation
(five-seed paired tests with confidence intervals vs.\ a default-picking
ablation). We cannot attribute the discrepancy to any one of these from the
published numbers alone, and we flag it rather than claim a general conclusion.
What this experiment does establish is narrower, and we believe robust:
\textbf{within this architecture and task, the PR constraint is what makes
learnability pay.}

\subsection{Mechanism: what unconstrained learning actually does}
\label{sec:mech}

We measure the analysis--synthesis round-trip error of the \textbf{trained}
transforms on their final checkpoints (Table~\ref{tab:rt}, and
Fig.~\ref{fig:mech}a), over ten seeds per arm on S1.

\begin{table*}[t]
\centering
\caption{Round-trip error (relative $L_2$) of the trained transforms, on the
final checkpoint of each arm. The probe uses a fixed random input, so this is a
property of the trained transform rather than of the test set; the seed count is
therefore the only thing that matters, and S1 has $n=10$. The \texttt{fixed} arm
has no learned parameters and is seed-invariant by construction.}
\label{tab:rt}
\small
\begin{tabular}{@{}lrrr@{}}
\toprule
Arm & Wavelet params & S1 ($n$=10) & S2 ($n$=3)\\
\midrule
\texttt{pr} & 24 & \textbf{1.62e-07} $\pm$ 0.01e-07 & \textbf{1.65e-07}\\
\texttt{fixed} & 0 & 1.64e-07 (seed-invariant) & 1.64e-07\\
\texttt{unconstrained} & 64 & \textbf{1.35e-01} $\pm$ 0.19e-01 & \textbf{1.45e-01}\\
\bottomrule
\end{tabular}
\end{table*}

Unconstrained learning drives the transform to a state where \textbf{11--18\% of
the signal is lost in a forward--backward pass} (S1 mean 13.5\%, per-seed
11.4--17.9\%; S2 mean 14.5\%, per-seed 11.9--16.4\%), against
$1.6\times10^{-7}$ --- the float32 limit --- for the two invertible arms. That
gap spans \textbf{5.9 orders of magnitude}; the network must then denoise
\emph{and} compensate for its own transform.

\textbf{The comparison above is correlational.} The unconstrained arm both loses
invertibility \emph{and} performs like fixed Haar; that alone does not show the
former causes the latter. We therefore \textbf{intervene directly}. Holding
architecture, capacity and training protocol fixed, we inject a controlled
round-trip error into the \textbf{PR} arm by having its synthesis step use
$(1-\varepsilon)\cdot U$ while analysis still uses $U$ --- reproducing, in
isolation, exactly the defect the unconstrained arm develops on its own
(Table~\ref{tab:interv}, Fig.~\ref{fig:mech}b):

\begin{table*}[t]
\centering
\caption{Intervention: forcing a controlled round-trip error into the PR arm.
Every dose is significant (paired $t$-test, independent unit = training seed,
$n=5$). At $8.7\%$ error the PR arm lands on the fixed-Haar level (30.5591~dB).}
\label{tab:interv}
\small
\begin{tabular}{@{}lrrrr@{}}
\toprule
Injected $\varepsilon$ & Measured RT & PSNR (S1) & $\Delta$ & Paired $p$\\
\midrule
0 (control) & 1.6e-07 & 30.8314 $\pm$ 0.0815 & --- & ---\\
0.111 & 5.5e-02 & 30.7142 $\pm$ 0.0508 & $-0.117$ & \textbf{0.0067}\\
0.222 & 8.7e-02 & 30.5500 $\pm$ 0.0618 & $-0.281$ & \textbf{0.0010}\\
0.444 & 1.2e-01 & 30.2850 $\pm$ 0.0459 & $-0.546$ & \textbf{$<$0.0001}\\
\bottomrule
\end{tabular}
\end{table*}

Performance falls \textbf{monotonically} with the injected error, and every dose
is significant. At \textbf{8.7\%} round-trip error the PR arm lands at
\textbf{30.5500~dB} --- on the fixed-Haar level (30.5591), the performance of an
arm that never learned anything. The intervention therefore supports the
mechanism \textbf{causally}: an error of this magnitude is not merely correlated
with the failure, it is \emph{sufficient} to produce it.

\textbf{Honest boundary.} The dose--response is somewhat \emph{steeper} than the
cross-arm comparison: at 11.8\% error our injected arm reaches 30.2850~dB, below
both the unconstrained arm (30.5341) and fixed Haar (30.5591). The unconstrained
arm's mismatch lies in \emph{both} its analysis and synthesis filters, whereas
our injection perturbs synthesis only --- the two defects are of the same
magnitude but not of the same structure. We therefore claim \textbf{sufficiency
at the same order of magnitude}, not exact reproduction of that arm's error.

PR-LWT keeps the round-trip error at the float32 limit while remaining fully
adaptive. \textbf{This is the entire mechanism.}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig2_mech.png}
\caption{\textbf{(a)}~Round-trip error of the trained transforms ($n=10$ seeds on
S1; error bars are std) on a fixed random probe: the two invertible arms sit at
the float32 limit, the unconstrained arm 5.9 orders of magnitude above them.
\textbf{(b)}~Intervention:
injecting that error into the PR arm degrades it monotonically, crossing both the
fixed-Haar and the unconstrained reference levels. The square marks the
unconstrained arm's own operating point.}
\label{fig:mech}
\end{figure*}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig1_arms.png}
\caption{Ablation: the reconstruction constraint is what makes a learnable
wavelet pay off. Dashed line is the identity floor on the S1 test set.}
\label{fig:arms}
\end{figure}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig8_power.png}
\caption{What this experiment can and cannot detect. Minimum detectable effect
(80\% power, paired $t$-test) as a function of the number of training seeds.
The observed PR benefit ($+0.272$~dB) lies \emph{above} the $n=5$ detection
floor ($0.136$~dB), while the observed null gap ($0.025$~dB) lies
$5.6\times$ \emph{below} it --- so the negative result is a bound, not a blind
spot.}
\label{fig:power}
\end{figure*}

\subsection{What the transform actually learns, and where the bound bites}
\label{sec:learns}

The results so far say the PR constraint is \emph{sufficient} to make learning
pay. They do not say what the learned transform looks like, or how tight the
bound must be. Two experiments address this.

\textbf{The task has a preferred operating point, and it is not Haar}
(Fig.~\ref{fig:e3}). We initialise the lifting taps at a controlled distance $d$
from the Haar initialisation ($\theta_0 = \operatorname{atanh}(d/\beta)$, so
$|P - P_{\text{init}}| \approx d$) and measure where training leaves them
(Table~\ref{tab:e3}):

\begin{table}[t]
\centering
\caption{Initialisation distance from Haar $\to$ drift after training. Over the
range where the initial transform is numerically intact the drift converges to a
narrow band; at $d=4$ the forward pass is already broken and training cannot move
at all.}
\label{tab:e3}
\small
\begin{tabular}{@{}lrr@{}}
\toprule
Initial drift $d$ & Drift after training & TEST PSNR (dB)\\
\midrule
0.00 (Haar) & \textbf{0.775} & \textbf{31.034}\\
0.50 & 0.709 & 30.983\\
1.00 & 0.773 & 30.776\\
2.00 & 0.654 & 30.578\\
4.00 & 4.000 \emph{(stuck)} & \textbf{3.292}\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig16_e3_initdrift.png}
\caption{\textbf{(a)}~Training pulls every numerically intact starting point into
a narrow band of drift from Haar (0.654--0.775), against an initial span of
2.0; starting \emph{at} Haar the taps are pushed \emph{away} to 0.775, so the
gradient does not consider Haar optimal. \textbf{(b)}~The failure mode is a
cliff, not a slope: from $d=4$ the forward transform already emits noise
(round-trip $4.3\times10^{1}$) and training cannot move at all --- drift stays at
exactly 4.000 and the output is unrelated to the input.}
\label{fig:e3}
\end{figure*}

Over $d \le 2$, training converges to a \textbf{narrow band of drift,
0.654--0.775} (span 0.12 against an initial span of 2.0; $r = -0.75$, i.e.\ both
directions converge inward). Starting \emph{at} Haar, training pushes the taps
\textbf{away} to 0.775 --- the gradient does not consider Haar optimal. The honest
reading is that \textbf{the optimal wavelet is slightly off Haar, near drift
$\approx 0.7$}, which is also why ``learning'' buys so little: there is little to
learn. We state this as the measured behaviour it is; the residual correlation
with the starting point ($r = -0.75$, not $-1$) shows the attractor is soft, not
exact.

\textbf{Numerical collapse is a cliff, not a slope} (Fig.~\ref{fig:e3}b). At
$d = 4$ the initial transform's round-trip error is already $4.3\times10^{1}$ ---
the forward pass emits noise. Training then does \textbf{nothing}: the drift stays
at exactly 4.000 and the network reaches \textbf{3.29~dB}, i.e.\ output unrelated
to the input. There is no gradient signal to recover from, because the
coefficients the network receives are already garbage. Contrast $d = 2$
(round-trip $1.4\times10^{-2}$), which recovers to 0.654 and 30.578~dB. This is
why bounding is \textbf{necessary} rather than merely prudent: crossing the cliff
is irreversible \emph{within training}.

\textbf{The default bound is tighter than it needs to be}
(Figs.~\ref{fig:bound},~\ref{fig:bounddrift}). Sweeping $\beta$
(Table~\ref{tab:bound}):

\begin{table*}[t]
\centering
\caption{Bound sweep. $\Delta$ and $p$ are paired $t$-tests on the \textbf{matched
three-seed subset} (seeds 0--2), because only the default level was run with more.
The default row reports all ten, and its 3-seed subset (drift 0.228, 30.885~dB)
sits within 0.03~dB of the $n$=10 estimate, so the baseline is not
cherry-picked. Only $\beta = 1.0$ and $2.0$ are individually significant;
the $n=3$ levels put the \emph{location} of the peak out of reach. The bound does
not clip the drift (utilisation is 46\% at $\beta=0.5$); it limits how far the
optimiser searches.}
\label{tab:bound}
\small
\begin{tabular}{@{}lrrrrr@{}}
\toprule
Bound $\beta$ & $n$ & Drift & TEST PSNR (dB) & $\Delta$ vs default & $p$\\
\midrule
\textbf{0.5 (default)} & 10 & 0.230 & 30.858 & --- & ---\\
1.0 & 3 & 0.293 & 30.986 & \textbf{+0.101} & \textbf{0.038}\\
2.0 & 3 & 0.371 & 31.036 & \textbf{+0.151} & \textbf{0.029}\\
4.0 & 3 & 0.651 & 31.050 & +0.165 & 0.058\\
8.0 & 3 & 0.775 & 31.034 & +0.149 & 0.062\\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig11_bound_curve.png}
\caption{Performance against the bound $\beta$. We deliberately title this
cautiously --- ``performance is insensitive to the bound value'' --- because at
$n=3$ only $\beta = 1.0$ and $2.0$ are individually better than the default.}
\label{fig:bound}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig17_bound_drift.png}
\caption{The mechanism chain: bound $\to$ drift $\to$ PSNR. The horizontal axis is
the \emph{realised} drift rather than the bound, so the independent E3
measurements of Fig.~\ref{fig:e3} can be overlaid directly. The default
\texttt{bound}$=0.5$ pins the optimiser at drift 0.23, well below the 0.65--0.78
plateau the task prefers.}
\label{fig:bounddrift}
\end{figure*}

At $\beta = 0.5$ --- the value used everywhere else in this paper --- the
optimiser settles at drift 0.23, far below the $\approx 0.7$ the model prefers,
costing \textbf{+0.10 to +0.15~dB}. That is roughly \textbf{half of the entire PR
effect}, for one hyper-parameter.

\textbf{A hypothesis we tested and rejected} (Fig.~\ref{fig:e1}). One natural
explanation for the whole negative result is that the heads can \emph{substitute}
for the transform: give them enough capacity and a good wavelet stops mattering.
We tested this by varying head capacity (\texttt{--n-conv}, 94 $\to$ 2{,}159
$\to$ 18{,}399 parameters) and measuring the PR $-$ fixed gap
(Table~\ref{tab:e1}):

\begin{table}[t]
\centering
\caption{The PR advantage does not shrink as the heads grow --- the opposite of
substitution. Endpoints have $n=3$, so we do not claim the upward trend itself;
what is supported is that the advantage persists across two orders of magnitude
of head capacity.}
\label{tab:e1}
\small
\begin{tabular}{@{}lrr@{}}
\toprule
Head capacity & $\Delta$(PR $-$ Fixed Haar) & $n$\\
\midrule
94 & +0.186 & 3\\
2,159 & +0.284 & 10\\
18,399 & +0.304 & 3\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig15_e1_capacity.png}
\caption{A \textbf{negative} result, shown as measured. The hypothesis was that
the transform matters more when the heads are small; the measured direction is
the opposite and monotone (+0.186 at 94 parameters, +0.284 at 2{,}159, +0.304 at
18{,}399). The endpoints carry $n=3$ only, so this figure is used to
\emph{reject} substitution, not to establish a trend.}
\label{fig:e1}
\end{figure*}

The gap \textbf{grows} with capacity rather than shrinking ($r = +0.968$ on
$\log_{10}$ params). The defensible statement is a \textbf{robustness} one: the PR
advantage persists across two orders of magnitude of head capacity (always
0.19--0.30~dB); it neither vanishes when the heads grow nor amplifies when they
shrink. This rules out ``the transform is just covering for weak heads'' as an
explanation.

\subsection{Where the accuracy comes from: structure, not capacity}
\label{sec:structure}

Section~\ref{sec:learns} showed that head capacity does not \emph{substitute} for
the transform. This section asks the complementary question --- \emph{what does
buy accuracy?} --- and reports a systematic sweep of the four architectural
degrees of freedom that had never been varied, together with a re-run of every
headline number at $3\times$ the training budget.

\begin{quote}
\small\textbf{Cohort note.} Everything in this section is measured on the
re-acquired 421-slice test set (Section~\ref{sec:replication}), and its
30-epoch reference is the $n=10$ \texttt{fix\_*} batch on that same data ---
\emph{not} the 435-slice numbers in Tables~\ref{tab:main}--\ref{tab:paired}.
Mixing the two cohorts shifts values by 0.03--0.06~dB, which is large relative
to some of the effects below.
\end{quote}

\textbf{Longer training widens the gap rather than compressing it.} The
limitation that every run stopped at 30 epochs with the best epoch equal to the
last one is testable. We re-ran all three arms at 90 epochs ($n=5$,
Table~\ref{tab:conv}):

\begin{table}[t]
\centering
\caption{Three arms at $3\times$ the training budget, on the same 421-slice data.
The reference column is the same cohort at 30 epochs, so the only thing that
changes across the table is the budget.}
\label{tab:conv}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lrr@{}}
\toprule
Arm & 30 ep ($n$=10) & 90 ep ($n$=5)\\
\midrule
\texttt{pr} & 30.8575 $\pm$ 0.0740 & \textbf{31.0752 $\pm$ 0.0973}\\
\texttt{unconstrained} & 30.5427 $\pm$ 0.0460 & 30.7147 $\pm$ 0.0373\\
\texttt{fixed} & 30.5739 $\pm$ 0.0497 & 30.7403 $\pm$ 0.0177\\
\bottomrule
\end{tabular}
\end{table}

The gaps between arms therefore move as follows. \texttt{pr} $-$ \texttt{fixed}
grows from $+0.2837$ to $\mathbf{+0.3349}$~dB ($p=0.0017$, $d_z=+3.37$);
\texttt{pr} $-$ \texttt{unconstrained} grows from $+0.3148$ to
$\mathbf{+0.3604}$~dB ($p=0.0003$, $d_z=+5.09$); and
\texttt{unconstrained} $-$ \texttt{fixed} is essentially unchanged, from
$-0.0312$ to $-0.0255$~dB ($p=0.23$). The concern was that the 30-epoch
comparison might be an artefact of an arbitrary stopping point. It is not: the
effect \emph{grows} with budget, and the null result is remarkably stable across
a $3\times$ change in schedule.

\emph{Are the arms converged at 90 epochs?} Not formally, but the remaining
headroom is negligible. For \texttt{pr}, the per-5-epoch validation gain falls
from $+0.0950$~dB (epochs 0--30) to $+0.0340$ (30--60) to $+0.0069$ (60--90), a
$14\times$ collapse; the final 30 epochs add only 0.041~dB. We therefore call
this convergence in practice, not convergence in proof.

\textbf{Four architectural levers, three of which do nothing.} We swept the
degrees of freedom that had never been varied: \texttt{global\_residual} was
\texttt{False} in all 203 prior runs, and \texttt{levels}, \texttt{mid\_ch} and
\texttt{n\_taps} had each only ever taken a single value
(Table~\ref{tab:levers}).

\begin{table}[t]
\centering
\caption{Architecture sweep. Only the full-resolution residual branch pays, and
it costs 305 parameters. The other three either hurt or do nothing --- two
negative results we report as such.}
\label{tab:levers}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lrrl@{}}
\toprule
Lever & Params & $\Delta$ PSNR & Verdict\\
\midrule
\texttt{--global-residual} & 2,159$\to$\textbf{2,464} & \textbf{+0.3515} & large, cheap\\
\texttt{--mid-ch 32} & 2,159$\to$4,287 & +0.094 & poor for the cost\\
\texttt{--levels 3} & 2,159$\to$3,086 & \textbf{$-$0.079} & \textbf{hurts}\\
\texttt{--n-taps 5} & 2,159$\to$2,175 & \textbf{$-$0.001} & \textbf{nothing}\\
\bottomrule
\end{tabular}
\end{table}

The \texttt{global\_residual} effect is $+0.3515$~dB at $n=5$
($p=1.2\times10^{-6}$, $d_z=+21.2$). Two negative results are worth stating:
finer frequency decomposition is \emph{not} better here, and longer lifting
filters are irrelevant. Combining the useful lever with \texttt{--mid-ch 32}
reaches $31.6275 \pm 0.0253$ at 4,896 parameters ($+0.4365$, $n=5$,
$p=2.1\times10^{-6}$).

\textbf{Then the returns collapse.} Scaling the network further buys almost
nothing:

\begin{table}[t]
\centering
\caption{The capacity ceiling. On a like-for-like seed-0 basis, the first 305
parameters are worth $+0.3478$~dB and the next 265,792 are worth $+0.2597$~dB ---
$871\times$ the cost for a smaller gain.}
\label{tab:ceiling}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lrrr@{}}
\toprule
Configuration & Params & PSNR & $\Delta$\\
\midrule
\texttt{+global-residual} & 2,464 & 31.5706 & ---\\
\texttt{+ mid\_ch 64} & 9,760 & 31.6912 & +0.121\\
\texttt{+ n\_conv 3} & 18,704 & 31.6847 & +0.114\\
\texttt{+ mid\_ch 32, n\_conv 3} & 69,632 & 31.8165 & +0.246\\
\texttt{+ mid\_ch 64, n\_conv 3} & 268,256 & 31.8303 & +0.260\\
\bottomrule
\end{tabular}
\end{table}

This is the quantitative form of the paper's thesis: on this task, accuracy comes
from the \emph{structure} of the transform, not from the capacity of the network
around it. It points the same way as the substitution experiment above.

\textbf{A matched comparison to RED-CNN, at equal budget and equal data.} The
single-seed, cross-budget RED-CNN comparison in Section~\ref{sec:main-res} is not
a fair one, so we retrained RED-CNN to the full 90-epoch budget on the same
421-slice split, at $n=3$ seeds (Table~\ref{tab:redcnn90}):

\begin{table*}[t]
\centering
\caption{Matched-budget, matched-data comparison. Paired by seed ($n=3$),
RED-CNN leads the 2,464-parameter model by 0.8491~dB (95\% CI [0.723, 0.975],
$p=0.0012$). The distance is essentially budget-insensitive: extending training
helped both methods similarly.}
\label{tab:redcnn90}
\small
\begin{tabular}{@{}lrrr@{}}
\toprule
Method & Params & PSNR & Dist.\ to RED-CNN\\
\midrule
RED-CNN ($n$=3) & 1,848,865 & 32.0572 $\pm$ 0.0401 & ---\\
PR-LWT, $\beta$=2.0 (+ \texttt{gr}) & \textbf{2,464} & 31.5425 $\pm$ 0.0223 & \textbf{0.515 dB}\\
PR-LWT, $\beta$=2.0 (no \texttt{gr}) & 2,159 & 31.1910 $\pm$ 0.0340 & 0.866 dB\\
\bottomrule
\end{tabular}
\end{table*}

We report the honest reading: \textbf{PR-LWT does not close the gap to
RED-CNN}, and it does not close it by scaling either --- Table~\ref{tab:ceiling}
leaves 0.23~dB unclosed at 268K parameters.

\textbf{A stability difference, not an accuracy difference.} One further
comparison favours the constrained design and is independent of PSNR. Across the
validation curves, the constrained arms are far better behaved: the median
validation range is \textbf{0.433~dB} for PR-LWT (15 curves, worst 0.675) against
\textbf{4.243~dB} for RED-CNN (3 curves, worst 11.246). RED-CNN's curve
transiently collapses --- to 19.9~dB --- and recovers; PR-LWT's never does. This
is consistent with the paper's thesis, but with three curves we state it as an
observation rather than a measured property.

% ============================================================================
\section{Discussion and Limitations}
% ============================================================================

\textbf{Why we do not claim ``lossless''.} Perfect reconstruction guarantees a
round-trip only when the coefficients are \emph{unmodified}. Denoising modifies
them by definition, so PR does \textbf{not} imply a lossless denoiser. Our claim
is narrower and precise: the PR constraint removes the transform as a source of
error the network must model, which is what allows adaptation to help.

\textbf{Limitations.}
\begin{enumerate}
\item \textbf{Single dataset} (AAPM-Mayo). Generalisation to other CT data is
untested --- including LoDoPaB-CT~\cite{leuschner2021lodopab}, which we note is a
\emph{reconstruction} benchmark (its observations are sinograms) and therefore
not directly usable as a zero-shot denoising test without an FBP step.
\item \textbf{Small test cohort} (2 patients in S1, 1 in S2). This is why we
report per-patient results and use seeds as the unit of analysis. For scale, the
\emph{patient-to-patient} identity-floor spread is \textbf{3.75~dB}
(Table~\ref{tab:floor}) --- an order of magnitude larger than any effect reported
here --- which is precisely why the seed, not the slice, is the unit of analysis.
\item \textbf{Same-split baselines cover RED-CNN only.} RED-CNN was retrained by
us under our own splits and protocol (Section~\ref{sec:main-res}); CTformer and
the remaining methods are still quoted from the literature, and the
training-set differences noted there apply to them. Retraining further baselines
was out of scope here.
\item \textbf{The 30-epoch budget truncates before convergence --- tested, and it
does not change the conclusion.} The main tables compare the arms at a fixed
30-epoch budget, not at their optima. We re-ran all three arms at 90 epochs
(Section~\ref{sec:structure}): the PR advantage \textbf{widens} rather than
compresses ($+0.2837 \to +0.3349$~dB over \texttt{fixed}), and the null result is
stable ($-0.0312 \to -0.0255$~dB). The arms are still marginally improving at
epoch 90 (per-5-epoch gain $+0.0069$~dB, a $14\times$ collapse from the opening
phase), so this is convergence in practice rather than in proof. The main tables
are left at 30 epochs for internal consistency with
Tables~\ref{tab:main}--\ref{tab:paired}; readers should treat the effect as
\emph{at least} the quoted size.
\item \textbf{Same-split baselines cover RED-CNN only.} RED-CNN has now been
retrained at the matched 90-epoch budget on the matched data at $n=3$ seeds
(Section~\ref{sec:structure}), which makes a seed-level paired test possible; the
resulting distance is 0.85~dB (95\% CI [0.72, 0.98]). The remaining concern is
not the seed count but the \emph{choice} of baseline: RED-CNN is from 2017, and
no modern lightweight method has been reimplemented under our protocol. CTformer
and the others are still quoted from the literature, and --- as
Section~\ref{sec:eval} documents --- cross-paper PSNR on this dataset carries a
$\sim$2~dB noise floor, which is \textbf{larger than every effect reported in
this paper}. That is why no literature-quoted number appears in any comparison we
draw, and why the efficiency claim in Section~\ref{sec:structure} is stated
against a single same-protocol baseline rather than against a Pareto frontier of
published methods.
\item \textbf{The injected defect is structurally simpler than the real one.} We
perturb the synthesis filter only, whereas the unconstrained arm's two filter
banks drift apart independently. The intervention establishes sufficiency at
matched magnitude (Section~\ref{sec:mech}), not exact reproduction.
\item \textbf{The PR arm's seed-to-seed spread is numerically larger}
(SD 0.0815 vs.\ 0.0305 and 0.0256~dB), though a Levene test does not confirm
heteroscedasticity at $n=5$ ($p=0.167$ S1, $0.630$ S2). We flag it rather than
claim it: if real, PR-LWT's advantage carries a modestly higher training-variance
cost. More seeds are needed to settle this.
\item \textbf{The bound was tuned against a measurement that is only significant
at $n = 3$} (Section~\ref{sec:learns}). Our claim that $\beta = 0.5$ is worse than
1.0--2.0 rests on three seeds per level; the $+0.10$--$+0.15$~dB it implies
should be treated as provisional until replicated with more seeds. We report it
because the direction is consistent across all four comparisons, not because it
is settled.
\item \textbf{Our head-capacity experiment is underpowered at both ends} ($n = 3$
at 94 and 18{,}399 parameters; $n = 10$ only at the default 2{,}159). It is enough
to \emph{reject} substitution, which is what we use it for, but not to support the
apparent upward trend in Section~\ref{sec:learns}.
\item \textbf{The training protocol itself was corrected during this work.} Random
patch cropping originally drew from an unseeded generator, so a given
\texttt{--seed} did not reproduce a given run. All results reported here are from
the corrected, seed-deterministic pipeline (Section~\ref{sec:replication}); the
correction changes individual numbers by $\sim$0.01--0.05~dB and, more
importantly, \textbf{reduces the variance} that previously masked the bound effect.
\item \textbf{No downstream task} (e.g.\ segmentation) is evaluated.
\end{enumerate}

\begin{figure*}[t]
\centering
\includegraphics[width=0.92\textwidth]{fig3_visual.png}
\caption{Visual comparison on patient L506. Top: low-dose input, identity, our
result, and the full-dose reference. Bottom: absolute error against the
reference, in HU.}
\label{fig:visual}
\end{figure*}

\begin{figure}[!tbp]
\centering
\includegraphics[width=\columnwidth]{fig4_pareto.png}
\caption{Parameter efficiency on L506. The dotted line is the published RED-CNN
score; our same-split reproduction lands within 0.02~dB of it.}
\label{fig:pareto}
\end{figure}

% ============================================================================
\section{Conclusion}
% ============================================================================

We asked whether making the wavelet learnable helps LDCT denoising. By default,
\textbf{it does not} --- an unconstrained learnable wavelet performs no better
than a fixed Haar basis (and, on the corrected pipeline, marginally worse),
because learning destroys the transform's invertibility. Constraining the wavelet
to be perfectly reconstructible by construction --- via a lifting scheme whose
inverse shares the forward filters --- makes adaptation pay off, for a network of
only 2,159 parameters.

Two further findings sharpen the picture. The transform does learn \emph{something},
but not much: from any numerically intact starting point, training converges to a
narrow band of drift from Haar ($\approx 0.65$--$0.78$), which is why the payoff
is modest. And the numerical failure mode is a \textbf{cliff rather than a slope}
--- once the forward transform's round-trip error exceeds the signal, the
coefficients are noise, the gradient carries no information, and training cannot
recover. That is the precise sense in which the bound is necessary: not because
performance degrades smoothly without it, but because crossing the boundary is
irreversible within training.

The lesson generalises beyond this task: \textbf{when a learned component is meant
to be invertible, invertibility should be structural rather than hoped for} --- and
the numerical envelope that makes that structure usable deserves to be stated
explicitly, not left to a default.

% ---------------------------------------------------------------------------
\section*{Reproducibility}
Every number in this paper comes from a completed training run. The structure is
as follows, with the tag prefix each run carries in \texttt{experiments/runs/}:

\begin{table*}[t]
\centering
\caption{Run structure behind every number in this paper. The prefix is the tag
each run carries in \texttt{experiments/runs/}.}
\label{tab:runs}
\small
\begin{tabular}{@{}llr@{}}
\toprule
Study & Design & Runs\\
\midrule
Original, S1 (\texttt{w3\_}) & 3 arms $\times$ 5 seeds & 15\\
Original, S2 (\texttt{lit\_}) & 3 arms $\times$ 3 seeds & 9\\
Replication, S1 (\texttt{fix\_}) & 3 arms $\times$ 10 seeds & 30\\
Bound sweep (\texttt{fix\_bnd}, \texttt{new\_bnd}) & 5 levels $\times$ 3 seeds$^{\dagger}$ & 29\\
Init-drift, E3 (\texttt{e3\_}) & 5 levels $\times$ 3 seeds & 15\\
Head capacity, E1 (\texttt{e1\_}) & 2 settings $\times$ 3 arms $\times$ 3 seeds & 18\\
\bottomrule
\end{tabular}
\end{table*}

$^{\dagger}$The default level additionally carries seven more seeds, reported in
the main replication; the extra batch \texttt{new\_bnd} is missing one run.

Each run writes its full configuration (data sizes, parameter breakdown, LR
milestones, code revision) to \texttt{config.json} and per-slice metrics to
\texttt{results.json}; the tags above are the only lookup needed to regenerate any
table here. Round-trip measurements come from
\texttt{verify\_roundtrip.py --checkpoints}. We do not claim a single aggregate
run count, because the exploratory runs also present in that directory are not
part of any result reported. Code and configurations will be released upon
acceptance.

% ============================================================================
\section*{Supplementary figures}
% ============================================================================

This section carries the figures that support the statistical argument but are
not cited in the body, so that every figure generated for this study is
reproducible from the manuscript alone. They are ordered by the question they
answer, not by figure number.

\begin{figure*}[t]
\centering
\includegraphics[width=0.82\textwidth]{fig5_slice_dist.png}
\caption{\textbf{(a)}~Per-seed PSNR distribution across test slices (half-violin
with jittered points; black bars are quartiles and median). The three arms'
distributions overlap almost completely, so the effect is not carried by a
handful of slices. \textbf{(b)}~Variance decomposition: the \emph{per-slice} SD
(1.75--1.80~dB) exceeds the \emph{between-seed} SD (0.030--0.087~dB) by
\textbf{20--60$\times$}. This is the direct justification for using the seed
rather than the slice as the unit of analysis.}
\label{fig:slice-dist}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.82\textwidth]{fig6_convergence.png}
\caption{Convergence dynamics: \textbf{(a)}~training $L_1$ and \textbf{(b)}~validation
PSNR, three arms, mean $\pm$ 1 SD over five seeds. The two panels use
\emph{separate} $x$ axes --- validation runs every fifth epoch, so it has six
points against the training curve's thirty; plotting both on one axis would
compress the time axis fivefold. PR-LWT is ahead on both metrics (final-epoch
training $L_1$ 0.003346 against 0.003396 and 0.003414). The dashed line in (b)
marks that \emph{every} seed's best epoch is the last one --- i.e.\ training has
not converged, which is limitation~4.}
\label{fig:convergence}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.82\textwidth]{fig7_forest.png}
\caption{Effect sizes for all six paired comparisons: $\Delta$ with 95\% CI. The
grey band is the MDES interval at that $n$ --- the range the design cannot
resolve. The two null results fall entirely inside the band; the two significant
results have intervals wholly outside it.}
\label{fig:forest}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.82\textwidth]{fig9_perpatient.png}
\caption{\textbf{(a)}~Per-patient consistency: the effect direction is the same on
L506 ($n=211$) and L067 ($n=224$). \textbf{(b)}~The two patients' identity floors
differ by \textbf{2.69~dB}, larger than any effect reported in this paper --- which
is why results are given per patient and tested per seed.}
\label{fig:perpatient}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.82\textwidth]{fig12_effect_forest.png}
\caption{The original study against the replication: $\Delta$ and 95\% CI for each
comparison, with each one's MDES band. The paired ratios replicate; only the
absolute levels shift, with the identity floor.}
\label{fig:effect-forest}
\end{figure*}

% Flush pending floats here. Without this, the supplementary figures -- whose
% section appears just above -- are deferred past the bibliography and end up
% printed after the references (observed: figures on pp. 14-17, REFERENCES on
% pp. 13-14). A \clearpage makes the ordering match the source.
\clearpage

% ============================================================================
\begin{thebibliography}{33}
% ============================================================================

\bibitem{chen2017redcnn} H. Chen, Y. Zhang, W. Zhang, P. Liao, K. Li, J. Zhou,
and G. Wang, ``Low-dose CT via convolutional neural network,'' \emph{Biomed.
Opt. Express}, vol.~8, no.~2, pp.~679--694, 2017.

\bibitem{yang2018wganvgg} Q. Yang, P. Yan, Y. Zhang, H. Yu, Y. Shi, X. Mou,
M. K. Kalra, Y. Zhang, L. Sun, and G. Wang, ``Low-dose CT image denoising using
a generative adversarial network with Wasserstein distance and perceptual
loss,'' \emph{IEEE Trans. Med. Imaging}, vol.~37, no.~6, pp.~1348--1357, 2018.

\bibitem{huang2022dugan} Z. Huang, J. Zhang, Y. Zhang, and H. Shan, ``DU-GAN:
Generative adversarial networks with dual-domain U-Net-based discriminators for
low-dose CT denoising,'' \emph{IEEE Trans. Instrum. Meas.}, vol.~71, pp.~1--12,
2022.

\bibitem{wang2023ctformer} D. Wang, F. Fan, Z. Wu, R. Liu, F. Wang, and H. Yu,
``CTformer: Convolution-free Token2Token dilated vision transformer for
low-dose CT denoising,'' \emph{Phys. Med. Biol.}, vol.~68, no.~6, p.~065012,
2023.

\bibitem{zhang2021transct} Z. Zhang, L. Yu, X. Liang, W. Zhao, and L. Xing,
``TransCT: Dual-path transformer for low dose computed tomography,'' in
\emph{MICCAI}, LNCS 12906, 2021, pp.~55--64.

\bibitem{wang2021tednet} D. Wang, Z. Wu, and H. Yu, ``TED-Net: Convolution-free
T2T vision transformer-based encoder-decoder dilation network for low-dose CT
denoising,'' in \emph{MLMI}, LNCS 12966, 2021, pp.~416--425.

\bibitem{chen2024litformer} Z. Chen, C. Niu, Q. Gao, G. Wang, and H. Shan,
``LIT-Former: Linking in-plane and through-plane transformers for simultaneous
CT image denoising and deblurring,'' \emph{IEEE Trans. Med. Imaging}, vol.~43,
no.~5, pp.~1880--1894, 2024.

\bibitem{gao2024corediff} Q. Gao, Z. Li, J. Zhang, Y. Zhang, and H. Shan,
``CoreDiff: Contextual error-modulated generalized diffusion model for low-dose
CT denoising and generalization,'' \emph{IEEE Trans. Med. Imaging}, vol.~43,
no.~2, pp.~745--759, 2024.

\bibitem{gao2025need} Q. Gao, Z. Chen, D. Zeng, J. Zhang, J. Ma, and H. Shan,
``Noise-inspired diffusion model for generalizable low-dose CT
reconstruction,'' \emph{Med. Image Anal.}, vol.~105, p.~103710, 2025.

\bibitem{chen2026founddiff} Z. Chen, Q. Gao, Z. Li, J. Zhang, Y. Zhang,
J. Zhao, and H. Shan, ``FoundDiff: Foundational diffusion model for
generalizable low-dose CT denoising,'' \emph{IEEE Trans. Med. Imaging},
vol.~45, no.~8, pp.~4366--4379, 2026.

\bibitem{sun2026wdbdm} Q. Sun, T. Li, G. Wang, Y. Huang, S. Dong, L. Yu,
K. Shi, Z. Yao, Y. Fu, and B. Hu, ``WDBDM: Wavelet-based dual-branch diffusion
model for low-dose CT and PET denoising,'' \emph{Comput. Med. Imaging Graph.},
vol.~133, p.~102785, 2026.

\bibitem{li2026mobilemambaunet} J. Li, H. Liu, X. Wang, and J. Hong,
``Efficient low-dose CT image enhancement using MobileMamba-UNet with
wavelet-enhanced long-range modeling,'' \emph{J. Appl. Clin. Med. Phys.},
vol.~27, no.~7, p.~e70680, 2026.

\bibitem{tang2025mlarunet} H. Tang, N. Que, Y. Tian, M. Li, A. Perelli, and
Y. Teng, ``MLAR-UNet: LDCT image denoising based on U-Net with multiple
lightweight attention-based modules and residual reinforcement,'' \emph{Phys.
Med. Biol.}, vol.~70, no.~4, p.~045021, 2025.

\bibitem{yao2026fmdnet} Y. Yao, Y. Liang, W. Xiao, Z. Zhou, Y. Xu, Y. Pan, and
X. Xia, ``FMDNet: Spatial-frequency feature routing for low-dose CT denoising,''
\emph{J. Appl. Clin. Med. Phys.}, vol.~27, no.~6, p.~e70656, 2026.

\bibitem{li2025ctmamba} L. Li, W. Wei, L. Yang, W. Zhang, J. Dong, Y. Liu,
H. Huang, and W. Zhao, ``CT-Mamba: A hybrid convolutional state space model for
low-dose CT denoising,'' \emph{Comput. Med. Imaging Graph.}, vol.~124,
p.~102595, 2025.

\bibitem{li2026amfanet} J. Li, Y. Li, F. Qi, S. Wang, Z. Zhang, Z. Huang, and
Z. Yu, ``Lightweight network enhancing high-resolution feature representation
for efficient low dose CT denoising,'' \emph{IEEE J. Biomed. Health Inform.},
vol.~30, no.~1, pp.~564--574, 2026.

\bibitem{liu2018mwcnn} P. Liu, H. Zhang, K. Zhang, L. Lin, and W. Zuo,
``Multi-level wavelet-CNN for image restoration,'' in \emph{CVPRW}, 2018,
pp.~773--782.

\bibitem{li2020wavecnet} Q. Li, L. Shen, S. Guo, and Z. Lai, ``Wavelet
integrated CNNs for noise-robust image classification,'' in \emph{CVPR}, 2020,
pp.~7243--7252.

\bibitem{huang2021linn} J.-J. Huang and P. L. Dragotti, ``LINN: Lifting inspired
invertible neural network for image denoising,'' in \emph{EUSIPCO}, 2021,
pp.~636--640.

\bibitem{huang2022winnet} J.-J. Huang and P. L. Dragotti, ``WINNet:
Wavelet-inspired invertible network for image denoising,'' \emph{IEEE Trans.
Image Process.}, vol.~31, pp.~4377--4392, 2022.

\bibitem{wang2026adasffuse} M. Wang, Z. Liu, K. Li, Y. Wang, Y. Wang, Y. Wei,
and F. Wang, ``Task-generalized adaptive cross-domain learning for multimodal
image fusion,'' \emph{IEEE Trans. Multimedia}, vol.~28, pp.~4624--4637, 2026.

\bibitem{wang2025wavefusion} Q. Wang, Z. Li, S. Zhang, N. Chi, and Q. Dai,
``WaveFusion: A novel wavelet vision transformer with saliency-guided
enhancement for multimodal image fusion,'' \emph{IEEE Trans. Circuits Syst.
Video Technol.}, vol.~35, no.~8, pp.~7526--7542, 2025.

\bibitem{daubechies1998factoring} I. Daubechies and W. Sweldens, ``Factoring
wavelet transforms into lifting steps,'' \emph{J. Fourier Anal. Appl.}, vol.~4,
no.~3, pp.~247--269, 1998.

\bibitem{sweldens1996lifting} W. Sweldens, ``The lifting scheme: A custom-design
construction of biorthogonal wavelets,'' \emph{Appl. Comput. Harmon. Anal.},
vol.~3, no.~2, pp.~186--200, 1996.

\bibitem{vetterli1986filter} M. Vetterli, ``Filter banks allowing perfect
reconstruction,'' \emph{Signal Process.}, vol.~10, no.~3, pp.~219--244, 1986.

\bibitem{vetterli1995wavelets} M. Vetterli and J. Kovacevic, \emph{Wavelets and
Subband Coding}. Englewood Cliffs, NJ, USA: Prentice Hall, 1995.

\bibitem{donoho1994ideal} D. L. Donoho and I. M. Johnstone, ``Ideal spatial
adaptation by wavelet shrinkage,'' \emph{Biometrika}, vol.~81, no.~3,
pp.~425--455, 1994.

\bibitem{donoho1995denoising} D. L. Donoho, ``De-noising by
soft-thresholding,'' \emph{IEEE Trans. Inf. Theory}, vol.~41, no.~3,
pp.~613--627, 1995.

\bibitem{wang2020orthogonal} J. Wang, Y. Chen, R. Chakraborty, and S. X. Yu,
``Orthogonal convolutional neural networks,'' in \emph{CVPR}, 2020,
pp.~11502--11512.

\bibitem{mccollough2020tcia} C. McCollough, B. Chen, D. R. Holmes III, X. Duan,
Z. Yu, L. Yu, S. Leng, and J. Fletcher, ``Low dose CT image and projection data
(LDCT-and-Projection-data),'' The Cancer Imaging Archive, 2020, doi:
10.7937/9NPB-2637.

\bibitem{moen2021ldctdata} T. R. Moen, B. Chen, D. R. Holmes, X. Duan, Z. Yu,
L. Yu, S. Leng, J. G. Fletcher, and C. H. McCollough, ``Low-dose CT image and
projection dataset,'' \emph{Med. Phys.}, vol.~48, no.~2, pp.~902--911, 2021.

\bibitem{leuschner2021lodopab} J. Leuschner, M. Schmidt, D. O. Baguer, and
P. Maass, ``LoDoPaB-CT, a benchmark dataset for low-dose computed tomography
reconstruction,'' \emph{Sci. Data}, vol.~8, p.~109, 2021.

\end{thebibliography}

\end{document}
