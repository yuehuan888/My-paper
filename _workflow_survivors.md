## §5.3 的 S1 闭环误差 1.25e-01 在仓库中查无出处（实测为 n=1 的 1.49e-01）
verdict=CONFIRMED impact=medium cost=零算力（改文字）；若要从 n=1 补到 n=5 需重训 S1 的 5 个 unconstrained 臂——但既然 w3_unconstrained 的 5 个 run 都在，重建后只需重训+重测（约 5×304s 训练 + 秒级测量）

**claim:** 主张的实质成立，且我找到比主张本身更强的证据。逐项核对：(1) S1↔w3_ 的映射由代码确证——analysis_extended.py L57 `"S1": {"prefix": "w3_", "seeds": range(5)}`、L299 `("roundtrip_checkpoints_w3.json", "checkpoints_S1")`，所以 w3.json 就是 S1，lit.json 就是 S2，主张的映射没错。(2) roundtrip_checkpoints_w3.json 三臂全部 n=1，unconstrained = 0.14916279911994934；论文 L387 该格写 1.25e-01。同一行的 pr/fixed（1.62e-07 / 1.64e-07）与 w3.json 精确吻合（1.6191729912407027e-07 / 1.6383954459797678e-07），唯独第三格对不上——这不是另一个测量口径，是同一个文件里两格对得上、一格对不上。(3) 我独立扫描了全仓 JSON（含 analysis_extended.json 的 /roundtrip/checkpoints_S1，同样 n=1），不存在任何含 5 个 S1 闭环值的数组；对 [0.115,0.132] 全区间的数值扫描只命中 calibrate_mismatch.json 的 /target=0.1253 本身，即那个硬编码常量，没有任何独立测量值 ≈0.125。(4) 论文 L378-380 写 "on the final checkpoint of every seed"，而 S1 的 5 个 run 目录（w3_unconstrained_s0..s4）虽在，.pth 计数为 0（find 全仓 0 个），实际只有 s0 的 checkpoint 被测过。(5) 主张说「L363 是 per-seed range」——行号错了（该句实际在 L390），但引文逐字存在，属引用瑕疵不影响实质。主张漏掉的两点使情况比它描述的更糟：其一，该句在 LaTeX 里出现两次，main.tex L516 正文与 L522 表注都写 "on the final checkpoint of every seed"，表注也撒了谎；其二，"per-seed range 0.11–0.15" 在 n=1 下不仅无支撑而且逻辑上不可能，其出处是 calibrate_mismatch.py L48 的 `TARGET_LO, TARGET_HI = 0.11, 0.15`——一个标定用的设计旋钮被论文当成了实测逐种子范围。最关键的是 e9d7113 的 commit message 自己写着「论文纠错（三处数字查无出处）… §5.3: 闭环误差 99% -> 12.5-14.5%（实测 checkpoints，差约 7 倍）」：作者当时正在修一个"查无出处"的数，却用一个同样查无出处的数替换了它，且同一 commit 才首次创建 roundtrip_checkpoints_w3.json（n=1，值 0.1492，git log 显示该文件仅此一次提交）。所以循环引用是坐实的：calibrate_mismatch.py 的注释称该值来自论文 §5.3，而论文 §5.3 无任何产物可溯。反向风险（"1.253e-01 源自某次未落盘的早期测量，论文没错只是产物链断了"）基本可排除：range 0.11–0.15 同样无源且不可能是测量，而 0.1253 在全仓的唯一出现就是那个常量自身。两种情形的修法确实相同，但证据天平明显偏向"该数无源"。

**reason:** 主张的实质成立，且我找到比主张本身更强的证据。逐项核对：(1) S1↔w3_ 的映射由代码确证——analysis_extended.py L57 `"S1": {"prefix": "w3_", "seeds": range(5)}`、L299 `("roundtrip_checkpoints_w3.json", "checkpoints_S1")`，所以 w3.json 就是 S1，lit.json 就是 S2，主张的映射没错。(2) roundtrip_checkpoints_w3.json 三臂全部 n=1，unconstrained = 0.14916279911994934；论文 L387 该格写 1.25e-01。同一行的 pr/fixed（1.62e-07 / 1.64e-07）与 w3.json 精确吻合（1.6191729912407027e-07 / 1.6383954459797678e-07），唯独第三格对不上——这不是另一个测量口径，是同一个文件里两格对得上、一格对不上。(3) 我独立扫描了全仓 JSON（含 analysis_extended.json 的 /roundtrip/checkpoints_S1，同样 n=1），不存在任何含 5 个 S1 闭环值的数组；对 [0.115,0.132] 全区间的数值扫描只命中 calibrate_mismatch.json 的 /target=0.1253 本身，即那个硬编码常量，没有任何独立测量值 ≈0.125。(4) 论文 L378-380 写 "on the final checkpoint of every seed"，而 S1 的 5 个 run 目录（w3_unconstrained_s0..s4）虽在，.pth 计数为 0（find 全仓 0 个），实际只有 s0 的 checkpoint 被测过。(5) 主张说「L363 是 per-seed range」——行号错了（该句实际在 L390），但引文逐字存在，属引用瑕疵不影响实质。主张漏掉的两点使情况比它描述的更糟：其一，该句在 LaTeX 里出现两次，main.tex L516 正文与 L522 表注都写 "on the final checkpoint of every seed"，表注也撒了谎；其二，"per-seed range 0.11–0.15" 在 n=1 下不仅无支撑而且逻辑上不可能，其出处是 calibrate_mismatch.py L48 的 `TARGET_LO, TARGET_HI = 0.11, 0.15`——一个标定用的设计旋钮被论文当成了实测逐种子范围。最关键的是 e9d7113 的 commit message 自己写着「论文纠错（三处数字查无出处）… §5.3: 闭环误差 99% -> 12.5-14.5%（实测 checkpoints，差约 7 倍）」：作者当时正在修一个"查无出处"的数，却用一个同样查无出处的数替换了它，且同一 commit 才首次创建 roundtrip_checkpoints_w3.json（n=1，值 0.1492，git log 显示该文件仅此一次提交）。所以循环引用是坐实的：calibrate_mismatch.py 的注释称该值来自论文 §5.3，而论文 §5.3 无任何产物可溯。反向风险（"1.253e-01 源自某次未落盘的早期测量，论文没错只是产物链断了"）基本可排除：range 0.11–0.15 同样无源且不可能是测量，而 0.1253 在全仓的唯一出现就是那个常量自身。两种情形的修法确实相同，但证据天平明显偏向"该数无源"。

**refined:** 论文 §5.3 机制表的 S1 格（1.25e-01）及其"5 种子均值 / 逐种子范围 0.11–0.15"标注在仓库中查无出处：S1 唯一的实测是 roundtrip_checkpoints_w3.json 的 n=1、0.1491628（S2 的 n=3、0.1451 才对得上），而"5 种子"与"0.11–0.15"两个说法都源自 calibrate_mismatch.py 里一个以论文自身为出处的硬编码常量（TARGET=1.253e-01 / TARGET_LO,HI=0.11,0.15，e9d7113 首次引入）——即作者在前一个 commit 修掉"99% 查无出处"时，用另一个同样查无出处的数替换了它。因此该表及表注（main.tex L516/L522）必须改为如实标注 S1 n=1、S2 n=3 并填入实测值（1.49e-01 / 1.45e-01），或在环境重建后重训 w3_unconstrained s0–s4 补到真正的 n=5；机制结论（六 orders of magnitude、"12–15%"）与因果结论（锚在实测 8.7%）均不受影响，但 ε 网格的标定依据需要一并说明。

**required_work:** 零算力（立即做，改文字，注意三处副本 + 表注）：1) conference_paper.md L387 与 conference_paper_zh.md L252、latex/main.tex L531 的 S1 格 1.25e-01 → 1.49e-01；2) L378-380 与 main.tex L516 正文、L522 表注的 "on the final checkpoint of every seed" → 改为如实表述（S1 n=1、S2 n=3），这是本次最该改的一句，因为表注也在撒谎；3) 删除或改写 L390 / main.tex L537 的 "(S1 per-seed range 0.11–0.15)"——n=1 无范围可言，若要保留范围须明确标为 S2 的 n=3 逐种子范围（0.119–0.164）；4) L389 与 L54 摘要的 "12–15%" 口径复核（实测 S1 14.9%、S2 均值 14.5%、S2 极值至 16.4%，"12–15%" 只覆盖两个均值）；5) calibrate_mismatch.py L46-48 的注释删去「（论文 §5.3）」这一循环出处，并注明 ε 网格是以一个未落盘的目标值标定的；6) README.md L111-112 的「实测为 12.5–14.5%」同源，需一并回改，否则历史文档的已知错误清单里又要新增一条。重建环境后（廉价，可选但推荐，因为论文自己给审稿人指了 `verify_roundtrip.py --checkpoints` 这条路）：w3_unconstrained_s0..s4 的 config.json/history.json/results.json 都在、协议可复原（30 epoch、w3 划分、seed 0-4），权重已删故需重训约 5×304s，再跑 `verify_roundtrip.py --checkpoints --prefix w3_` 得到真正 n=5 的 S1 值（注意该脚本遇缺失 checkpoint 静默跳过并照写 JSON，正是造成 n=1 的成因，建议顺手加一条断言/告警），届时用新均值替换 1.49e-01 或如实保留 n=1；如需彻底自洽，用真实 S1 均值重跑 calibrate_mismatch.py 复核 ε 档位（以 0.1492 线性插值会得 ε≈0.264 而非 0.222，剂量-响应单调与穿越固定 Haar 的定性结论稳健，但表内 ε 值会变）。

## 贡献点 3 的证据确为手工 taps 扫描；§5.3 的 S1 闭环数字（1.25e-01 / 0.11–0.15）在仓库中查无出处，且已提交的 Fig. 2(a) 印的是 1.49e-01，与正文表格直接冲突
verdict=CONFIRMED impact=medium cost=零算力（重跑 verify_roundtrip.py --checkpoints 覆盖全部 5 种子并提交产物）+ 廉价消融（真跑一次无界 PR 训练臂测漂移）

**claim:** 核查结论：该审计的事实内核成立，且比它自己声称的更强；但其中两个子主张和一个成本估计是错的，且它作为"贡献点"完全不成立（属勘误级修复，非学术增量）。逐条：(1) 手工扫描——CONFIRMED。贡献 3 的"3.6e-07→5.2e+00"唯一可追溯来源是 tests/test_lifting_pr.py 的 §6：它手工把 p_init/u_init 按 {1.0,2.0,3.5,5.3} 缩放、置 bound=0.0、theta 清零，即"假设 taps 已在幅度 X、float32 闭环如何恶化"；lifting_dwt.py:82-92 docstring 的表格（|P|≈5.3→1e+01，float64 复测 σ=3.0 时 12.19→6.3e-07）就是这次扫描的产物。runs/ 46 个目录里没有任何 bound=0 / 无界 PR 训练臂：w3_pr_s0..s4、lit_pr_s0..s2 全用默认 bound=0.5，w3_unconstrained_* 是 legacy 分离参数臂，mis*_s* 是 (1−ε)U 注入臂。此外该内核对论文措辞的指控成立：正文"without it the round-trip error degrades from 3.6e-07 to 5.2e+00"写成学习结果，而证据是手工设定。(2) S1 数字——CONFIRMED，且更强。正文（conference_paper.md:378-390 与 latex/main.tex:516,522,531,537）称"on the final checkpoint of every seed"、S1 unconstrained = 1.25e-01、S1 逐种子范围 0.11–0.15；calibrate_mismatch.py 把它硬编码为 TARGET=1.253e-01 并注明"S1 五种子均值"，TARGET_LO/HI=0.11/0.15 注明"S1 逐种子范围"——干预实验的 ε=0.222 正由此标定。然而仓库唯一的 S1 产物 roundtrip_checkpoints_w3.json 是 n=1、值 0.14916279911994934；analysis_extended.json 的 checkpoints_S1 也拷的是这份 n=1；runs/*/results.json 不含任何 roundtrip 字段（train.py:303 只在 stdout 打印）；identity_floors.json 无关。git 显示该文件自 e9d7113 引入时就是 n=1，从未有过 5 种子版本。更强的一点：已提交的 figures/fig2_mech.png 读这份 n=1 文件，图上印的是 1.49e-01，且 yerr=0（make_figures.py 在 vals.size<=1 时 stds=0.0），即图与表在论文的核心机制数字上互相打架，且"零误差棒"与正文"every seed / range 0.11–0.15"直接矛盾。对照之下 S2 列 1.45e-01 与 roundtrip_checkpoints_lit.json 均值 0.145137 精确吻合——S1 是唯一的例外。(3) 被驳的子主张：verify_roundtrip.json 的 paper_relL2（0.99/9.3e-08）不是"自相矛盾"，而是刻意保留的对账台账字段（verify_roundtrip.py 的 PAPER dict 注释写明"论文 §4.3 声称的值，用于对账"，字段名就叫 paper_*）；其存在本身就是"旧值复现不出来"的记录，且当前论文已不再引用 9.3e-08/9.9e-01（.md/.tex grep 零命中）。(4) 被驳的成本："零算力重跑 verify_roundtrip.py --checkpoints"是错的——全仓库无任何 *.pth（.gitignore:25 忽略，find 零命中），而该脚本对缺失 checkpoint 是静默 continue，今天跑它只会写出一个空 dict。要得到真正的 5 种子 S1 数字必须重训 w3 三臂共 15 个模型（各 30 轮，6GB 单卡），并被任务 #1/#2（环境、数据集）阻塞。(5) 其"更接近 S2 散布"的归因是推测而非证据：S2 的 per_seed=[0.1643,0.1190,0.1521]、均值 0.1451，而 S1 写的是 0.1253、范围 0.11–0.15，两者均值对不上，只能判为"出处不明"，不能判为"抄自 S2"。对论文结论的影响是中等而非致命：§5.2 的三臂 PSNR（30.5341±0.0305 / 30.5591 / 30.8314）全部来自 runs/、站得住；把 S1 换成实测 14.9% 后"六个数量级"仍为 6.0 个数量级，干预的 8.7% 与 14.9% 仍同量级，单调性与显著性均不变；坏掉的只是可复现性与图-表一致性。

**reason:** 核查结论：该审计的事实内核成立，且比它自己声称的更强；但其中两个子主张和一个成本估计是错的，且它作为"贡献点"完全不成立（属勘误级修复，非学术增量）。逐条：(1) 手工扫描——CONFIRMED。贡献 3 的"3.6e-07→5.2e+00"唯一可追溯来源是 tests/test_lifting_pr.py 的 §6：它手工把 p_init/u_init 按 {1.0,2.0,3.5,5.3} 缩放、置 bound=0.0、theta 清零，即"假设 taps 已在幅度 X、float32 闭环如何恶化"；lifting_dwt.py:82-92 docstring 的表格（|P|≈5.3→1e+01，float64 复测 σ=3.0 时 12.19→6.3e-07）就是这次扫描的产物。runs/ 46 个目录里没有任何 bound=0 / 无界 PR 训练臂：w3_pr_s0..s4、lit_pr_s0..s2 全用默认 bound=0.5，w3_unconstrained_* 是 legacy 分离参数臂，mis*_s* 是 (1−ε)U 注入臂。此外该内核对论文措辞的指控成立：正文"without it the round-trip error degrades from 3.6e-07 to 5.2e+00"写成学习结果，而证据是手工设定。(2) S1 数字——CONFIRMED，且更强。正文（conference_paper.md:378-390 与 latex/main.tex:516,522,531,537）称"on the final checkpoint of every seed"、S1 unconstrained = 1.25e-01、S1 逐种子范围 0.11–0.15；calibrate_mismatch.py 把它硬编码为 TARGET=1.253e-01 并注明"S1 五种子均值"，TARGET_LO/HI=0.11/0.15 注明"S1 逐种子范围"——干预实验的 ε=0.222 正由此标定。然而仓库唯一的 S1 产物 roundtrip_checkpoints_w3.json 是 n=1、值 0.14916279911994934；analysis_extended.json 的 checkpoints_S1 也拷的是这份 n=1；runs/*/results.json 不含任何 roundtrip 字段（train.py:303 只在 stdout 打印）；identity_floors.json 无关。git 显示该文件自 e9d7113 引入时就是 n=1，从未有过 5 种子版本。更强的一点：已提交的 figures/fig2_mech.png 读这份 n=1 文件，图上印的是 1.49e-01，且 yerr=0（make_figures.py 在 vals.size<=1 时 stds=0.0），即图与表在论文的核心机制数字上互相打架，且"零误差棒"与正文"every seed / range 0.11–0.15"直接矛盾。对照之下 S2 列 1.45e-01 与 roundtrip_checkpoints_lit.json 均值 0.145137 精确吻合——S1 是唯一的例外。(3) 被驳的子主张：verify_roundtrip.json 的 paper_relL2（0.99/9.3e-08）不是"自相矛盾"，而是刻意保留的对账台账字段（verify_roundtrip.py 的 PAPER dict 注释写明"论文 §4.3 声称的值，用于对账"，字段名就叫 paper_*）；其存在本身就是"旧值复现不出来"的记录，且当前论文已不再引用 9.3e-08/9.9e-01（.md/.tex grep 零命中）。(4) 被驳的成本："零算力重跑 verify_roundtrip.py --checkpoints"是错的——全仓库无任何 *.pth（.gitignore:25 忽略，find 零命中），而该脚本对缺失 checkpoint 是静默 continue，今天跑它只会写出一个空 dict。要得到真正的 5 种子 S1 数字必须重训 w3 三臂共 15 个模型（各 30 轮，6GB 单卡），并被任务 #1/#2（环境、数据集）阻塞。(5) 其"更接近 S2 散布"的归因是推测而非证据：S2 的 per_seed=[0.1643,0.1190,0.1521]、均值 0.1451，而 S1 写的是 0.1253、范围 0.11–0.15，两者均值对不上，只能判为"出处不明"，不能判为"抄自 S2"。对论文结论的影响是中等而非致命：§5.2 的三臂 PSNR（30.5341±0.0305 / 30.5591 / 30.8314）全部来自 runs/、站得住；把 S1 换成实测 14.9% 后"六个数量级"仍为 6.0 个数量级，干预的 8.7% 与 14.9% 仍同量级，单调性与显著性均不变；坏掉的只是可复现性与图-表一致性。

**refined:** §5.3 的 S1 闭环数字（1.25e-01 五种子均值、0.11–0.15 逐种子范围）在仓库中查无出处——唯一的 S1 产物 roundtrip_checkpoints_w3.json 是 n=1 且值为 1.49e-01，已提交的 Fig. 2(a) 印的正是 1.49e-01，与正文表格在论文的核心机制数字上直接冲突；同时贡献 3 的"3.6e-07→5.2e+00"来自 tests/test_lifting_pr.py §6 的手工 taps 缩放扫描（手工置 bound=0、θ=0），并非任何真实训练臂的参数漂移，仓库 46 次运行中不存在无界 PR 训练臂。

**required_work:** 按优先级：(1) 立刻修 §5.3，且不依赖任何算力：把 S1 列改为 n=1 实测 1.49e-01，删掉"on the final checkpoint of every seed"、"S1 per-seed range 0.11–0.15"、以及 calibrate_mismatch.py 里"五种子均值/逐种子范围"的措辞；同时把 calibrate_mismatch.py/json 的 TARGET 改为有出处的数（要么 1.49e-01，要么干脆改用 S2 的 0.1451 并在文中标明 ε 是按 S2 标定的），否则干预实验的剂量选择仍挂在无源数字上。注意：这一步会让 ε=0.222 与目标的对应关系改变，需在文中如实说明（8.7% vs 14.9% 仍是同量级，机制的"充分性"论证方向不变）。(2) 重生成 fig2_mech.png，使图与表同数（当前 1.49e-01 vs 1.25e-01 是审稿人用肉眼就能命中的矛盾）。(3) 给 verify_roundtrip.py --checkpoints 加硬失败（缺失 checkpoint 时报错而非静默 continue），否则它今天会写空 dict 且没人发现。(4) 真正的 5 种子 S1 数字只有重训 w3 三臂 15 个模型才能拿到——被任务 #1/#2 阻塞，不要把它算作"零算力"。(5) 贡献 3 的表述降级或补强：要么明写"这是受控 taps 幅度扫描 + float32/float64 对照（单元测试级观测），不是训练漂移"，要么跑已写好的 run_ablation.ps1 bound 扫描把断言变成剂量-响应曲线（4 次训练，可顺带检验 PSNR 是否跟随闭环误差，使贡献 3 获得与贡献 2 同级的因果地位）。切勿把本项审计作为贡献写进论文。

## 证据一致性风险: §5.3 的 S1 无约束闭环数与盘上产物不符 —— 事实成立，但这是"必须修的出处缺陷"，不是创新点
verdict=CONFIRMED impact=low cost=廉价但需环境（GPU + checkpoints 或重训 30 轮 × 5 种子）；重训成本 = 现有 w3 三臂脚本的一半

**claim:** 主张的事实部分我逐条核对后成立，且比它自己说的更严重；但它不是贡献点，是一个必须修的内部一致性问题。

【成立的部分（硬证据）】
1. 论文 §5.3 S1 列 `unconstrained` = 1.25e-01（conference_paper.md:387、main.tex:531、zh:252），而全仓唯一的 S1 产物 roundtrip_checkpoints_w3.json 记的是 0.14916279911994934（n=1）。相差 19%。
2. 映射无歧义：w3_*_s0..s4 五个运行 config 的 n_test=435（=S1），lit_* 三个 n_test=211（=S2）；且 S1 列的 pr=1.62e-07、fixed=1.64e-07 与 w3 文件里那两个 n=1 值（1.6192e-07 / 1.6384e-07）逐位吻合，S2 列的 pr=1.65e-07 / unconstrained=1.45e-01 又恰好等于 lit 的均值 1.6454e-07 / 0.145137。所以"w3=S1、lit=S2"成立，1.45e-01 对得上、1.25e-01 对不上。
3. "on the final checkpoint of every seed"（:363-364）与 n=1 冲突 —— 而且不止 unconstrained：S1 三行全部来自那个 n=1 的文件。只有 S2 是真 n=3。
4. "S1 per-seed range 0.11–0.15"（:390）不可由 S1 产物导出（n=1 无 range）。附带纠正：它也不严格等于 S2 的 range（lit per_seed=[0.1190,0.1521,0.1643]，即 0.12–0.16），所以主张里"只可能来自 S2 那 3 个样本"这一步是推不出来的。
5. 不可复算：全仓 find *.pth/*.pt/*.ckpt = 0；.gitignore:25 就是 `*.pth`，:22 注明只有 config/results/history/DONE 必须入库 —— 即 checkpoint 从未入库；history.json 只记 epoch/train_l1/val_psnr/val_ssim，该指标训练时根本没被记录，所以现有产物里无法重建。比主张描述更糟的是：from_checkpoints() 对 s0..s4 逐个 `continue` 跳过缺失文件，今天跑 `--checkpoints` 会写出 {} 并**覆盖掉唯一幸存的 S1 记录**。
6. 出处追踪：git 全历史 grep `1.25e-01|0.1253|1.253e-01` 只命中 calibrate_mismatch.py/.json（硬编码 TARGET=1.253e-01，注释自称"S1 五种子均值"）、make_figures.py 注释、论文三份文件 —— **从未命中任何测量产物**。引入该数的 e9d7113 的 commit message 自述 "§5.3: 闭环误差 99% -> 12.5-14.5%（实测 checkpoints，差约 7 倍）"，即作者意图是"两个划分分别 12.5%/14.5%"。另查到更早产物 LDCT_三臂对照结果_2026-09-13.md:44-48 里那张表仍是 9.3e-08/1.1e-07/9.9e-01（来自旧融合阶段 legacy_dwt 的口径），同一文档 §六.5 已明确留 TODO"完整训练的三臂闭环应从各自 checkpoint 复测"——说明这次 checkpoint 复测就是为了替换它。
7. 1.25e-01 在观测分布里不具代表性：四个已测 unconstrained 闭环值（0.1492, 0.1643, 0.1190, 0.1521）中只有一个低于 0.1253；1.253e-01 既低于唯一的 S1 实测值，也低于 S2 均值。

【我尝试反驳但失败的点】
- 不能证明"1.25e-01 是伪造的"：若 S1 五种子为 [0.1492, ~0.11, ~0.11, ~0.15, ~0.13]，均值确可凑到 0.1253。所以正确措辞是"任何幸存产物都不支持它"，而非"它是错的"。主张的标题用"不一致/证据一致性风险"是站得住的。
- 但也不能推翻：没有任何替代探针或替代划分能让 1.25e-01 落地（calibrate_mismatch.py 与 verify_roundtrip.py 共用同一固定 256×256、seed=7 探针，这正是为了可比性；换成真实切片反而毁掉与干预臂的可比性）。50 步协议下的 unconstrained 是 3.05e-02（verify_roundtrip.json），旧阶段是 0.64–1.20，12.5% 两头都不靠。

【为什么它不是创新点】
- 结论不变：若 S1 真值取 1.49e-01，正文"12–15% of the signal is lost"仍覆盖 14.9%，与 1.64e-07 之比仍是 9.1e5 ≈ 六个数量级。摘要 :20/:54 的 "12–15%" 也侥幸存活（诚实写法应为 12–16%，range 0.11–0.16）。即改一个格子、改一个 range 字符串，不动任何科学结论。
- 零新颖性、零可发表性：这是 artifact provenance 修复，ICIP/EUSIPCO/ICME 不会因为"作者把 n 写清楚了"给分，4–6 页里多写这段纯属浪费版面。
- 主张自评也对："不是新颖性风险，是可复现性风险"。所以它是 action item（v0.2 必做），不是 contribution。

【主张里两处小错，记录以免照抄】
- "paper/latex/main.tex:506" 错，表格行在 main.tex:531（:506 是 WINNet 段落）。
- "重训成本 = w3 三臂脚本的一半"偏了：w3 套件是 3 臂 × 5 种子 = 15 次运行，复现 S1 只需 unconstrained × 5 = 1/3。
- 另有一处它没看到但更该修的连带影响：calibrate_mismatch.py 的 TARGET=1.253e-01 与 run_intervention.ps1 的注释"eps = 0.222 -> round-trip ~12.5% <- matches the unconstrained arm's mean"都锚在这个未落实的数上，而 ε=0.222 实测只到 8.7e-02，比无约束臂真实的 ~1.49e-01 低 1.7 倍。修完表后必须同步改 §5.3 的 "Honest boundary" 段与标定注释，否则"剂量匹配"的话术与表里唯一的实测值自相矛盾。

**reason:** 主张的事实部分我逐条核对后成立，且比它自己说的更严重；但它不是贡献点，是一个必须修的内部一致性问题。

【成立的部分（硬证据）】
1. 论文 §5.3 S1 列 `unconstrained` = 1.25e-01（conference_paper.md:387、main.tex:531、zh:252），而全仓唯一的 S1 产物 roundtrip_checkpoints_w3.json 记的是 0.14916279911994934（n=1）。相差 19%。
2. 映射无歧义：w3_*_s0..s4 五个运行 config 的 n_test=435（=S1），lit_* 三个 n_test=211（=S2）；且 S1 列的 pr=1.62e-07、fixed=1.64e-07 与 w3 文件里那两个 n=1 值（1.6192e-07 / 1.6384e-07）逐位吻合，S2 列的 pr=1.65e-07 / unconstrained=1.45e-01 又恰好等于 lit 的均值 1.6454e-07 / 0.145137。所以"w3=S1、lit=S2"成立，1.45e-01 对得上、1.25e-01 对不上。
3. "on the final checkpoint of every seed"（:363-364）与 n=1 冲突 —— 而且不止 unconstrained：S1 三行全部来自那个 n=1 的文件。只有 S2 是真 n=3。
4. "S1 per-seed range 0.11–0.15"（:390）不可由 S1 产物导出（n=1 无 range）。附带纠正：它也不严格等于 S2 的 range（lit per_seed=[0.1190,0.1521,0.1643]，即 0.12–0.16），所以主张里"只可能来自 S2 那 3 个样本"这一步是推不出来的。
5. 不可复算：全仓 find *.pth/*.pt/*.ckpt = 0；.gitignore:25 就是 `*.pth`，:22 注明只有 config/results/history/DONE 必须入库 —— 即 checkpoint 从未入库；history.json 只记 epoch/train_l1/val_psnr/val_ssim，该指标训练时根本没被记录，所以现有产物里无法重建。比主张描述更糟的是：from_checkpoints() 对 s0..s4 逐个 `continue` 跳过缺失文件，今天跑 `--checkpoints` 会写出 {} 并**覆盖掉唯一幸存的 S1 记录**。
6. 出处追踪：git 全历史 grep `1.25e-01|0.1253|1.253e-01` 只命中 calibrate_mismatch.py/.json（硬编码 TARGET=1.253e-01，注释自称"S1 五种子均值"）、make_figures.py 注释、论文三份文件 —— **从未命中任何测量产物**。引入该数的 e9d7113 的 commit message 自述 "§5.3: 闭环误差 99% -> 12.5-14.5%（实测 checkpoints，差约 7 倍）"，即作者意图是"两个划分分别 12.5%/14.5%"。另查到更早产物 LDCT_三臂对照结果_2026-09-13.md:44-48 里那张表仍是 9.3e-08/1.1e-07/9.9e-01（来自旧融合阶段 legacy_dwt 的口径），同一文档 §六.5 已明确留 TODO"完整训练的三臂闭环应从各自 checkpoint 复测"——说明这次 checkpoint 复测就是为了替换它。
7. 1.25e-01 在观测分布里不具代表性：四个已测 unconstrained 闭环值（0.1492, 0.1643, 0.1190, 0.1521）中只有一个低于 0.1253；1.253e-01 既低于唯一的 S1 实测值，也低于 S2 均值。

【我尝试反驳但失败的点】
- 不能证明"1.25e-01 是伪造的"：若 S1 五种子为 [0.1492, ~0.11, ~0.11, ~0.15, ~0.13]，均值确可凑到 0.1253。所以正确措辞是"任何幸存产物都不支持它"，而非"它是错的"。主张的标题用"不一致/证据一致性风险"是站得住的。
- 但也不能推翻：没有任何替代探针或替代划分能让 1.25e-01 落地（calibrate_mismatch.py 与 verify_roundtrip.py 共用同一固定 256×256、seed=7 探针，这正是为了可比性；换成真实切片反而毁掉与干预臂的可比性）。50 步协议下的 unconstrained 是 3.05e-02（verify_roundtrip.json），旧阶段是 0.64–1.20，12.5% 两头都不靠。

【为什么它不是创新点】
- 结论不变：若 S1 真值取 1.49e-01，正文"12–15% of the signal is lost"仍覆盖 14.9%，与 1.64e-07 之比仍是 9.1e5 ≈ 六个数量级。摘要 :20/:54 的 "12–15%" 也侥幸存活（诚实写法应为 12–16%，range 0.11–0.16）。即改一个格子、改一个 range 字符串，不动任何科学结论。
- 零新颖性、零可发表性：这是 artifact provenance 修复，ICIP/EUSIPCO/ICME 不会因为"作者把 n 写清楚了"给分，4–6 页里多写这段纯属浪费版面。
- 主张自评也对："不是新颖性风险，是可复现性风险"。所以它是 action item（v0.2 必做），不是 contribution。

【主张里两处小错，记录以免照抄】
- "paper/latex/main.tex:506" 错，表格行在 main.tex:531（:506 是 WINNet 段落）。
- "重训成本 = w3 三臂脚本的一半"偏了：w3 套件是 3 臂 × 5 种子 = 15 次运行，复现 S1 只需 unconstrained × 5 = 1/3。
- 另有一处它没看到但更该修的连带影响：calibrate_mismatch.py 的 TARGET=1.253e-01 与 run_intervention.ps1 的注释"eps = 0.222 -> round-trip ~12.5% <- matches the unconstrained arm's mean"都锚在这个未落实的数上，而 ε=0.222 实测只到 8.7e-02，比无约束臂真实的 ~1.49e-01 低 1.7 倍。修完表后必须同步改 §5.3 的 "Honest boundary" 段与标定注释，否则"剂量匹配"的话术与表里唯一的实测值自相矛盾。

**refined:** §5.3 的 S1 无约束闭环数 1.25e-01 在任何入库产物中都没有对应测量（唯一的 S1 产物是 n=1 的 1.49e-01），且因 *.pth 从未入库、训练时也不记录该指标，该数在当前仓库不可复算 —— 这是投稿前必须修的出处与 n 声明缺陷（连同 §5.3/摘要的 12–15% 措辞、干预标定注释），而非可写进论文的新增贡献点。

**required_work:** 零算力即可做（现在就能做完，建议全部并入 v0.2）：
1. 改数：§5.3 表 S1 unconstrained 改为盘上实测值并标注来源文件；把 S1 的 pr/fixed 也标注为 n=1（它们同样来自那个 n=1 文件），或直接删掉 S1 列只保留 S2（n=3），避免"一张表两种样本量"。
2. 改措辞：删/改"on the final checkpoint of every seed"（conference_paper.md:363-364、main.tex 同段），改成"每臂可用 checkpoint 的实测值，样本量见表注"；:390 的 range 与摘要 :20/:54 的 "12–15%" 统一为"S1 14.9% / S2 14.5%，逐种子 11.9–16.4%"这类可核对的区间。
3. 加防御：roundtrip_checkpoints_*.json 落盘前断言 out 非空且每条 n>=1，否则拒绝写文件（当前实现会静默写 {} 覆盖唯一记录）；同时把 from_checkpoints 的 PAPER 字典同步为表里的真实值（现在仍是 9.3e-08/1.1e-07/9.9e-01）。
4. 让未来可复算（3 行改动，零 GPU）：train.py 每轮把 transform_roundtrip 的 rel_l2 写进 history.json，这样即使 checkpoint 不入库，闭环数也永远能从已入库的 history.json 重建 —— 这是根治措施，比补一次测量更值。
5. 同步修标注目标：calibrate_mismatch.py 的 TARGET/target_range 与 run_intervention.ps1:21 的注释（"ε=0.222 -> ~12.5% matches the unconstrained arm's mean"）不得再引用 1.253e-01；§5.3 "Honest boundary" 段要说明注入剂量的标称值是按 8.7e-02 实测口径讲的，而不是按无约束臂的 ~1.49e-01。

需环境重建后才做（优先级低于上面 5 条）：
6. 复现 S1 全 5 种子：重训 w3_unconstrained_s0..s4 共 5 次 30 轮（≈ w3 全 15 次套件的 1/3，不是主张说的"一半"），然后 `python experiments/verify_roundtrip.py --checkpoints --prefix w3_ --n-seeds 5`，把 n 写进表注。第一步应先确认原始训练机（config 指向的 D:\图像相关论文\LDCT）是否还留有 best.pth —— 若有，这一步几乎免费，无需重训。
7. 顺带用同一批 checkpoint 复测 pr/fixed 的 S1 五种子（同一脚本一次跑完），把 S1 列补齐到 n=5，彻底消掉"样本量与 S2 不一致"的质疑。